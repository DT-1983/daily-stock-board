# daily-stock-board 開發日誌

> 產業鏈看板（remote `board`=DT-1983/daily-stock-board、GitHub Pages）。記錄自建功能的重大變更；歷史細節見 git log。

## 2026-07-17 · 七鏈產業深度解讀系統 + 每月本機自動

**看板守備清單 top 6→8**
- `screen.py` TOPN=8、`chain_themes.py` TOP_N=8：每鏈守備清單多顯示 2 檔（tw-board 的 STOCK_LIST 帶動每日分析涵蓋到第 7、8 檔）。

**Gemini 一段題材（每週自動）**
- `chain_themes.py` 一句 →三欄 dict（catalyst/risk/watch），`board_html._theme_html` 做成可展開；weekly-screen 產出、board_html 渲染，舊字串相容。

**七鏈「📖 產業深度解讀」全螢幕彈窗（本日主軸）**
- 內容管線：`chain_reports_src/reports_data/<slug>.json`（研究資料）→ `chain_reports_src/render_reports.py`（可攜、unescape `&lt;b&gt;`）→ repo 根 `chain_reports.json`（保留未更新鏈舊內容）。
- UI：`board_html.py` 每鏈注入 `<button class="rptbtn">` + `.rptmodal` 全螢幕彈窗；CSS 前綴 `.rpt` 不碰既有樣式；手機表格「標籤左欄 absolute + 內容右欄」（避免 flex 把 `<b>` 拆成偽欄位）；多空用綠`多`/紅`空`色塊各一行。
- 內容：TL;DR/產業規模/競爭格局市佔/價值鏈/催化劑/多空 bull-bear/風險/觀察/8+8 檔個股獨立解讀。
- 六鏈（矽光子·機器人·低軌·電力核能·太陽能·Bitcoin）由平行 general-purpose Agent 上網查證 2026 資料 → 結構化 JSON → 統一模板；AI 伺服器維持人工 bespoke（多雲端 capex 表）。
- 報告另存 obis `04_AI Report/Investment/產業鏈深度報告/`。

**每月自動更新（本機、Claude 品質）**
- 用戶要求不犧牲品質 → 不走 Gemini 自動版；改本機排程叫 headless Claude（同早報機制 ClaudeMorningBriefing）。
- Windows 排程 `RefreshChainReports`：每月 1 號 09:23 跑 `C:\Users\Mophy\AI\refresh_reports_monthly.cmd`（headless `claude -p "重跑產業深度報告"`）。
- 指令＝全域 skill `refresh-chain-reports`（研究→渲染→部署→驗證；互動用背景 Agent、headless 用同步）。
- `monthly-report-reminder.yml`：每月 1 號 09:07 Telegram 提醒（+ 電腦沒開的手動備援）。

**踩坑**：tw-board 結尾 `git push || echo 略過` 吞掉 push 失敗 → 部署途中若我 push，發佈 index.html 被拒卻顯示 success、線上靜默不更新。**部署期間絕不 push**。

## 2026-07-18 · 巴菲特價值清單加台股分頁 + 俗貴價核對

**台股分頁（美/台 toggle）**
- `data_tv.get_buffett_snapshot_taiwan`（TV taiwan、TWSE+TPEX、市值 TWD、ticker 帶 .TW）；stage2 的 yfinance `fetch_fundamentals` 逐檔重抓 → 台股完全重用評估邏輯、下游零改。
- `buffett_scan --markets us,tw`：併掃標 market；FinMind TaiwanStockInfo 補中文名。台股門檻 50億（180 檔過關取 76 檔 BUY/WATCH）。
- `buffett_html`：美/台 toggle 分頁（同主看板 UX）、台股去 .TW 顯示代號+中文名+TWD。
- weekly-screen 巴菲特步驟加 FINMIND_TOKEN + `--tw-max-candidates 200`。

**排序：EPS估降殿後** — 洪瑞泰「EPS 變差＝俗價假便宜」，✅體質過關浮上、⚠️EPS估降沉底。

**俗貴價核對（對第一手講稿）** — 詳見記憶 hongruitai_method：
- 爬 mikeon88 官方 blog 確認：俗價 EPS×12（買）、貴價 EPS×30（賣）＝洪瑞泰官方，跟我們一致。
- 合理價 ×20 是我們自補中點、洪瑞泰無官方定義 → **拿掉合理價**，只留買/賣兩線（🟢買進/🟡觀望/🔴太貴）。

## 2026-07-28 · 績效總體檢 + 模擬倉調整（依回測，非短期績效）

**一、實盤 4 週（6/30–7/26）**：巴菲特 +6.7%（唯一正）｜SPY −1.1%｜七鏈 −2.9~−13.4%｜趨勢倉 −20.9%｜Pairs 爆倉。

**二、Pairs 根因＝程式 bug 不是虧損**：pre-v7 sizing 用「股數×HR」而非名目金額拆分（`qty1=qty2*hr`），
對 AVGO_HYG(hr=4.08) 造成 3–7 倍槓桿 → 虛擬帳戶 10,000 → −15,124（虧 251%，數學上不可能）。
v7 已改正確拆分，但負現金讓 alloc_cap 也變負 → 報酬率正負號翻轉。已加護欄 + `/pairs_reset`（待用戶執行）。

**三、SuperTrend 診斷（關鍵）**
- 日線 32 次賣出有 **24 次(75%)賣完股價續漲**（+8.2%/10日）；買進訊號正常（+10.4%/20日）→ **問題單獨在賣出**
- 同池同期：日線 **65 次**訊號 vs 週線 **10 次**，多出來的是雜訊（AI 股日內波動 > ATR10×3 軌道寬）
- 2026 YTD 回測（13 檔池）：買進持有 +78.6%/MDD−14.3% ｜ 週線 +71.1%/−16.1%/8次 ｜ 日線+2天確認 +36.4%/−20.2% ｜ 日線 +17.9%/−28.9%/47次

**四、5.5 年回測（含 2022 空頭）—— 更正先前「dominated」的說法**
- QQQ（零選股偏差）：買進持有 +128.3%/MDD−35.1% ｜ 週線 +73.5%/−17.6%/9次 ｜ 日線 +70.8%/−15.5%/44次
  · **2022：買進持有 −32.6% vs 週線 −11.7%** → 趨勢規則在指數上確實有效，是「多頭少賺換空頭少賠」的取捨，非 dominated
- **同規則放到 13 檔高波動個股：MDD 反而更大**（日線 −76.6%、週線 −65.9% vs 買進持有 −59.1%）
  → **結論：SuperTrend 對指數有效、對高波動個股無效**
- 七鏈（現時持股回推，有倖存者偏差，只看行為特徵）：巴菲特 MDD **−20.0%**、2022 **+7.5%**（唯一全天候）；
  AI 電力/核能 2022 **+25.4%**（更正「七鏈都是同一個 AI 賭注」的說法）；Bitcoin→AI MDD **−94%**；
  太陽能 5.5 年輸 SPY 且回撤兩倍；產業鏈精選 MDD −55.9%（比單一鏈更差＝過度集中）

**五、實作調整**
1. 趨勢倉 → **週線** SuperTrend（`TREND_WEEKLY`，公式參數不動，只換 K 棒週期）；確認天數 2→1
2. ~~新增指數擇時倉~~ → **同日依用戶指示移除**（`market_is_green()` 函式保留備用，未接任何倉）
3. **Bitcoin→AI 限重 10%**（`BTC_MAX_WEIGHT`，跨鏈主倉皆套用，`_capped_weights` 超額按比例分配）
4. 太陽能**不動**——輸 SPY 是有倖存者偏差的回測結果，無結構性理由不砍（避免追回測過擬合）

**原則**：模擬倉是實驗器材，只改「有結構性證據」的（訊號雜訊、風險集中），不追回測報酬。

### 同日修正（依用戶指示）
- **產業鏈精選 → 產業鏈全**：`chain_select_union()` 改回七鏈完整守備清單聯集，**13 檔 → 48 檔**。
  依據正是本次回測發現「每鏈取前 2」過度集中（MDD −55.9%，比單一鏈 −36~−39% 還差）。
  單一股權重 **25% → 2.2%**。`_btc_caps` 同步改用整條鏈成員（Bitcoin 實際權重 6.2% < 10% 上限）。
- **移除指數擇時倉**：用戶不採用；`market_is_green()` 保留備用。
- 守備清單維持 **8+8**（`screen.py TOPN=8` 不動）。

---

## 2026-07-31 — 巴菲特清單：補回洪瑞泰缺的三條件

### 起因
用戶拿洪瑞泰七條原則來核對系統實作：GDP高點減碼／GDP低點進場／俗買貴賣／
行業地位穩固／ROE穩定／盈再率<80%／配息率>40%。

### 核對結果（改之前）

| 條件 | 原狀 |
|---|---|
| 俗買貴賣 | ✅ EPS×12 / ×30 |
| 盈再率 < 80% | ✅ 硬關卡 |
| ROE 穩定 | ⚠️ 有算 `roe_pass_years` 但訊號只看當年 |
| 行業地位穩固 | ⚠️ 龍頭#N 只當頁面標籤，不篩 |
| 配息率 > 40% | ❌ `payout_ratio` 有抓進 DB，**零行程式用它** |
| GDP 高/低點 | ❌ 看板 repo 完全沒有 |

**GDP 更正**：GDP 模組在 **TradingBot**（`gdp.py`：World Bank + Philly Fed SPF + 中經院），
但**純資訊面** —— `/gdp` 指令 + `gdp_line` 貼在報告抬頭 + 每季提醒，
**沒有接到任何買賣訊號或部位調整**。「GDP 給你看，但系統不照它動作」。

### 實作（commit f052a8d）

1. **配息率 ≥ 40% 納入品質關**（`PAYOUT_MIN = 0.40`）
   - `payout` 有數字 → 照門檻；無數字但有配息 → 放行（資料缺）；完全不配息 → 擋
2. **ROE 穩定改硬條件**：`roe_pass_years >= ROE_YEARS(3)`，近 4 年至少 3 年 ROE≥15%
3. **拿掉合理價**（用戶先前已指示「不需要合理價 都可以直接拿掉」，當時只改了頁面沒改邏輯）
   - WATCH 上界 `fair_price(EPS×20)` → `exp_price(EPS×30)`，`HOLD` 訊號移除
   - **連帶**：`buffett_sp500.py` Stage1 預篩 PE 門檻同步 20→30，
     否則 PE 20~30 的標的永遠進不了 Stage2，改動等於空轉
   - 頁面說明（🟡觀望＝俗價~貴價）與程式邏輯**這下才一致**
4. 頁面新增：**配息率欄**、ROE 欄後方 **n/4 達標年數**、品質關說明列

### ⚠️ 配息率門檻的已知副作用（用戶知情下選擇 A：忠於原方法）

拿改前的 203 檔美股實測 → **只剩 87 檔（43%）通過**。
被刷掉的含 GOOGL(4.3%)、NVDA(0.6%)、AAPL(12.6%)、HON(36%)、PDD(0%)。

**這是結構性誤殺不是公司變差**：美國公司用**庫藏股買回**還錢給股東，
yfinance `payoutRatio` **只算現金股利不算買回**（蘋果 2025 買回 >$900億，配息率仍 12.6%）。
洪瑞泰這條是**為台股設計**（台灣公司少做買回，現金股利是唯一證據）。

提了三案：A 照原規則 / B 美股改「股東回報率＝(股利+買回)/淨利」/ C 台股套美股不套。
**用戶選 A** —— 忠於洪瑞泰原方法，接受美股清單瘦身、偏向金融/公用/傳產。
（若日後想改，B 案的實作點在 `buffett_screener.py` 的 `payout_ok`）

### 沒做的

- **GDP 擇時**：用戶先前已明確「不用指數擇時」，GDP 擇時本質同一件事，
  且 GDP 是落後指標（季度、還會修正）。維持只顯示不動作。
- **行業地位當硬條件**：龍頭排名是「同 sector 市值前 3」的粗糙代理，
  當硬門檻會誤殺中型股（台股尤甚，候選才剛放寬到 200 檔）。留著當標籤。

### 同日：全掃描實測 + 挖出上櫃代號 bug

**第一輪（A 案三關全開）**：美 **203 → 16 檔**（我先前只套配息率估 87，低估了三關疊加）。
剩下的 16 檔：外國 ADR（NVO/PBR/ITUB/UGP/CIG）、航運（BWLP/HAFN/TRMD）、
資管+MLP（APAM/TROW/MPLX/BSM），**一般美企只剩 ACN/DOX/WU/CPB 四檔，科技股全滅**。
→ 這頁的實際定位已變成「台股清單 + 美股附帶」，用戶知情。

**🔴 挖出 bug：上櫃股(TPEX)代號後綴錯了**（commit d6f8eb4）
- log 裡 11 個 404 全是台股 → 查到 `data_tv.py` 對 853 檔台股**一律掛 `.TW`**
- 實際：TWSE 596 檔用 `.TW` ✅、**TPEX 257 檔要用 `.TWO`** ❌ → yfinance 全 404
- **靜默失敗**：404 被 except 吞掉印個 SKIP，外觀等同「基本面不合格」。從台股功能上線就存在。
- 修法：`df['exchange'].map({'TWSE':'.TW','TPEX':'.TWO'})`；
  連帶 `buffett_scan` / `buffett_html` 剝後綴改 `rsplit('.',1)`

**第二輪（修完）**：美 16 / 台 **45 → 76**（新進 31 檔全是上櫃），404 歸零。
新進榜含 **鈊象3293（ROE 99.2%／盈再 8.4%／配息 73%）**、元太8069、
巨漢6903（盈再 1.5%）、瑞穎8083、普萊德6263 —— 教科書級洪瑞泰標的，
**先前全部被靜默丟掉**。

**教訓**：外部 API 的 404/例外若只印 SKIP，會偽裝成正常的業務結果。
台股 TWSE/TPEX 後綴不同是 yfinance 常見坑。

---

## 2026-07-31 — 看板判讀層搬回本機（Max plan），Actions 不再呼叫 LLM

### 起因：先查帳單，不憑印象

用戶問「看板每日產業評論具體是什麼？可以改 Max plan 嗎？」
我先答「Flash 很便宜，搬家不划算」，用戶要求**查真實花費** —— 結果打臉兩次。

**查法（可重用）**：BigQuery billing export
`data-collector-489107.billing_export.gcp_billing_export_v1_01A5A5_21B1E4_017086`，
`bq` 已登入可直接查。按 `project.name` + `sku.description` + 日期分組。

**歸因鐵證**：`gemini 3 flash` 花費在 **7/20、7/26、7/27 歸零**，
那正好是週日/週一 —— cron 是 `2-6`（週二~週六）。不是統計相關，是完全對應。

| 日 | 星期 | 看板 |
|---|---|---|
| 7/23 | 四 | NT$23.40 |
| 7/26 | **日** | **NT$0.68** |
| 7/27 | **一** | **NT$0.00** |
| 7/28 | 二 | NT$25.15 |

→ **每運行日 NT$24 × 21 日 ≈ NT$500/月**

### 🔴 我犯的兩個錯

1. **幣值**：帳單 `currency` 欄位是 **TWD**，我當成 USD 又乘 32.3，
   把 NT$500/月 講成 **NT$16,000/月**，並基於錯的數字給了「搬家省 19 萬」的建議。
   舊記憶 `gemini_cost_spike_2026-07` 同一個錯誤（寫「$2,027.86 USD ≈ NT$66,000」）
   已沿用兩週，一併更正。
2. **7/18 的省錢預測錯 170 倍**：當時用牌價推算「Flash 便宜 33 倍 → NT$3/月」，
   實測只省 **3.7 倍**（NT$89/日 → NT$24/日）。實付單價 **NT$95.6/百萬 output token**，
   遠高於牌價 $0.30。原因是 `main.py` 的 agent pipeline 每檔股票跑好幾輪，
   10 天燒 170 萬 output token。**換模型省多少要事後查帳單，不能用牌價推算。**

用戶在知道實際只有 NT$500/月 後，仍決定搬。

### 實作

```
本機 08:30（接在 ClaudeMorningBriefing 之後，週二~週六）
  briefing.ps1 → 推完早報 → board_analyze_daily.cmd
      us_analyze.py  49 檔 → 依鏈分批 7 次 claude → reports/report_*.md
      tw_analyze.py  37 檔 → 依鏈分批 7 次 claude → tw_analysis.json
      git push board
Actions 09:00（cron 0 0 → 0 1）
  只讀報告 → board_html.py → 發佈 Pages（不再呼叫任何 LLM）
```

- **新增 `llm_board.py`**：判讀層轉接器。prompt **走 stdin** 不走命令列參數
  （避開長度上限與跳脫字元地雷）。`BOARD_LLM=claude|gemini` 可切，Gemini 留備援。
- **新增 `us_analyze.py`** 取代 `main.py`：yfinance 算均線/乖離率/趨勢強度/量比（免費），
  判讀依鏈分批。輸出必須符合 `board_html.parse_report()` 契約 —— 已實測驗證：
  摘要 49 筆評分抓到、56 個個股區塊、CHAIN_MAP 49/49 對得到。
- **改 `tw_analyze.py`**：`analyze()` 拆成 `collect()` + `analyze_chain()`，
  呼叫數 **86 次/日 → 14 次/日**。
- **改 `tw-board.yml`**：移除美股/台股兩個 LLM step（含 GEMINI_API_KEY / TAVILY_API_KEYS），
  改為檢查本機產出是否存在；報告逾 3 天未更新發 `::warning::`。
- **改 `briefing.ps1`**：附加呼叫，放在 Telegram 推送**之後**，兩者失敗互不影響。
  附加時用 `UTF8Encoding($false)` + `AppendAllText`，避免 PS5.1 的 `Add-Content`
  在檔案中間再插一個 BOM（檔首既有 BOM 已驗證未破壞）。

### 踩到的坑

- **跨鏈重複**：NVDA/AMD/MU/AMZN/ANET/TER 同時屬於多條鏈，
  49 個代號會產出 **56 個 `##` 區塊** → 看板同一檔渲染兩張卡。已按評分取高者去重。

### 取捨（用戶知情）

- 放棄 Tavily 新聞層（輿情情緒/風險警報/利好催化/最新動態），
  改為精簡的「結論 + 理由 + 觀察條件 + 風險 + 空手者/持有者建議」。
- **電腦沒開機就沒有當日判讀**；Actions 沿用前一日報告並發 warning。

## 2026-08-04 · 投資資訊首頁改版 + GDP 觀察頁 + 修今日看板 workflow 失敗

### 改版（用戶拍板三決定：儀表板當首頁／加 14:05 排程／新聞用鉅亨）

```
首頁 index.html（新：大盤行情 + 鉅亨頭條10條 + 分頁入口動態摘要）
├── board.html      產業鏈看板（原 index.html 搬家）
├── buffett.html    巴菲特清單
├── portfolios.html 策略賽馬
├── earnings.html   財報分析
└── gdp.html        GDP 觀察（新）
```

- **NAV 中心化**：六頁導覽統一定義在 `board_theme.NAV`，各頁不再自維護
  （board_html 原本硬編 4 連結、buffett/portfolio 各一份 NAV，全數收攏）。
- **新增 `gdp_fetch.py` → gdp_data.json**：
  美國實際＝FRED CSV（GDPC1 自算 SAAR）、預測＝Philly Fed SPF（移植 TradingBot/gdp.py）；
  台灣實際＝主計總處 nstatdb API（自動）、預測＝`gdp_manual.json` 手動維護，
  asof 超過 120 天自動推 TG 提醒（主計總處每季 2/5/8/11 月發新聞稿，只有 PDF）。
- **新增 `gdp_html.py` → docs/gdp.html**：洪瑞泰「GDP 高點賣股票、不買股票」提醒燈，
  高點判定＝近 8 季實際＋預測取最大：未來=尚未到頂(綠)/最新季=接近高點(黃)/過去=已過高點(紅)。
  維持既有定案：只顯示不接買賣訊號。首跑結果：美國已過高點(2025-Q3)、台灣接近高點(2026-Q1 14.55%)。
- **新增 `market_fetch.py` + `home_html.py`**：指數 7 檔（美 4+加權+VIX+美元/台幣）走 yfinance；
  頭條走鉅亨 JSON API（`api.cnyes.com/media/api/v1/newslist/category/{tw_stock,wd_stock}`）各 5 條。
- **新增 `market-home.yml`**：週一~五 14:05（UTC 06:05）台股收盤後重建首頁，純 yfinance 零 LLM。

### 修今日 09:00 workflow 失敗（用戶轉來 GH 失敗信）

1. **ImportError CHAIN_ICON/TW_NAME**：昨天 v2 扶正成 board_html.py 時漏了
   alert_telegram.py 還從它 import 這兩個名字 → 已在 board_html.py re-export。
2. **`ls -t` 挑報告失準**：checkout 後所有檔 mtime 相同，今天實挑到 6/24 舊報告。
   檔名帶日期 → 改 `ls | sort | tail -1`；逾期警告同步改由檔名取日期（原 mtime 算法在
   Actions 上永遠是 checkout 時間，等於從未生效過）。

### 踩到的坑

- FRED/主計總處都會擋 python 的 TLS（前者 reset 連線、後者憑證缺 SKI 被 3.13 拒收），
  curl 卻都通 → `_get_text_curl_fallback()`：requests 失敗改走 subprocess curl。
  curl 回傳**不可用 text=True**（Windows 會拿 cp950 解 UTF-8，reader thread 直接炸）。
- 主計總處 2026-05-29 新聞稿的 Q3/Q4 逐季預測網路查不到（在 PDF 表格內），
  gdp_manual.json 只填了查證過的 Q2 10.83% 與全年 9.64%，不瞎編；8 月中新版發布時提醒會叫人補。
- cp950 印 emoji 炸 print：board/portfolio/buffett/gdp/market/home 全部補
  `sys.stdout.reconfigure(encoding="utf-8")`。

### 同日迭代（用戶看圖回饋）

- GDP 頁季度數字改**橫向排列**（跟 X 軸同方向，一季一格、預測黃虛框，手機橫滑）。
- 首頁行情定版 **2 排 × 4**：美股 S&P/那斯達克/道瓊/費半｜台股加權/三大法人買賣超/美元台幣/美債10Y殖利率。
  - 法人＝證交所 `BFI82U` API（單位元→億；合計+外資+投信）；**約 15:00 才公布當日值**，
    下午排程 14:05 → 15:10 順延。VIX 由美債殖利率取代（用戶授權我建議第 8 格）。
  - ⚠️ yfinance 的 `^TNX` **直接報 %**（4.74=4.74%），不是傳統 10 倍報價，別除以 10。

### 接回新聞層（零成本版）+ 財報加 3 檔（2026-08-04 下午）

- 用戶質疑判讀理由「都是 MA5>MA10」——正確：7/31 砍 Tavily 後 LLM 只剩技術面+一個營收數字，
  reason 四個詞其實同一條收盤價序列講四遍。
- 接回新聞層，**零成本**（vs Tavily 方案約 NT$950/月）：
  - 美股：`yf.Ticker(code).news` 近 7 天取 3 條（us_analyze._news）
  - 台股：鉅亨 ESS 關鍵字搜尋 `ess.api.cnyes.com/ess/api/v1/news/keyword` 近 5 天 3 條，
    標題要清 `<mark>` 標籤（tw_analyze._news）
  - prompt 加規則：有新聞須納入判斷、與技術面矛盾要指出不硬湊；沒附新聞不准提新聞
  - 端到端驗證過：NVDA 判讀有引用估值重評新聞並點出「多空矛盾訊號」
- 財報追蹤 US_WATCH 加 MU（美光）/AVGO（博通）/PLTR（Palantir），共 7 檔；
  三張圖卡已產出、earnings 索引頁 7 份。

### 七鏈深度報告 8 月更（同日晚間，回應「8月有跑嗎?」）

- **8/1 排程其實沒跑成**：Task Scheduler 回報成功(0)，但 log 只有一行 `end (exit 3)`、
  連 start 都沒寫，chain_reports.json 從 7/17 上線至今沒動過。根因＝
  `refresh_reports_monthly.cmd` 含中文（同 8/1 board_analyze_daily.cmd 的坑，當時漏了這支）。
- 修法：.cmd 全英文、prompt 改 ASCII 斜線指令 `/refresh-chain-reports`；乾跑驗證 start/end 都有寫。
  **教訓：所有給 Task Scheduler 跑的 .cmd 一律全英文，一支都不能漏。**
- 8 月份已在對話內補跑：7 個研究 Agent 平行（各鏈 WebSearch 查證 2026/8 資料）→
  reports_data/*.json → render → push → 線上驗證 rptbtn=7。
  AI 伺服器 bespoke 版一併換成標準模板（資料新鮮度 > 客製表格）。

## 2026-08-05 · 第 8 條產業鏈上線：玻璃基板/TGV

- **緣起**：7 月底用戶貼玻璃基板文章，當時研究完停在「要我怎麼做?」沒人拍板，
  對話壓縮後斷線。用戶追問「之前有說要做第八條為什麼沒做了」→ 補做。
  教訓：**懸而未決的提案要落成待辦，不能停在問句。**
- **設計**：獨立第 8 鏈（不併 AI 伺服器——併入會在三因子篩選輸給巨頭，永遠隱形）。
  手選池：美 6（GLW/INTC/AMAT/ONTO/CAMT/KLIC）+ 台 12；逐鏈獨立 top8 只跟自己人比。
  `screen.py` 加 `US_MANUAL`（無 ETF 的鏈用手選池）。
- **賽馬排除**：`paper_portfolio.RACE_EXCLUDE`——比賽中途不換賽制，玻璃只上看板+報告。
- **深度報告**（reports_data/glass.json）：誠實原則——每檔標實際進度
  （已試產/送樣中/僅市場歸類），點名南電/景碩是純聯想題材；晶呈是全鏈唯一有真實
  TGV 營收者（篩選器也把它排第一，交叉印證）。時間表 2026 驗證/2027 試產/2028 量產。
- **看板**：CHAIN_ORDER 8 鏈、新 TGV 面板 icon、`if not us and not tw` 跳過條件加
  「有深度報告仍露出」fallback；TW_NAME 補 11 檔中文名。skill 更新為八鏈。

### 🔴 FinMind 限流連環坑（同日）

1. screen.py + tw_analyze 同日連跑 → 撞限流，後三鏈台股全跳過。
2. **根因：`.env` 裡一直有 FINMIND_TOKEN，但 tw_analyze 從沒 `_load_env()`**，
   匿名低額度跑了一個多月沒炸只是因為每天單趟剛好夠用。已修：載入 .env。
3. 限流時 tw_analysis.json 被覆寫成殘缺版（一度只剩 1 檔）→ 加保險絲：
   **產出 < 守備清單 50% 拒絕覆寫**（git 還原救回）。
4. 玻璃鏈首跑 LLM 呼叫暫時性失敗（拿預設觀望 50）→ 單鏈重試回寫成功。
   首日判讀：玻璃台股 8 檔全觀望（33~52 分），與「驗證期」定調一致。

## 2026-08-06 · 產業鏈定位上線（BEST MATCH 拆解功能 #1）

用戶帶來一份第三方「投顧錄音查核」報告（3037 欣興 BEST MATCH），要求拆解四個功能學習導入：
產業鏈定位／財報營收實況／綜合判斷／技術面四指標。逐項評估可行性後，
本次先做**產業鏈定位**（優先順序最高：資料已備妥、複用現有 8 條鏈清單）。

- **新增 `chain_positioning.py`**：讀 `chain_reports_src/reports_data/*.json` 的 `valuechain`
  （8 條鏈 107 檔股票，環節分組），算每檔毛利率（台股 FinMind FinancialStatements／
  美股 yfinance grossMargins）+ 近60日報酬，寫 `chain_positioning_cache.json`。
  快取一天建一次，財報卡引用時純讀快取、不重打 API
  （教訓：8/5 才因為財報卡連續產出撞過 FinMind 限流，這次直接把快取設計進架構裡）。
- **踩坑**：8 條鏈的 `valuechain.stocks` 欄位格式不統一——有的純代號（`"2330"`）、
  有的代號+中文名（`"3105 穩懋"`）、有的代號+英文全名附註（`"TE（T1 Energy）"`）。
  第一版 parser 只用分隔符號切字串沒過濾格式，把「穩懋」「上銀」這些中文公司名
  當成代號去查 FinMind/yfinance，全部 404（32 檔查不到）。
  修法：先砍掉全形/半形括號內容，切完詞後用正則白名單（`^\d{4,5}$` 或 `^[A-Z][A-Z.]{0,5}$`）
  過濾，只留真代號。修完 107 檔全數命中、0 缺漏。
- **整合**：`earnings_infographic.py` 財報卡底部新增「產業鏈定位」區塊，依環節分組列出
  同鏈股票的毛利率+60日報酬，目標股票用青框標記；不在任一鏈上的持股（傳產/金融股）
  優雅跳過不顯示，不硬湊。
- 3037（欣興）實測：6 個環節、鈦昇/友威科/東捷/晶呈科技等玻璃鏈股票全部對得上，
  已上線驗證。

其餘三項（財報營收實況/綜合判斷/技術面四指標）待後續依序動工。

## 2026-08-06（下半）· 財報卡三項新功能：財報營收實況／技術面四指標／綜合判斷

BEST MATCH 拆解的剩餘三項一次做完（用戶：「都做一做吧」）。

- **`fundamentals_reality.py`**：近6季趨勢表（台FinMind FinancialStatements／美yfinance）
  ＋月營收YoY（台股專屬，FinMind無現成YoY欄位、自己拿去年同月算）＋業外>本業自動警示。
  驗證：3037 抓到的 Q2 數字跟 BEST MATCH 報告逐項對上（營收428.9億/毛利24.8%/
  業外89.2億/本業66.5億/EPS8.45），連月營收YoY六個月數字都精確吻合。
- **`technical_indicators.py`**：SuperTrend 復用 board_html_legacy 既有實作；雙重颱風K線＝
  SuperTrend 的 SMA-ATR 變體；EXCEED CHARGE＝TTM Squeeze擠壓動能（布林帶樣本標準差 vs
  凱特納SMA-of-TR，動能用線性迴歸投影）；RS相對強弱＝Mansfield RS（股價/大盤比值對其均線乖離%，
  短線25日+長線200日）。四個指標全部只需 OHLCV，yfinance 一次抓齊，不需新資料源。
  驗證：3037 SuperTrend翻多「第2根」，比對股價確實從7/31的787反彈到8/6的973（+23.6%），
  指標即時反應，不是套錯資料。
- **`narrative()` 擴充**：新增 `verdict` 欄位整合前兩項+產業鏈定位，LLM 輸出實測會同時引用
  洪瑞泰三關卡（盈再率203%地雷）、業外品質警示、技術面動能、同鏈定位，不是各講各的。

### 架構決策：compute+render 兩層拆分

FR/TI 的 `build(ticker)` 一次抓資料回傳 `(html, summary_text)`，`earnings_infographic.py`
的 `main()` 只呼叫一次，html 進版面、summary_text 進 LLM prompt——避免財報卡渲染跟
narrative() 敘事層各自重打一次 API（8/5 FinMind 限流的教訓內化進架構）。
CP 走快取讀取，天生零額外成本，加 `summary_text()` 供 narrative() 引用。

### 踩坑：發現既有 bug（非本次新增，但擋住整合測試）

`earnings_infographic.py` 的 `facts` 字串對 6 個 yoy 欄位直接 `:+.1f` 格式化，
沒防 None。3037.TW 的 EPS yoy 剛好是 None（yfinance 台股 EPS 欄位缺值，非固定規律，
2317.TW 有值但3037.TW沒有）→ LLM 敘事層每次都拋例外退化成純數據版，
且錯誤訊息「unsupported format string passed to NoneType.__format__」不會講是哪個欄位。
修法：加 `yoy()` helper 統一 None-safe 格式化，6 處全部替換。

### 已知限制：資料源季度落差

核心卡片（洪瑞泰/分析師共識）走 yfinance，台股季報更新常落後 FinMind 一季以上；
新的「財報與營收實況」區塊走 FinMind，比較新。已在頁尾加說明，兩者季度標籤
不一致時以「財報與營收實況」為準，不是抓錯。

至此 BEST MATCH 拆解的四項功能全數上線：①產業鏈定位 ②財報營收實況 ③綜合判斷 ④技術面四指標。

## 2026-08-06（再修）· verdict 脫離洪瑞泰單一濾鏡 + 台股中文名 + 業外趨勢偵測

用戶拿 3037 卡片實測回饋兩點：①台股顯示英文名（標題"Unimicron..."、基準指數"^TWII"）
②verdict 把「業外損益連續4季加速成長」壓縮成洪瑞泰三關卡底下的單點附註，沒獨立點出這是趨勢。

- 台股中文名：財報卡標題查 TW_NAME、技術面基準指數改中文映射（台股加權指數/S&P 500）
- **架構決策**：thesis/sentiment/entry 維持洪瑞泰框架不變；verdict 改成獨立查核總結，
  財務趨勢/技術面/同鏈定位平行呈現，不套進三關卡當唯一濾鏡（用戶：「不要用洪氏作為比較基準」，
  範圍限定在 verdict，不動搖 thesis 既有的巴菲特選股法判斷）
- fundamentals_reality.py 加業外損益逐季表格欄+序列文字；趨勢偵測用**尾端4季窗口**而非
  強制整段6季嚴格單調（3037 案例 2025Q2 有一次回檔打斷全段單調性，但近4季仍是清楚加速）
- 驗證：verdict 輸出三個平行獨立觀察（本業改善/業外加速結構性轉變/技術面），
  不再全部繞著洪瑞泰三關卡打轉；bottom_line 仍是純洪瑞泰視角，兩套視角分工清楚

## 2026-08-17～18 · ARK 追蹤頁上線 + 第9條鏈 + 三個靜默失敗 bug + 模擬倉全部重建

### 新功能

- **第 9 條產業鏈「關鍵金屬/原物料」**（FCX/SCCO/PAAS/CCJ，銅/銀/鈾）。
  用戶原本問「記憶體要不要也開一條」，查證後發現 **MU 已經在 AI 伺服器鏈的 top 8 裡**
  （透過 SMH/SOXX 這些半導體 ETF 自然篩進來），另開會變重複曝險 → 新鏈只留純原物料。
  台股沒有夠格的礦業標的，這條鏈只有美股池。
- **ARK ETF 追蹤頁**（`ark_report.py` → `docs/ark.html`），詳見 memory `ark_etf_tracker`。
  ARKK/ARKW/ARKG 三檔，五節結構、3/5/10 年回測 vs SPY/QQQ（含 Sharpe/Sortino）、
  點選基金可切換全頁視角。本機排程 `ArkReportBiweekly` 每 14 天跑。
- **GDP 頁改每月更新**（原每日）：季頻資料每天抓 99% 的日子拿到同一份數字。
  抓 1-3 號三天窗口而非只認 1 號——tw-board 只在週二~週六跑，1 號落在週日/週一會整月跳過。
- **玻璃基板/TGV 進賽馬**（原列題材驗證期觀察鏈）。關鍵金屬維持排除。

### 🔴 三個靜默失敗 bug（全部綠燈、全部壞超過一週）

通用教訓已寫進 memory `silent_failure_pattern`。

1. **缺套件被 `|| echo` 吃掉**：`requirements.txt` 少 `markdown`/`opencc-python-reimplemented`，
   `portfolio-nav.yml` 每天 ModuleNotFoundError 但 job 回綠燈 → 模擬倉淨值凍結 7 天。
   `weekly-screen.yml` 同一個坑（該 job 自己 pip install 一串，也沒帶這兩個）→ 巴菲特倉每週調倉從沒成功。
2. **`buffett_top30()` 讀已移除的 `fair` 欄位**：洪瑞泰方法論只剩俗價/貴價兩條線，
   `buffett_scan.py` 早改成輸出 `cheap`/`expensive`，這支沒跟著改。`fair` 永遠 None
   → `if not (p and cheap and fair): continue` 把 82 檔候選全部跳過，**持股永遠 0**。
   修法：改成方法論真正的規則（現價 ≤ 俗價才買），拿掉「合理價」那層放寬——
   俗價~貴價之間在這套方法裡是「觀望」不是「買進」，補了會跟賽馬頁自己寫的說明矛盾。修完 21-23 檔。
3. **`rebalance` 抓價範圍 ≠ 目標範圍**：`allt = _all_tickers(state)`（目前持股）+ 巴菲特清單，
   但目標清單來自 `screen_result.json`。每週重篩新選進來、目前沒持有的股票沒被抓價
   → `_alloc_shares` 靜默丟掉。實測產業鏈全目標 87 檔只買到 71 檔，
   AVAV/RKLB/TXN/NET/UEC 等 16 檔從來買不進去 → **每週重篩形同半殘：只能賣落選的，加不進新入選的**。
   修法：`allt` 併入所有鏈的守備清單。修完產業鏈全 71 → 101 檔。

### 模擬倉全部重建（用戶決定）

bug 3 代表那 48 天跑的並不是「產業鏈全」這個策略本身 → 留著錯的基準會誤導決策。
4 主策略 + 8 條產業鏈明細**全部以 $10,000 於 2026-08-18 重新起跑**，起跑線一致可互相比較。
舊資料在 git commit `4cce070`。

配套：`rebalance` 補新鏈建倉邏輯（原本只走既有 portfolios，新鏈永遠長不出來），
新倉記自己的 `inception`，賽馬頁標「⚠ 才加入」避免跟從頭跑的鏈比錯報酬率。

### 其他修正

- 全頁面稽核：board/portfolio 標題寫死「7 條產業鏈」（實際 9 條）改動態；
  board/buffett/earnings/gdp 導覽列缺 ARK 入口（產生時間早於 ARK 上線，NAV 是共用的但要各自重跑）。
- **各頁 header 加註更新頻率**，資料新鮮度一眼可辨。
- `earnings_index.py` 從未進任何自動化 → 財報季 `earnings_watch.py` 自動產的新卡不會出現在
  索引頁。已加進 `board_analyze_daily.cmd`。
- 佳必琪代號修正 **6134 → 6197**（6134 其實是萬旭電子、上櫃股，跟佳必琪無關；
  FinMind TaiwanStockInfo 查證）。也是為什麼 6134.TW 一直抓不到財報。
- 台股財報卡補齊 OCF/CapEx/FCF：原本誤判 FinMind 沒有現金流量表所以清成 N/A，
  其實有 `TaiwanStockCashFlowsStatement`，FCF = OCF + CapEx。
- 財報卡與看板的 RS 窗口統一成 30 日/1 年（原 25/200），並多抓一年資料暖機
  （長線 RS 要 250 個交易日均線，只抓 1 年的話圖表前段全空）。
- 看板產業鏈明細新增 EXCEED CHARGE + RS 兩張圖（跟財報卡同規格）。
  踩坑：只帶了 `EC.CSS`，漏掉 `CP/FR/TI` 三個模組的 CSS，個股卡片完全沒版面。

## 2026-08-24 · 盈再率公式修正（漏長投）+ Mike 發文交叉比對

### A. 盈再率算錯了，而且錯到會選錯股票

`buffett_screener.py` 原本用 **|CapEx 累積| ÷ 淨利累積** 當盈再率，
但洪瑞泰的正式公式是 **（期末固資+長投 − 期初固資+長投）÷ 近 4 年稅後淨利**
——**漏掉長期投資**。盈再率要抓的地雷之一正是「靠長投亂擴張」的公司，
漏掉這塊等於少一半防護。

實測差距大到會改變 80% 門檻的判定，不是小數點誤差：

| 標的 | 現行(CapEx) | 正式(含長投) | 影響 |
|---|---|---|---|
| MSFT | 63.9% | **83.4%** | 正式會被 80% 上限擋掉，現行放行 |
| 2330 | 91.3% | **80.3%** | 反向：現行擋掉，正式在邊緣 |
| AAPL | 10.9% | 31.7% | 差 +20.8pp |
| NVDA | 5.4% | 15.6% | 差 +10.3pp |

修法：`bs.balance_sheet` 取 `Net PPE` +（`Long Term Equity Investment`
或 `Investments And Advances`），需 5 期（期末 vs 4 年前）。
**期數不足不硬算**，退回 CapEx 版並標記 `reinvest_method="capex_fallback"`，
一路存進 `buffett_watch.json`。寧可標「資料不足」也不要給看似精確的錯數字
（2317.TW 只有 4 期，正確被標成 fallback）。

### B. Mike 發文 × 我們的盈再表：即時交叉比對

新增 `TradingBot/mike_crosscheck.py`，掛進既有的 `stocks_forum.py` 每日摘要。
討論區抓到買賣訊號 → 用我們自己的洪瑞泰篩選跑一次 → 並排推 Telegram。

**價值在持續校準**：他每次進出都在幫我們檢查系統。一致與分歧都推——
分歧不代表誰錯，可能是我們算錯（要修），也可能是他看到表面數字外的東西（要學）。

實測（Ted持股分享串真實貼文）：
```
❔ NAVI  Mike 賣 | 我們：資料不足 → 無法比對
⚠️ AN    Mike 買 | 我們：不合格（盈再率 118%）→ 分歧
✅ LVS   Mike 賣 | 我們：不合格（盈再率 238%）→ 一致
```

**踩到的坑**：
1. 一開始拿「mikeon88持股大公開」串測，25 則貼文零訊號——查了才發現
   **那串最新貼文停在 2023-04**，都是感謝留言。Mike 現在天天發文但
   **交易訊號集中在「持股分享」類的串**（如 #101 Ted持股分享）。
   舊版 `_extract_signals` 對同一批也抓不到 → 規則沒問題，是選錯測試對象。
2. 首版有自相矛盾的輸出：NAVI 判定寫「資料不足」但註解寫「我們的表：觀望區」。
   `_agreement()` 沒有單獨處理資料不足，落到了觀望那一支。已修成獨立分支——
   **查不到就講查不到，不要用看似有結論的字眼蓋過去**。

### 合法性邊界（查證過）

stocks.ddns.net **全站公開不需登入**（Overview.aspx / Screener.aspx / Tutorial.aspx
實測皆 200，無轉址登入頁）。教學頁明寫盈再表「按照洪老師所教的方法算出來」，
**方法本身公開，不需逆向工程程式碼**。
原則：只做數字比對，不重製轉貼他的表格內容（著作權歸洪瑞泰）；會員區不碰。

## 2026-08-24（續）· 盈再表/俗貴價與官方對照：查證結果與待辦

### 官方公式確認（公開資料，非逆向工程）

`盈再率 = (長投₄+固資₄ − 長投₀−固資₀) ÷ 四年淨利加總`
（Smart 自學網、撿股讚均有記載）→ **跟今天實作的一致，公式方向沒錯**。

### 但數字對不上，且找到三個結構性原因

1. **分母欄位錯**：FinMind `IncomeAfterTaxes`（歸屬母公司）≠ 官方「常利」。
   寶成 9904 2025：官方常利 9.74B、`IncomeAfterTaxes` 4.82B、
   `TotalConsolidatedProfitForThePeriod` 10.79B ← 這個才對得上數量級。
   改用合併口徑後 9904 從 −456% 改善到 −77%（官方 −4%），仍有落差。
2. **「常利」是他自訂的調整後指標**（排除一次性項目），無公開定義，
   任何標準財報欄位都不會剛好等於 → **完全一致在原理上做不到**。
3. **他是多來源整合**：網站自述「資料來源包含但不限於各證券商網站、MoneyDJ、
   Yahoo Finance、Morningstar、TradingView、Stockanalysis」。
   我們用 FinMind 單一來源，本來就不可能一致。

### 🔴 俗貴價的落差比盈再率更嚴重（且影響更大）

盈再率只是門檻，俗貴價是**買賣點**。實測 9 檔官方對照：
只有 3 檔誤差 <10%，2905 差 −96%、2915 差 +136%。

**根因：他用預期 EPS，我們用實績 EPS**（他的表有「預期」列與「預期報酬%」欄）。
2905 過去連年虧損但預期大幅獲利 → 兩個數字在回答不同問題。
另外他的貴價÷淑價不是固定 2.5 倍（9904 是 2.81、5519 是 2.54），代表貴價另有算法。

### 預期 EPS 涵蓋率實測 → 美台要分開處理

| | 美股 | 台股 |
|---|---|---|
| 預期 EPS 涵蓋率 | **11/11 = 100%** | **8/14 = 57%**（缺的全是上櫃中小型股） |
| 實績 vs 預期差距 | 多數 ±20% 內 | 有預期的 ±13% 內 |

→ 台股若改用預期 EPS 會讓一半標的算不出俗貴價，不可行。

### ⚠️ 我今天引入的 bug（要修）

`buffett_screener.py` 的 `_v()` 把「查無資料」當成 `0.0`。
yfinance 台股第 5 期常常是空的 → 期初＝0 → 「四年來把全部固資長投都當新增投資」
→ 9904 算出 272%。**正好犯了自己記憶庫「寧可標資料不足，也不要給看似精確的錯數字」。**

### 📌 待辦：美股驗證要往回撈更多資料

美股**沒有官方對照組**（US.aspx 是 JS 空殼、Screener.aspx 帶參數也只回 TWSE，
會員區不碰）。唯一可得的對照是討論區的實際交易紀錄，但目前：

- 只從首頁 15 串撈到 **8 筆**美股交易，樣本太小
- **解析會抓錯價格**：貼文常寫「2018/10/12 買入@21.8，2025/11/14 賣出@194」，
  現在抓到第一個價格（七年前的買價）去比今天的俗價 → THC/MCK/RHHBY 被誤判成
  「賣在俗價之下」。**這是解析缺陷，不是他判斷錯**。

**要做的**：
1. 修解析：一行多個價格時要能分辨買入價/賣出價/當前價，取對應動作的那個
2. 往回撈更多歷史貼文（SQLite 存檔 + 討論區分頁），把樣本從 8 筆拉到夠用

## 2026-08-24（三）· BoardAnalyzeDaily 排程漏跑週一（自 7/31 起）

Leo 在 Sonia 看板卡發現「個股訊號好像沒更新」——SIGNALS 卡日期停在 8/22。查 Windows
工作排程器「BoardAnalyzeDaily」的觸發設定，`DaysOfWeek` bitmask 是 **124**（=週二~週六），
**從建立時的 StartBoundary 7/31 起，每個週一都被排程規則直接跳過**——不是腳本跑失敗、
是排程本身從一開始就設錯了一天（少了週一、多了不需要的週六）。已改成 62（週一~週五）。

**補跑今天的資料時踩到一個環境坑**：先用 Claude Code 的 Bash 工具直接
`cmd.exe /c board_analyze_daily.cmd` 手動觸發，回傳「成功」但 `board_analyze.log`
完全沒新增任何一行——查出這個沙盒 Bash 環境下 `cmd.exe` 解析不到 `python`
（`where python` 在這個 context 下找不到路徑，換 PowerShell 工具跑就找得到），
腳本第一行呼叫 python 就整個靜默失敗。改用 PowerShell 的 `Start-ScheduledTask`
直接觸發真正的排程任務（用 Task Scheduler 自己配置好的執行環境跑）才成功。
**教訓**：本機排程腳本要手動補跑驗證時，優先觸發真正的 Scheduled Task 而非在
沙盒工具裡直接呼叫 cmd/python，執行環境（PATH 等）不保證跟 Task Scheduler 一致，
「exit 0 但什麼都沒做」是這類環境落差最容易偽裝成的假象。
3. 有了樣本才能回答「美股數字差異大不大」——**目前無法回答，不要假設美股沒問題**

## 2026-08-24（續二）· 交易計畫層上線（trade_plan.py）

緣由：Leo 提供桌面資料「20個交易者可以直接使用的AI指令」。20 個指令本身多半用不上
（那是寫給「拿 TradingView 截圖問 AI」的散戶，我們是自動抓資料算指標），
但它點出三件比指令本身值錢的事：

1. **三條基本限制**（要抄進所有 AI prompt）：
   「請把事實、推論與假設分開」「資料不足請直接列出缺少的，不要自行補值」
   「不要替我執行交易，最終決策由我負責」
   ⚠️ 第二句正好打中我同一天犯的錯（`_v()` 把查無資料當 0）——
   **這不是巧合，是 LLM 跟人都會犯的同一種毛病。**
2. **使用前要先給五件事**：標的/週期/持有時間/可接受風險/任務。
   我們昨天設計的投資長**後兩項是空的** → 建議懸空，這就是「不實用」的根因。
3. **五類指令對照我們的現況**：一二類已有，**第三類（建立交易計畫）跟
   第五類（復盤與紀律）完全沒有** → 這次先補第三類。

### Leo 拍板的參數（2026-08-24）

| 參數 | 值 |
|---|---|
| 單一策略內·單一持股上限 | **50%** |
| 單筆虧損保險絲 | **−30%，只警示不自動賣出** |
| 真正出場依據 | 動能=賣出訊號；洪瑞泰=到貴價 |
| 風控計算基礎 | **只算 Firstrade** |

**🔴 最重要的設計修正**：台股(繼承帳戶)11 檔 （金額已隱去）
是**家人留下的資產**，Leo 明確指示「不應拿來做策略跟考量」。
我原本的設計把它跟自操部位混在一起算集中度，是錯的——
混進去會讓所有風控判斷失真（例如 2412 佔 25% 會誤觸集中度警示，
但那根本不是他的操作決定）。
現在 `ACTIVE_ACCOUNTS = {"Firstrade"}`，繼承部位**僅顯示不計算**。

另外 Leo 決定「持股數少時自動降上限」先不做，**先跑一陣子看實際情況**
→ 程式碼刻意保持單純，不自作聰明加規則。

### 實跑結果（保險絲立刻有發現）

```
🛡 風控摘要
· 策略部位（Firstrade）：66 檔，$2,108,905 TWD
⚠️ 保險絲觸發 3 檔（虧損 ≥30%，賣出訊號尚未出現）
· API  -95.4%　成本 $3,027  → 現值 $139
· XYZ  -50.6%　成本 $5,294  → 現值 $2,615
· AAOI -36.8%　成本 $31,419 → 現值 $19,862
另有繼承部位 11 檔 $8,856,128（僅供知悉，不參與策略計算）
```

Firstrade 66 檔**成本資料 100% 齊全**，保險絲可用。

### 還沒做（下一步）

- 第五類：**復盤官**（檢查實際交易紀律：是否提早進場/移動停損/虧損後加碼/
  計畫外交易佔比）。要累積一段實際交易才有東西可復盤。
- 把三條基本限制寫進研究員/投資長的共用 prompt
- 交易計畫層接進每日推播

## 2026-08-24（續三）· 盈再率真因水落石出 + 俗貴價美台分流

### 🔴 真因：yfinance 根本沒有「4 年前」的資產負債表

前面兩次修正都沒對症。追到最後發現：**yfinance 的第 5 期一律是 NaN**
（AAPL、9904 實測皆然），而盈再率的定義就是「今年 vs 四年前」。

而我當天寫的取值函式 `_v()` 把缺值回 `0.0` → 期初基數＝0
→ 等於宣稱「這四年把全部固資長投從零蓋起來」→ 9904 算出 272%（官方 −4%）。

**所以「正式公式」從頭到尾沒真正算對過**，先前顯示 `official` 的數字全是假的。
這是同一天內第二次犯「把不知道當成零」——早上才在記憶庫寫下
「寧可標資料不足，也不要給看似精確的錯數字」，下午自己踩。
那份 PDF 也有同一條：「資料不足請直接列出缺少的，不要自行補值」。

### 修法：台股改走 FinMind

FinMind 台股有完整 6 期年報，欄位直接對應公式：
`PropertyPlantAndEquipment`（固資）、`InvestmentAccountedForUsingEquityMethod`（長投）。
分母用 `TotalConsolidatedProfitForThePeriod`（合併稅後淨利）——
資產負債表是合併的，分母也要合併才一致（`IncomeAfterTaxes` 是歸屬母公司，只有一半）。

新增 `_reinvest_tw(code)`，方法標記 `official_tw`。
**美股 FinMind 沒有財報**（只有 USStockInfo 清單 19,342 檔）→ 仍退回 CapEx 版並標記。

### 盈再率改分級制（用戶指示：要寫清楚哪裡異常）

| 等級 | 條件 | 處理 |
|---|---|---|
| `ideal` | 0 ~ 40% | 過關 |
| `acceptable` | 40 ~ 80% | 過關 |
| `warn` | ≥ 80% | 淘汰 |
| **`shrinking`** | **< 0** | **待複核**——四年來固資+長投淨減少，是縮表不是效率好 |
| **`unknown`** | **\|值\| > 300%** 或算不出來 | **待複核**，附具體異常原因 |

實測 2105 正新 −431%、1216 統一 +332%：數學上沒錯（2105 固資四年少 261 億、
四年淨利才 60 億），但反映的是「公司結構大變動」不是「資本效率」，
拿去比 80% 門檻會篩出錯的東西。官方用「常利」平滑掉，我們沒那個欄位 → 明講無法判斷。

**🔴 順帶補掉兩個原本會誤放行的漏洞**：訊號判定原本只寫 `rr < 0.80`，
所以 −431%（unknown）跟 −77%（shrinking）都會被當成「合格」。
現在只有 ideal/acceptable 過關，另兩者標 `needs_review` 不自動放行也不自動淘汰。

### 俗貴價：美股改預期 EPS、台股維持實績

洪瑞泰官方用預期獲利，但兩市場資料可得性差很多——
**預期 EPS 涵蓋率：美股 11/11=100%、台股 8/14=57%**（缺的全是上櫃中小型股）。
台股改用會讓一半標的算不出俗貴價而消失 → 分開處理，`eps_basis` 欄位標明用了哪個。

**影響很直接**：11 檔美股實測 **5 檔判定改變**——
CIG/PGR/NVO/CPB 從買進降為觀望、BBVA 從觀望升為買進。
方向是「買進變少」，因為這批分析師多預估獲利下滑。

### 順帶查證：合理價 ×20 之爭（虛驚）

發現 TradingBot `db.py` 有 `_PE_FAIR = 20` 且註明「依講稿原文修回 12/20/30」，
一度以為我今天拿掉「合理價放寬層」是錯的。查記憶庫 `hongruitai_method.md` 確認：
**2026-07-18 已對第一手（洪瑞泰官方 blog）查證定案——官方只有俗價/貴價兩條線，
×20 是我們自己補的中點**。所以今天的修改與既有定案一致。
（這也剛好說明為什麼要有本機資料庫：同一個結論散在記憶庫、兩個專案的程式碼註解裡。）

---

## 2026-08-25　美股盈再率接 SEC EDGAR；抓到兩個新的靜默失敗

### 起因
美股一直算不出官方盈再率——yfinance 的資產負債表**第 5 期（4 年前）永遠是 NaN**，
而盈再率的定義就是「今年 vs 四年前」，所以美股從頭到尾都只能用 CapEx÷淨利 的替代算法。

### 新增 `sec_edgar.py`
SEC EDGAR 是美國證管會官方申報資料庫：免費、無金鑰、歷史完整。
Yahoo/MoneyDJ 的美股財報本來就是從這來的，直接讀源頭。

實作過程踩到的四件事，每一件都會讓整批股票**安靜地**算不出來：

1. **長投科目每家公司名字不同**。AAPL 用 `MarketableSecuritiesNoncurrent`、
   MSFT 用 `LongTermInvestments`、KO 用 `EquityMethodInvestments`、JNJ 一個都沒有。
   → 候選清單依序找；全找不到就當 0，但**標成 `_nolti`**，
   讓「這家真的沒有長投」跟「有但我們沒抓到」永遠分得開。
2. **外國發行人報 IFRS 不是 us-gaap**。HAFN（丹麥）、BWLP（百慕達）
   **一個 us-gaap 科目都沒有**。只看 us-gaap 會把整批外國股當成「EDGAR 查無此公司」。
   → 加 `ifrs-full` 分類與對應標籤，標成 `official_us_ifrs`。
3. **IFRS 用當地幣**。CIG/ITUB 報 BRL、NVO 報 DKK。
   分子分母幣別不同就是拿巴西幣的固資去除美金的淨利——**算得出數字但完全沒意義，而且不會報錯**。
   → 幣別鎖定：淨利必須與固資同幣，長投幣別對不上就當沒有。
4. **固資與淨利的最新年度不同步**。BSM 固資到 2025 但淨利只到 2024；
   NVO 固資從 2020 才有但算式需要 2019。原本硬用最新年度當期末，
   一格對不上就整檔作廢 → 白白漏掉 3 檔。
   → 改成從新到舊找第一個「期末固資＋期初固資＋四年淨利」全齊的年度（最多往回 3 年）。

結果：17 檔美股涵蓋 **14 檔**。未涵蓋的 HAFN/BWLP/NVO 是 EDGAR 上固資不滿五年，
屬**真的資料不足**，退回 fallback 並標記——這是正確行為，不是 bug。

### 🔴 同一個結論我訂正了兩次，第二次也錯

先寫「CapEx 版漏長投所以**低估**」。接完 EDGAR 拿 12 檔美股實測，
9 檔差距 >15pp 且全部往下修，於是改成「系統性**高估**」——
然後台股重掃出來，方向整個反過來：

| | CapEx 版 | 官方式 | 方向 |
|---|---|---|---|
| CPB（美） | +56% | +14% | 高估 |
| WU（美） | +21% | −1% | 高估 |
| GLPI（美） | +14% | −5% | 高估 |
| **9917（台）** | +59% | **+174%** | **低估 115pp** |
| 6581（台） | +62% | +101% | 低估 |
| 1342（台） | +38% | +86% | 低估 |

**兩次都是拿單邊樣本把方向講死。** 正確的說法是：
CapEx 用**毛支出**（拉高）、又**漏掉長期投資**（壓低），兩個反向誤差同時存在，
淨方向取決於公司資產結構 →
**它不是官方公式的近似值，是另一個東西**，只能標「資料不足、僅供參考」，
不能拿來比大小、更不能當門檻判斷。

教訓：第一次訂正時我手上只有美股樣本，就寫下了「系統性」三個字。
「系統性」是個很強的宣稱，單一市場的樣本撐不起來。

### 🔴 grade / note 算了卻沒寫出去

`_reinvest_grade()` 8/24 就做好了（分級 + 異常原因），但 `buffett_scan.py`
組 `buffett_watch.json` 時**沒把這兩個欄位放進去** → JSON 裡永遠是 `None`。
「說清楚哪裡異常」這需求等於只做了一半，前端根本拿不到。已補。

### 🔴 靜默失敗：FinMind 例外結果被寫進快取
台股重掃帶了 token 之後，`official_tw` 只從 23 → 26，遠低於預期。
而我單獨測 9910/2404/2421 **三檔全部成功**——同一份程式碼、同一個 token。

原因在 `_fm()`：
```python
except Exception:
    data = []
_FM_CACHE[key] = data      # ← 連例外的空結果也快取
```
長掃描途中任何一次逾時／斷線，都會被**當成「這家公司沒有財報」永久記住**，
然後安靜退回 capex_fallback。表面上：exit 0、檔案照產、看起來完全正常。

修法：重試一次（間隔 2 秒、逾時拉到 45s），仍失敗就回空但**不寫快取**，
並記進 `_FM_ERRORS`，掃描結束由 `fm_error_report()` 印出來。

### 順手補的可觀測性
`buffett_scan.py` 結束時多印一行**盈再率來源分布**：
```
   盈再率來源：official_tw 26、capex_fallback 41、official_us 6...
```
理由：8/24、8/25 連兩天踩到同一型——一批股票安靜退回替代算法，
掃描 exit 0、檔案照產，不印出來根本發現不了。

### ⚠️ 必須講清楚的界線
接 EDGAR 讓我們**算得更正確**，但**不等於「跟洪瑞泰官方數字一致」**：
- 他的分母是自訂的「常利」，**無公開定義**，無法重現；
- 他的美股頁是 JS 殼、Screener 只回台股，**根本沒有美股參考值可比對**。

「更正確」和「對得上 Mike」是兩件事，不要混。

---

## 2026-08-25（下午）　對齊洪瑞泰講稿的篩選器設定

### 起因
用戶問「掃描的是 S&P500 還是 Nasdaq？」，接著問「ROE≥15% + PE≤30 這條是怎麼寫的？
我怎麼不記得有」，最後要求**回去讀講稿**。

讀了才發現：他在講稿 1190-1206 行**親口講過篩選器怎麼設**，跟我們的實作有三處不一樣。

### 講稿原文 vs 我們的實作

| | 洪瑞泰 | 我們（改之前） |
|---|---|---|
| 美股母體 | **S&P 500** | 全市場 NASDAQ+NYSE+AMEX 1,675 檔 |
| 本益比（初篩） | **< 15**（兩國都是） | ≤ 30 |
| ROE（初篩） | 美股 **10%**、台股 15% | 兩邊都 15% |
| 取樣 | 全部（他是逐檔按盈再表） | **按 PE 由低到高取前 200** |

最後一項最嚴重：**那是我們自己加的，沒有任何出處**。
美股過篩 584 檔只算了最便宜的 200 檔，**385 檔連算都沒算**。
這正是記憶庫「跑偏史」那條通則——永遠不用便宜當主排序。

### 🔴 7/31 那次改動是「有理有據地改錯」

PE 從 20 改成 30，理由寫「對齊貴價線」。推理本身沒錯——貴價確實是 EPS×30。
**但他的篩選器用的不是賣出線，是 PE<15。**

初篩線與賣出線是兩個不同層次：
- PE≤15 → 找「快到買點」的候選
- PE=30 → 貴價／賣出線

用 30 當初篩線，等於把「快到賣點」的股票也收進觀察池，觀察它沒有意義。

### 改完的結果：美股清單整個換掉

| | 舊（全市場 PE≤30） | 新（S&P500 PE≤15） |
|---|---|---|
| 美股 | 15 檔 | **7 檔** |
| 台股 | 43 檔 | **34 檔** |

舊美股：`WU HAFN PHI TRMD ITUB OMF BWLP EC APAM FRO TROW PGR NVO CPB CNQ`
→ ADR（ITUB 巴西、NVO 丹麥）＋航運（HAFN/TRMD/BWLP/FRO）＋菲律賓電信

新美股：`BMY PGR ACN PSX DAL TROW VZ`
→ 必治妥施貴寶、Progressive、Accenture、Phillips 66、達美、T. Rowe、Verizon

**這解開了一個一直被誤判的問題。** 記憶庫寫著美股清單長得怪（16 檔全是 ADR/航運/MLP、
科技股全滅），一直歸因於「配息率門檻對美股的結構性誤殺」。
配息率確實有影響，但**主因是我們自己把 S&P500 限制拿掉了**——
那些 ADR 和航運股在他的 Finviz 設定裡本來就進不來。

台股 34 檔品質也對：儒鴻 ROE24%、技嘉 32%、一零四 36%、聚陽 27%。

盈再率來源：`official_tw 33、official_us 6、official_us_nolti 1、capex_fallback 1`
→ **41 檔中 40 檔走官方公式（97.6%）**，只剩 2850.TW 一檔用替代算法。

### 模擬倉接上分級

`buffett_top30()` 原本 `rr is not None and rr >= 0.80`，兩個洞：
① 抓不到資料直接放行——「一無所知」比「知道有問題」還容易過關；
② 只比大小不看方向——盈再率 −43%（縮表）會被當成優等生買進。

改成只收 `grade in ("ideal","acceptable")`。
**保留舊檔相容**：`grade is None` 時從 rr 推導，不直接判不合格——
8/17 就是讀一個永遠不存在的欄位（`"fair"`）讓巴菲特倉持股長期是 0 且完全不報錯。

驗證：買進區 16 檔不變（PGR 本來就在觀望區），但 PGR 與 2850.TW 已確認被規則排除。

### 順手修的靜默失敗
`weekly-screen.yml` 的 `python buffett_scan.py ... || echo "掃描失敗，跳過"`
→ 失敗會變成 exit 0、workflow 綠燈、頁面照更新，看不出資料沒動。已拿掉。

### 教訓
用戶說「**不要再自己變更條件！這之前已經有說過了**」。

三次跑偏是同一個模式：**有一個聽起來很合理的理由，但沒去查原文**。
第三次特別值得記——那不是亂改，是有理有據地改錯了。
**理由自洽 ≠ 有依據**，差別在有沒有回去讀第一手。

已寫進記憶庫 `feedback_no_self_change_criteria.md`，
並在 `hongruitai_method.md`（🔒 硬規則檔）補上篩選器設定原文。

---

## 2026-08-25（晚）· 技術面三指標補齊老墨加值層 + 產業輪動雷達 RRG 上線

### 技術面四指標：對照老墨 XQ 官方指標找出缺口

用戶分享老墨的 XQ XScript 腳本庫（GitHub `mophyfei/MOFI_XQ`）——SUPER TREND PRO MAX、
EXCEED CHARGE、RS STRONGER 三支指標的說明頁，跟我們 `technical_indicators.py` 現有版本
逐項對照，發現我們只做了「基礎線」，缺了官方版真正拿來判斷的加值層：

- **SuperTrend**：加「過去10年歷史統計」——多空平均/最短持續根數、20日延續機率、
  這波走了幾根、贏過歷史幾成。另抓10年資料算統計（跟展示用2年序列分開）。
- **Exceed Charge**：擠壓三級強度（微/中/極，用近一年自身分布相對分級）+ 動能四色文字標籤。
- **RS 相對強弱**：RS創新高偵測 + 「RS領先股價」背離訊號（老墨自己說最有價值）+ 短長線交叉。

指標邏輯不開源（`.xsb`/`.daox` 是編譯過的二進位容器，用 `file` 指令確認過），
只能照他公開說明頁的文字重建，不是照抄程式碼——數字不會逐點對上官方版，
方法論一致。**沒有動 RS 短30日/長1年這兩個既有窗口**，那是先前跟財報卡對齊的決定，
不是老墨的預設(60/240)，加值層直接套用在既有序列上。

驗證：合成資料測過訊號方向正確；41檔巴菲特清單全量跑過（擠壓分級10次觸發、
AMD實測極壓）；2454.TW 財報卡實際渲染確認沒破版。

### 產業輪動雷達 RRG：新功能，美台資料源不對稱

用戶截圖老墨的「PROJECT RX 產業輪動雷達」要求做同款功能。查證後這是 RRG
（Relative Rotation Graph，Julius de Kempenaer 發明，StockCharts/Bloomberg 業界標準），
**不是老墨自創、有公開方法論**，但精確公式他沒公布（連 RRG 官網自己都拒絕提供）。
改用業界廣泛引用的開源版本（BennyThadikaran/RRG-Lite wiki）：

```
RS-Ratio    = 100 + (RS − MA_N(RS)) ÷ StdDev_N(RS)
RS-Momentum = 100 + z-score(ROC_N(RS))
```

**資料源美台不對稱，是真實限制**：
- 美股用 11 檔 SPDR 類股 ETF（業界標準籃子，StockCharts 自己的美股 Sector RRG
  用的也是這組）——免聚合，11/11 全可抓。
- 台股沒有官方類股指數 ticker 可直接抓（yfinance 查無、Yahoo TW 只有網頁形式），
  改成自己聚合：TradingView sector 分類（美台共用同一套20分類，不用跨市場映射）下
  市值前8大成分股等權組成籃子指數。17個籃子（成分股≥3才收）全部算出結果。

老墨版吃的是 XQ 自己維護的台股「族群/細產業指數」（含「元宇宙」這種自訂主題），
那是 XQ 專屬資料接不到；我們的版本粒度較粗但方法論一致。

實作要點：籃子指數用「日報酬率等權平均後複利」而非直接平均價格（避免高價股蓋過
其他成分股，指數編制標準做法）；歷史抓3年緩衝（N=240的z-score還要疊ROC回看窗）；
軌跡尾巴存 `industry_rotation_history.json`，每次執行append+同日去重，留最近26週。

新增 `docs/rotation.html`，`board_theme.NAV` 加第8個入口，接進 `weekly-screen.yml`
（巴菲特掃描之後、commit之前，**刻意用 `||echo`**——這步失敗若讓它紅燈會擋掉
後面所有步驟，跟核心產出巴菲特掃描的判斷標準不同，見程式碼註解）。

### 順手：NAV 新增連結，6 個既有頁面重新產生才會長出新導覽

`board_theme.NAV` 加頁後其他頁不會自動更新導覽列（先前記憶庫就記過這條），
逐一重跑 buffett/portfolio/gdp/earnings/home/board 六個產生器確認都有新連結、
沒有破版才收工。ark_report.py 較貴（LLM呼叫），留給下次雙週排程自然帶到。

### 未完成

YouTube 教學連結（`tg1HIWUwDgQ`）字幕被關閉、WebFetch 也只拿得到 SPA 外殼，
無法取得內容，這部分是照 README 文字說明實作的，用戶事後看過影片若有出入待補充。

## 2026-08-25 · trade_plan.py 接俗貴價：Firstrade 實際持股現算（非巴菲特候選池）

投資 AI 幕僚升級藍圖（見記憶庫 `investment_advisor_architecture`）優先序 ① 收尾。

**問題**：`buffett_watch.json` 候選池只有 41 檔篩選候選，Firstrade 實際持有 66 檔完全是另一個
集合——用候選池算俗貴價，持股裡沒進候選池的檔案直接消失，等於白做。

**做法**：
- `trade_plan.py` 新增 `compute_valuation(active)`：對 active 持股去重後的 ticker 逐檔現抓，
  重用 `buffett_screener.fetch_fundamentals`+`evaluate`（不重造第二套 EPS 選擇邏輯——美股用預期
  EPS、台股用實績 EPS 這條規則已經在那邊校正過一次，見 `hongruitai_method` 記憶）。
- 新增 `valuation_alert.py`：跟 `st_alert.py` 同一種「翻面才推」模式——
  `state/valuation_state.json` 存上次每檔訊號，現價新翻過貴價（🔴）才推 Telegram；
  首次執行只記狀態不推，避免一次噴滿 60+ 檔。**同一份 state 檔案兼兩用**（翻面判斷的
  上次狀態 + assets-dashboard／Sonia 讀取的持久顯示資料源），不另存第二份。
- 本機排程（`board_analyze_daily.cmd`，Windows Task Scheduler 執行）跟雲端 GitHub Actions
  是兩個不同環境，環境變數不會預先 export——`valuation_alert.py` 跟 `main.py` 一樣直接讀本機
  `.env`（`dotenv_values`），不是像 `st_alert.py`/`alert_telegram.py` 那樣吃 `os.environ`
  （那兩支是給 Actions 雲端用的，吃的是 Actions Secrets）。
- 接進 `board_analyze_daily.cmd`：排在 `earnings_index` 之後、`git commit` 之前。

**首次實跑**（2026-08-25）：59/66 檔算出俗貴價（7 檔缺 EPS 或現價，跳過不猜）。
🔴 已過貴價 7 檔：AAPL / AMD / API / BA / GLW / SPCX / TSLA。🟡 觀望 24、🟢 俗價以下 28。

**顯示端（兩邊都做）**：
- `assets-dashboard`：新增 `/api/valuation` 端點（跨專案直讀 `daily_stock_analysis/state/
  valuation_state.json`，不複製一份）+ 新分頁「俗貴價」，Edge headless 驗證過渲染正確
  （🔴排最前、價位/EPS基礎欄位都對）。
- Sonia：`assets_card()` 加 `_valuation_note()`，ASSETS 卡片兩處都補一行俗貴價摘要
  （🔴貴幾檔／🟢俗幾檔），獨立 try/except 不連累原本的 WoW % 計算。Sonia 本機服務目前休眠中，
  下次啟動自動生效。

**⚠️ 待注意（沒動，先記錄）**：`board_analyze_daily.cmd` 現有的 `git add -f ... state ...`
會把新的 `state/valuation_state.json` 一起推到 board remote（GitHub Pages 用的那個 repo）。
這份檔案只含 ticker/現價/俗貴價（都是公開資訊），不含股數/金額，暴露程度跟既有的
`st_state.json`（同樣間接透露持股 ticker 清單）相當，沿用既有慣例沒有另外處理。

## 2026-08-25（續）· 俗貴價加台股（繼承帳戶）

Leo：「持股可以加上台股的部分嗎？雖然還要跟 Mike 的核對」。

- `compute_valuation()` 改吃 `active + legacy`（俗貴價是顯示不是策略計算，跟 legacy
  帳戶「僅顯示不參與策略計算」的既有原則不衝突）。台股純數字代號自動補 `.TW` 查
  yfinance（`TradingBot/mike_crosscheck.py` 也有同樣的已知限制：上櫃`.TWO`還沒處理，
  兩邊要一起修）。
- **Telegram 翻貴警示仍只算 Firstrade**：台股數字用戶自己說了要等跟 Mike 核對，
  不該拿還沒驗證過的數字去推警示噪音。
- 實跑：11 檔台股（繼承帳戶）7 檔算出、4 檔跳過——`0050`/`009816`/`00981A` 是 ETF
  沒有 EPS（正確跳過，不是 bug）；`1101`(台泥) TTM EPS 目前是負的，算不出俗貴價
  （同樣是正確跳過，不是 bug——虧損公司套 EPS 倍數法本來就不成立）。
- 總計 66/77 檔算出俗貴價（美股 59/66、台股 7/11）。
- assets-dashboard 俗貴價分頁加「市場」欄位 + 幣別對應（NT$/$）+ 市場篩選（全部/美股/台股）。
- 順手修表格對不齊的 bug：CSS `td.num{text-align:right}` 選擇器沒吃到 `<th class="num">`，
  表頭永遠靠左、資料靠右，對不上。改成 `.num{text-align:right}`（涵蓋 th+td），
  全站所有用 `<th class="num">` 的表格一併修正，不是只補這一頁。
- 加訊號篩選（全部/🔴貴/🟡觀望/🟢俗，各自帶數量）+ 顏色說明文字。

## 2026-08-25（續二）· 產業輪動雷達 5 點 UI 反饋（配色/篩選位置/說明位置/拖尾/動畫）

Leo 對 `docs/rotation.html`（`industry_rotation.py::render_html`）截圖反饋 5 點，
跑過 `dataviz` skill 的 `validate_palette.js` 才動手改配色，不是憑感覺挑色。

1. **配色混亂**：根因是同一顆泡泡「填色(8色籃子分類)＋邊框(4色象限)」疊了兩種色相，
   同象限的產業互相撞色，加上跟背景四象限色塊的色系也有重疊。
   - 拿掉泡泡邊框的象限色，改中性色 `rgba(232,242,255,.5)`——象限資訊改成只留
     背景色塊＋泡泡位置本身＋排行榜色點三個管道，不再讓泡泡邊框重複佔用一個色相軸。
   - 8 色籃子分類盤換成 `validate_palette.js` 驗證過的組合（`node scripts/validate_palette.js
     "<8色>" --mode dark --surface #0a1222`，adjacent-pairs 全過）。
   - `--pairs all` 模式下 8 色沒辦法全過（skill 本身文件也講明：全通過的上限是 3 色，
     這張圖要同時顯示 11~17 個籃子，折進「Other」等於毀了圖的用途）——
     每顆泡泡本來就強制標了產業名字（不是選擇性標籤），色相只是輔助辨識，
     這是刻意的取捨，寫進了程式碼註解，不是漏驗證。
   - 象限 4 色（領先/改善/弱化/落後）保留台股慣例：紅＝強／領先（不是西方紅=差），
     一律搭文字標籤（QLABEL）顯示，不單靠顏色分辨。
2. **排行榜「101.1／101.8」沒說明**：加固定表頭列「產業／象限／RS-Ratio／RS-Momentum」，
   跟資料列改用同一組 CSS Grid 欄寬（原本 flex 各算各的寬度，表頭跟資料對不齊）。
3. **篩選面板搬到圖左側**：原本整排在圖表正上方，改成 `.rrglayout` 兩欄 grid
   （240px 篩選欄 + 彈性寬度圖表區），篩選面板內每列改直排（標籤疊在控制項上面，
   橫排在 240px 塞不下）；<980px 收成單欄疊回最上面。
4. **產業說明搬到下面**：`note_html`（象限說明/資料源/泡泡大小/回填/基準限制那一大段）
   從 header 跟控制面板中間搬到圖表區塊下方，打開頁面先看到圖再看說明。
5. **拖尾不明顯 + 動畫還是格狀**：
   - 拖尾殘影透明度 `0.05+t*0.32`→`0.16+t*0.55`，連接線 `0.22`→`0.4`、寬度 `1`→`1.5`。
   - 動畫格狀的根因：原本手動補間 12 個假格、每 35ms 用 `update('none')` 強制關掉
     Chart.js 動畫重畫一次（含 quadrantBgPlugin 背景+bubbleLabelPlugin 文字），
     瀏覽器跟不上這個頻率就掉格，肉眼看到的就是跳格不是滑順移動。
   - 拿掉 `_buildPlaySequence` 手動補間整套機制，改成只在「真實一週」之間切換，
     交給 Chart.js 自己的動畫引擎（`animation:{duration:550,easing:'easeInOutQuad'}`
     + `update()` 預設動畫，不再傳 `'none'`）補間泡泡 x/y/半徑——這是它本來就該做、
     也做得比手動 setInterval 穩的事。播放間隔改 `STEP_MS=700ms`（一個真實週的節奏）。

**驗證方式**：`industry_rotation.py` 只改了 render_html/CSS/JS，沒動資料計算，
用現成 `industry_rotation_history.json`（今天週六排程剛跑過）直接 `render_html()`
重生 `docs/rotation.html`（不用重打一次 yfinance），本機 `http.server` 開起來、
Edge headless 逐項核對：controls 確實在圖左側、note 確實排在圖下面、排行榜表頭文字正確、
泡泡邊框已無象限色、8色籃子盤/trail alpha 數值都對、播放一輪 13 週無 console 錯誤、
播放完自動 stopPlay+draw 復位正常。

**未做**：`git commit`/`push` 沒有做（本檔跟 `industry_rotation.py`/`docs/rotation.html`
都还是 working tree 上的修改），要不要推到 board remote 讓 GitHub Pages 生效由 Leo 決定。

## 2026-08-25（續三）· 產業輪動雷達第二輪 UI 反饋（6 點）

Leo 看過第一輪修正後的截圖再反饋 6 點。

1. **排行榜移到圖表下面**（原本在右側）：`.rrgwrap` 從左右兩欄 grid 改成上下堆疊
   （`display:flex;flex-direction:column`），排行榜面板加 `max-width:640px` 避免
   在全寬版面下每列被拉得過寬、名字跟數字中間留一大段空白。
2. **篩選按鈕擠成兩排**：左側篩選欄從 240px 加到 300px，`.rrgctrl` 範圍內的按鈕
   縮小一號（padding/字級都比全站 `.seg button` 小），「計算週期」「回放範圍」
   兩組 4 顆一排的都確認維持一列不換行。
3. **🔴動畫從下往上長，不是照真實軌跡**：這是真的 bug，不是「不夠滑順」而已。
   根因：第一輪改成交給 Chart.js 自己的 `animation` 系統補間，但每次 `update()`
   都整個換掉 `data.datasets` 陣列——Chart.js 認不出「這是同一顆泡泡的延續」，
   把每次更新都當「新元素」處理，新元素找不到前一個真實座標時預設從 Y 軸底部
   （scale 的 0/base）長出來。
   **修法**：徹底不用 Chart.js 的動畫系統（`animation:false`、`update()` 永遠帶
   `'none'`），改成手動算好每一格補間座標（`_lerp` 直接在兩個真實週之間插值）
   直接餵給 Chart.js 畫——Chart.js 純粹畫「這格算好的靜態座標」，不會被它自己
   「新元素怎麼進場」的規則干擾。驅動方式從 `setInterval` 改 `requestAnimationFrame`
   （綁瀏覽器實際繪圖節奏，畫面忙不過來會自動跳格而不是硬擠，比固定間隔更穩）。
   **驗證方式**：這個沙盒環境的瀏覽器分頁 `document.visibilityState` 是 `hidden`，
   rAF 依規範會被瀏覽器暫停，沒辦法直接錄到動畫過程——改用純函式驗證：
   手動呼叫 `_lerp`/`_easeInOutQuad` 用真實的兩週資料算 t=0/0.3/0.5/0.8/1 的座標，
   確認 t=0 精確等於前一週真實值、t=1 精確等於下一週真實值、中間單調過渡，
   全程沒有一個值碰到 `AXIS_MIN`（座標軸底部）——證明軌跡是兩個真實點之間的
   直接插值，不會再出現「從底部長出來」這條路徑。實際動畫效果要 Leo 在真的瀏覽器
   分頁（非隱藏）驗證。
4. **X/Y 軸再多說明**：`quad_note` 補上白話解釋——RS-Ratio 100 是什麼意思、
   RS-Momentum 是「強弱的強弱」不是強弱本身，四象限各自也補一句白話註解。
5. **「甜蜜點」那句粗體反色**：加 `.hl`（`background:var(--accent);color:#04070f`），
   深底淺字的版面裡插一塊淺底深字的反色標記，視覺上會跳出來。
6. **下方說明段落不明顯，重新設計**：原本是一整坨用 `<br>` 接起來的 muted 灰字。
   改成有標題的卡片（📖 名詞與方法論說明）＋逐條分隔線，主要說明字色調亮
   （`#cfe6ff`），只有最後一條「已知限制」維持次要層級的 muted 色。

驗證：`docs/rotation.html` 用同一套（讀 `industry_rotation_history.json` 最後一筆快照
+ period key 轉型別）重生，不重打 yfinance；Edge headless 逐項核對版面（排行榜在下面、
篩選按鈕都一列、note 卡片文字）、`.hl` 樣式、播放/停止狀態機無 console 錯誤。

## 2026-08-25（續四）· 產業輪動雷達第三輪 UI 反饋（3 點）

1. **座標軸範圍太寬，資料擠在中間**：`_axis_bounds()` 原本用「絕對 min/max + pad=2.0」，
   52 週歷史裡只要有一兩天出現極端值，整個範圍就被撐開，平常 95% 的日子資料其實擠在
   中間一小段。改用 p2~p98 百分位數（丟掉最極端的 2% 尾巴）+ pad=1.0，範圍從
   93.8~107（span 13.2）縮到約 96.5~103.7（span 7.2），減少約 45%。
2. **排行榜太窄、太空**：`.rrgrankpanel` 拿掉 640px 上限，改跟圖表同寬；
   多出來的空間補兩欄現成資料（不用另外抓）：
   - **資金規模**：原本只有泡泡大小看得出來，數字化（美股 $xxB、台股 xx兆/億）
   - **強弱位置迷你條**：RS-Ratio 在座標軸範圍內的相對位置，跟圖表 X 軸對照著看
3. **篩選面板置中並固定在圖旁邊**：`.rrglayout` 的 `align-items` 從 `start` 改
   `center`（篩選面板內容比「圖表+排行榜」疊起來的總高度矮很多，貼頂看起來偏上，
   改置中跟整個右側區塊對齊）；`position:sticky` 本來就有（往下捲動會貼著畫面），
   小螢幕(<980px)維持 `position:static`（避免堆疊版面下 sticky 行為很怪）。

**⚠️ 這輪沒能視覺驗證第 3 點**：這次 Edge headless 分頁的 viewport 回報
`window.innerWidth/innerHeight = 0`（Browser pane 目前沒有實際顯示/合成畫面，
跟這個 session 稍早螢幕截圖失敗是同一個限制），觸發了 <980px 的手機版 media query，
量到的是手機版堆疊版面（篩選面板跟圖表同寬、sticky 被停用），不是桌面版真實效果。
第 1、2 點的驗證不受影響（座標軸範圍是算出來的數字、排行榜等寬是比較兩個同層級
元素，兩者都跟 viewport 寬度無關）。CSS 原始碼確認過沒有其他規則覆蓋
`position:sticky`（唯一的 override 正確限定在 `@media (max-width:980px)` 內），
邏輯上桌面版應該會如預期置中+sticky，但要 Leo 在真的瀏覽器分頁確認。

## 2026-08-25（續五）· 產業輪動雷達第四輪 UI 反饋（5 點）

1. **「強弱位置」是什麼**：加 `title` 屬性 hover 提示＋在下方說明卡片補一條，
   講清楚是 RS-Ratio 在座標軸範圍內的相對位置。
2. **RS-Ratio／RS-Momentum 標題太擠**：合併一欄擠成兩行，拆成兩個獨立欄位
   （各自欄寬夠放一行），排行榜從 6 欄變 7 欄。
3. **篩選面板不用跟著滾輪跑**：拿掉 `position:sticky`，改回一般文件流（`static`）——
   上一輪加的 sticky 是我自己過度設計，用戶要的其實是「待在圖表旁邊的固定位置」，
   不是「跟隨捲動釘住」。順手拿掉 `class="ctrl rrgctrl"` 裡混用的全站共用 `.ctrl`
   class（那是另一個頁面元件的 sticky 頂列，z-index/border-bottom/margin-bottom
   都是那邊要的，混在一起有樣式互相污染的風險，這裡完全自訂樣式不需要它）。
4. **座標軸範圍還要再小**：第一輪 p2~p98+pad1.0（span 7.2）反饋還是太寬，
   這輪收緊到 p5~p95+pad0.6，span 降到約 5.7。（再往下收會開始常態性地把普通日子
   的資料切在邊界外，不只是真正的極端值，先停在這裡。）
5. **拖尾還是不清楚**：根因跟「配色混亂」那次類似——不是透明度不夠，是
   11~17 條軌跡同時全部顯示，本來就會互相蓋住看不出方向。改成 hover-to-isolate：
   滑鼠移到某顆泡泡（Chart.js `onHover`）或排行榜某一列（`data-key` + 事件委派），
   只留那條軌跡清楚（線寬2.5/不透明度0.95），其餘壓到幾乎看不見（0.05），
   移開恢復原本全部淡淡顯示的樣子。播放中不處理 hover（避免跟動畫補間互相打架）。

驗證：`docs/rotation.html` 同一套方式重生；Edge headless 逐項核對——axis span
從 7.2 降到 5.70、`.rrgctrl` 已無 `ctrl` class 且 `position` 確認是 `static`、
排行榜每列 7 個欄位（RS-Ratio/RS-Momentum 已分開）、`data-key` 屬性存在、
直接呼叫 `setTrailHighlight('XLY')` 確認 11 條軌跡線裡剛好 1 條高亮(0.95)+
10 條壓暗(0.05)、重置回 null 恢復正常，全程無 console 錯誤。

## 2026-08-26 · 產業輪動雷達：確認第四輪已生效 + 新增產業複選功能

**Leo：「好像沒改成功」**——查證後 `docs/rotation.html` 確認已含全部第四輪修正
（`grep` 檔案內容：`.rrgctrl` 已無 `ctrl` class 且無 `position:sticky`、
`AXIS_MIN/MAX = 97.2/102.9`、`setTrailHighlight` 存在）。檔案 mtime 是週六排程
自動重跑覆蓋的（用最新程式碼、當下即時資料），不是沒改到——推測是瀏覽器快取
沒重新整理，或看錯頁面（本機預覽 vs 正式站，這批改動都還沒 push）。

**新功能：排行榜可複選只看某幾個產業**（Leo：「可以只選下面的產業只跑那一個
或幾個嗎」）：
- 點排行榜某一列＝勾選/取消勾選（複選），圖表跟播放都只顯示勾選的幾檔；
  再點一次「清除選取」恢復顯示全部。
- 跟既有的 hover 高亮軌跡（滑鼠移過去）是兩個獨立機制，不衝突——hover 是
  「看一下這條」的暫時效果，點擊才是「圖表只留這幾檔」的持久篩選。
- 換市場（美股/台股）會自動清空選取（代號完全不同一套，選取沒意義）；
  換基準/週期/回放範圍**不會**清空（選了哪幾個產業跟這些設定無關，沒理由重置）。
- 播放時的補間、軌跡都套用同一份篩選（`buildTrailDs` 加 `onlyKeys` 參數，
  被篩掉的籃子軌跡也不畫——反正對應泡泡已經不在畫面上，畫軌跡沒意義）。

驗證：Edge headless 直接呼叫（點擊模擬）——單選 1 檔泡泡數從 11→1、複選 2 檔→2、
清除恢復 11；切換市場確認選取被清空；播放中選取 1 檔確認畫面上只有 1 顆泡泡在動；
全程無 console 錯誤。

## 2026-08-26（續）· 產業輪動雷達第五輪反饋（4 點）

1. **篩選面板上緣沒對齊圖表上緣**：`align-items:center` 是上一輪我自己猜錯需求
   （以為要跟整個「圖表+排行榜」疊起來的總高度置中），改回 `align-items:start`。
2. **圖的顏色跟下面產業顏色不一致**：真的 bug——圖表泡泡填色是產業分類色
   （8色循環），排行榜圓點卻是象限色（4色：領先/改善/弱化/落後），兩套不同編碼
   當然對不上。改成排行榜圓點＋強弱位置迷你條標記都改用跟泡泡同一個
   `keyColor(產業)`，象限資訊本來就有文字欄顯示，不會少資訊。
   已驗證：排行榜某籃子 dot 顏色跟圖表上同一籃子泡泡的填色逐 RGB 比對完全一致。
3. **資料正確性查證**：獨立重新抓 XLK/^GSPC 價格、從頭手算 RS-Ratio/Momentum(N=20)，
   對到小數點後兩位精確吻合存檔值（99.19/100.32）——**計算邏輯本身沒問題**。
   但意外發現一個標籤時間差：存檔標的「日期」是排程執行當下的系統日期
   （`time.strftime()`），不是資料實際對應的收盤日——這次驗證時發現 2026-08-25
   標記的快照，用的其實是 2026-08-24 收盤價算出來的（差一個交易日）。
   不影響 RS 數值本身正確性，只是「這格標的日期」可能跟「這格用的是哪天收盤價」
   有一天落差，**還沒修，等 Leo 決定要不要處理**（要修的話是把 `date_str`
   改成從價格序列的實際最後一筆時間戳取，不是系統時間）。
4. **雙軸範圍改回固定 94~106、刻度間隔 1**：前兩輪從固定 93.8~107 逐步收緊到
   p2~p98(7.2)、p5~p95(5.7) 百分位數動態算，範圍越收，同樣的真實價格波動在畫面上
   占的比例越大，播放動畫看起來「移動太劇烈」——改回固定範圍（不再用
   `_axis_bounds()` 百分位數），animation 幅度不會再隨資料每天變動。

## 2026-08-26（續二）· 產業輪動雷達：排行榜填滿寬度 + 多週期欄 + 展開前5大成分股

1. **左右填滿**：強弱位置迷你條欄從固定 100px 改 `1fr`（吃掉剩餘寬度），
   在寬版面下會跟著排行榜面板一起伸縮，不再留一大塊空白。
2. **加多週期欄**（用戶要我建議加什麼）：4 個小圓點對應 20/60/120/240 日，
   顏色=該週期的象限色，一次看短中長期是否一致（趨勢一致 vs 正在轉折）。
   重用已經抓好的 `window.RRG_DATA` 全週期資料，不用額外打 API，零成本。
3. **展開看前5大成分股**（新功能）：
   - 美股：`yfinance` 的 `funds_data.top_holdings`——SPDR ETF 官方申報持股權重，
     不是我們拼的近似值。
   - 台股：`_tw_sector_members()` 順手多留前5大（本來就在同一批 TradingView 快照裡，
     幾乎零成本）；公司名用 `description` 欄位（英文全名，TradingView 沒有覆蓋夠廣的
     中文對照表，寧可顯示英文全名也不瞎猜——這頁台股其他地方顯示中文是因為用
     `SECTOR_TW_LABEL`/既有翻譯，個股層級沒有現成資料源）。
   - `_fetch_baskets()` 回傳值從 `(baskets, index_bench)` 擴成
     `(baskets, index_bench, holdings)`，`main()`/`render_html()`/`_rrg_script()`
     一路傳下去，embed 成 `window.RRG_HOLDINGS`（靜態資料，不像 RRG_DATA 要按
     市場/基準/週期分層）。
   - 前端：產業名稱前加展開箭頭（▸/▾），點擊只展開不連動勾選（獨立事件目標，
     跟「點列表勾選只看幾檔」是分開的兩個機制）。

驗證：獨立跑 `_spdr_top_holdings('XLK')` 確認回傳 NVDA/AAPL/MSFT 等權重合理；
`_tw_sector_members()` 確認 17 個 sector 都帶 top5；Edge headless 展開/收合來回測試
確認箭頭狀態、DOM 節點增減、`expandedKeys` 狀態都正確，展開不會誤觸圖表勾選；
多週期欄 4 個點的 tooltip 文字正確對應短線/波段/中期/長期；全程無 console 錯誤。

## 2026-08-26（續三）· 產業輪動雷達：修回歸 bug + 圖表點選

**🔴 真的 bug（不是反饋，是我自己上一輪引入的回歸）**：Leo 反饋「產業大類名字消失了」
「產業欄位沒有左右填滿頁面，且多週期、強弱位置跑掉」。

根因：加展開箭頭那次，把獨立的 `<span class="dot">` 併進「產業」欄本身
（`.nm` 裡塞展開箭頭+色點+名字），資料列從 8 個直屬 span 變成 7 個，
但 `grid-template-columns` 還留著原本 8 欄的格線定義（`14px 1fr 40px ...`），
表頭也還留著開頭那個對應舊 dot 欄的空 `<span></span>`。
7 個元素套進 8 欄格線，每一欄全部往前錯位一格——「產業」被塞進最窄的 14px
格子（文字被裁到看不見）、「象限」占走了本該給「產業」的 1fr 寬欄、後面每一欄
都跟著錯位，最後一欄（強弱位置該有的 1fr）沒人用，看起來像「沒填滿」。

**修法**：表頭跟資料列的欄數/欄寬改成一致的 7 欄（拿掉表頭那個空 span，
grid-template-columns 拿掉開頭 14px），`nth-child` 選擇器索引跟著調整。
同時發現色點（dot，對應 keyColor 分類色）在這次重構中被整個拿掉了——
這正是上上一輪才修好的「圖表顏色跟排行榜顏色一致」那個功能，等於做完新功能
順手弄丟了舊修正，補回 `.nm` 裡的色點 span。

**教訓**：改資料列的 HTML 結構（增減欄位）時，表頭的欄位數／grid 定義要
同步檢查，兩邊是分開維護的字串，沒有自動同步機制，這次是切欄位時漏了對表頭做
同樣的修改。

**新增：點圖表泡泡也能做產業篩選**（Leo：「新增點圖的圓圈就做產業篩選」）：
Chart.js `onClick` 呼叫跟排行榜點列表同一個 `toggleSelect()`，圖表跟排行榜是
同一套篩選狀態，兩邊點哪邊都一樣（點泡泡篩、點列表也篩，互通）。

驗證：Edge headless 確認表頭/資料列都是 7 個 span、對齊；產業名稱、dot 顏色
（跟泡泡填色逐 RGB 比對一致）都正常顯示；模擬呼叫 `onClick` 確認點泡泡能
選取/取消選取（用正確索引重新查找，避免用篩選前的舊索引誤判）；展開成分股功能
沒有被這次改動影響，仍正常；全程無 console 錯誤。

## 2026-08-26（續四）· 產業欄再收緊一輪：兩個 1fr 互搶空間

Leo：「產業左右邊框還是沒改到」——問清楚後確認是「產業欄太窄，還是被切到/擠住」。

**根因**：`grid-template-columns: 1fr 40px 60px 68px 64px 128px 1fr` 有兩個 1fr
（產業欄／強弱位置迷你條），CSS Grid 預設把剩餘空間平分給所有 1fr 軌——
但「強弱位置」只是一條裝飾用的迷你進度條，不需要那麼多寬度也能看，
真正需要空間的「產業」名稱欄卻只分到一半，長一點的產業名（如「非耐久消費品」
「電子科技」）在較窄版面下會被裁切。

**修法**：改成只有「產業」是 1fr（獨占全部剩餘空間），強弱位置從 1fr 改回固定
180px（比最早的 100px 寬，仍會隨版面整體寬度間接變寬，只是不再跟產業欄搶）。

⚠️ 這次沒能在這個 session 的測試環境裡拿到有效驗證——Edge headless 分頁量到
viewport 還是 0（跟前幾輪同一個環境限制，Browser pane 沒有實際顯示/合成畫面），
grid 在退化成極窄容器下量出全部 17 個台股產業名稱都被判定溢出，這是環境問題
不是真實訊號。CSS 邏輯本身是對的（拿掉了兩個 1fr 互搶的根因），但畫面實際好不好
要 Leo 自己看過才算數。

## 2026-08-26（續五）· 排行榜獨立滿版寬 + 篩選面板高度對齊圖表

Leo：「這個左右大小可以跟市場+圖左右一樣大嗎？然後市場(篩選)上下跟圖上下一樣大」。

1. **排行榜面板改滿版寬**：原本困在 `.rrglayout` 兩欄結構裡「圖表」那個 1fr 欄
   （寬度＝圖表寬度），現在拆成兩塊——`chart_html` 只剩圖表本身留在兩欄結構裡，
   `rank_html`（含選取列/表頭/清單）搬到 `.rrglayout` 外面獨立一個滿版寬區塊，
   寬度＝篩選欄+圖表欄相加的整個版面寬度。這也順帶讓上一輪「產業欄太窄」的問題
   有更多空間可用（多了原本篩選欄那 300px+間距）。
2. **篩選面板高度對齊圖表**：`.rrgctrl` 從自然高度改固定 520px（跟 `.rrgbox` 一樣），
   `justify-content:space-between` 把 6 排控制項平均撐開分布在整個高度，不是全部
   擠頂端底下留空白。小螢幕（≤980px，篩選面板疊到圖表上方那種版面）改回
   `height:auto`，不然自然堆疊時被鎖死 520px 會很奇怪。

驗證：Edge headless 這次直接查 DOM 結構（不是量 pixel，因為這個環境的 viewport
量測有已知限制）——確認 `.rrgrankpanel` 現在是 `.rrglayout` 的**同層級兄弟節點**
（`isSiblingOfLayout: true`），不再嵌在 `.rrgwrap` 裡面（`rankHTML_hasNoDoubleWrap:
true`），結構上保證了滿版寬（跟 `.rrglayout` 一樣是 `.wrap` 容器的直接子元素，
會自動吃到同樣的可用寬度，不受這個測試環境 pixel 量測失真的影響）。順手用
`toggleSelect`/`clearSelection` 確認搬動 DOM 結構後互動功能沒被破壞
（勾選→1顆泡泡、清除→恢復11顆），全程無 console 錯誤。

## 2026-08-26（續六）· 產業輪動雷達：預設值 + 說明文字 + 欄距加大

1. **預設 60日/3個月/8週**：計算週期預設從 20日改 60日（Leo 指定，JS 初始值＋
   對應按鈕的 aria-pressed 兩處要同步改，漏一處會出現「畫面顯示60日但curP還是20」
   這種狀態不一致）；回放範圍3個月、尾巴長度8週本來就已經是預設值，不用改。
2. **回放範圍／尾巴長度加說明**：直接寫在控制項下面（不是只塞進最下面的說明卡片）——
   這兩個常被搞混但其實是兩件事：「回放範圍」是按播放鍵時動畫要走多久的歷史；
   「尾巴長度」是任何時刻（不只播放中，靜態畫面也一樣）每顆泡泡身後拖著幾週前
   的殘影，兩者互相獨立。
3. **中間幾欄太擠**：象限/RS-Ratio/RS-Momentum/資金規模/多週期這幾欄反饋擠在一起。
   欄距 gap 從 8px 加大到 18px；順便把 RS-Momentum 欄加寬（68→86px，這個字本身
   在窄欄位還是會換行）、象限欄加寬（40→52px）。

驗證：Edge headless 確認 `curP` 初始值跟按鈕 `aria-pressed` 狀態一致都是60日、
回放/尾巴預設值正確、兩則新說明文字都有正確插入 DOM；欄距/欄寬的 CSS 數值
（gap:18px、各欄寬度）確認正確套用到樣式表。RS-Momentum 是否真的不再換行
這次同樣受限於這個 session 的 viewport 量測異常（同前幾輪），沒能拿到桌面寬度下
的可信視覺驗證，邏輯上 86px 應該夠放，實際效果待 Leo 確認。

## 2026-08-26（續七）· 產業輪動雷達：資金大小回填說明 + 軌跡起點/中點標記

**Q1「資金大小圈圈會跟著改嗎」**：不會，這是已知的簡化，不是新問題——
`compute_snapshot()` 的 docstring 早就寫了：「回填快照的資金規模是用**現在**的
AUM/市值回貼過去，RS-Ratio/Momentum 本身才是當時真實價格算的，只有泡泡大小是
簡化」。沒有另外抓歷史 AUM/市值資料（那要逐檔逐週再打一輪 API，成本高，而且
台股籃子的歷史市值資料來源目前也沒有），所以拖尾上的殘影泡泡大小其實都是
「今天的資金規模」套用到過去每個位置，只有 X/Y 座標（RS-Ratio/Momentum）是
當時的真實值。這個限制已經寫在頁面下方的說明卡片裡，這次只是口頭再確認一次，
沒有改動。

**Q2「可以標出第一個點還有中間跟最後嗎」**：加了。「最後（現在）」本來就有
主泡泡+名字標籤，新增起點（該籃子拖尾最舊的一格，標日期+「起點」字樣）跟
中點（拖尾中間那一格，標日期）兩個小標記。**只在 hover 高亮單一籃子時顯示**
（不是每條軌跡都標）——如果 11~17 條軌跡同時全部顯示起點/中點標記，又會變回
「配色/軌跡混亂」那個已經解決的問題，等於自打嘴巴。

實作：`buildTrailDs()` 收集歷史點時順手把該幀日期補進點物件（原本點物件沒帶
日期，只有 frame 本身有）；高亮某籃子時，把起點/中點的座標+日期寫進
`window._rrgTrailMarkers`；新增 `trailMarkerPlugin()`（跟既有的
`quadrantBgPlugin`/`bubbleLabelPlugin` 同一種寫法，Canvas 直接畫，不加外部套件），
用 `chart.scales.x/y.getPixelForValue()` 把資料座標換算成當下實際像素位置畫文字。

驗證：`setTrailHighlight('XLE')` 確認產生 2 個標記（起點帶日期+「起點」字樣、
中點帶日期），`setTrailHighlight(null)` 確認標記清空，全程無 console 錯誤。

## 2026-08-26（續八）· 產業輪動雷達：美股改用 TradingView 成分股聚合（全換，重大方法論變更）

**背景**：Leo 一路追問「資金規模圈圈會跟著改嗎」→「美股真的抓不到歷史市值嗎」→
「TradingView 美股分類是什麼」→「只換資金規模能做到嗎」→「TradingView 歷史市值抓
得到嗎」→ 最終拍板「用TV做吧，然後自己建資料庫存每週真實數據」。查證過程：

- `yfinance get_shares_full()` 對 ETF（XLK/XLF/SPY 全部測過）回傳 None，對一般股票
  （AAPL 測過）正常——ETF 股數變化走申購贖回機制，沒有像個股一樣的公開揭露資料源。
- TradingView 股票掃描器查無 XLK（這支掃描器骨子裡不收錄 ETF）。
- TradingView 掃描器本身沒有任何日期/歷史參數（`Query` 類別方法列表逐一查過），
  是純即時快照工具，不管台股美股都查不到「歷史」市值。
- 結論：資金規模要隨時間變化，只能「歷史股價(yfinance真實逐日) × 股數(TradingView
  現在查一次，假設這段期間大致不變)」自己算，不管台股美股都一樣，而且只有**個股**
  查得到股數，ETF 本身查不到——這就是「只換資金規模」為什麼繞不開換成分股籃子。

**決定**：美股放棄 SPDR ETF，改用跟台股同一套方法（TradingView sector 分類聚合成分股）。

**實作**：
- `_tw_sector_members()` 通用化成 `_sector_members(market, min_market_cap)`，
  美股/台股共用同一個函式，只有 exchange 篩選跟 ticker 後綴邏輯不同。
- 新增 `total_shares_outstanding` 欄位查詢；新增 `_basket_size_series()`
  （歷史股價×股數加總）+ `_size_at()`（查某時間點的值，抓不到就退回最近前一筆）。
- `compute_snapshot()` 的 baskets tuple 第4欄從單一 `size` 值改成 `size_series`，
  現在快照跟每一筆回填都各自查自己時間點的真實值，不再是「今天的值套用到過去」。
- `_fetch_baskets()` 美股台股合併成同一條路徑，移除 `SPDR_SECTORS`／`_spdr_aum()`／
  `_spdr_top_holdings()`。

**實測踩到的 2 個真 bug（不是我猜的，是抓下來資料才發現）**：
1. **同一家公司在美股清單裡被重複計算**：AT&T 的 `T`/`T/PA`/`T/PC`（普通股+兩種特別股）
   市值幾乎一樣、Deutsche Telekom 的 `DTEGF`/`DTEGY`（兩個 OTC ADR 代號）也是同一家——
   不篩會讓同一家公司的市值在同一個籃子裡被算兩三次，資金規模嚴重灌水。
   修法：只收 NYSE/NASDAQ（排除 OTC）、排除代號帶 `/` 的特別股。
2. **`BRK.A`/`BRK.B`/`PBR.A` 抓不到歷史價**：TradingView 特別股代號用句點，
   yfinance 要連字號（`BRK-B`）——不轉換 yfinance 直接回「無此標的」。修法：
   美股 ticker 丟進 yfinance 前先把 `.` 換成 `-`。

**驗證（實測，非推測）**：
- `_sector_members('america')` 確認 20 個分類、去重後無同公司重複、`BRK-B`可正常抓取。
- `_fetch_baskets('us')` 實跑 38.1 秒建出 20 個籃子。
- **核心驗證：資金規模真的隨時間變化**——直接印出 Consumer Durables 過去 8 週的
  size：$2,112B→$2,086B→$2,053B→$1,992B→$1,738B→$1,818B→$1,847B→$1,855B→
  （現在）$1,918B，逐週真實不同，不再是同一個數字複製貼上。
- 真的跑了一次 `python industry_rotation.py`（不是預覽腳本，是正式 main()）：
  美股20個籃子、台股17個籃子都算出結果並回填，`docs/rotation.html` 跟 obis
  `04_AI Report/Investment/產業輪動雷達.html` 都寫入成功，`industry_rotation_history.json`
  today(2026-08-26)這筆已經是新方法論的資料（basket key 從 `XLK` 變成
  `Commercial Services` 這種 TradingView 分類名）。
- Edge headless 開真實頁面確認：美股泡泡數20顆、排行榜20列、中文分類名正確顯示、
  拖尾殘影半徑逐格不同（17.6/17.8/20.3/20.1/18.6/16.5，非固定值），全程無 console 錯誤。

**已知代價（Leo 已知情選定）**：
- 跟 SPDR 官方版（業界標準、真實可交易）脫鉤，美股從此也是近似值，不再精確對應
  GICS 11大類。
- 美股既有52週歷史（用XLK真實股價算的）在切換這天（2026-08-26）出現方法論斷點，
  拖尾軌跡切換那一週會看到不連續的假跳動，不可逆——已在頁面說明卡片裡明講。
- 台股的資金規模同時受惠變準確（原本也是「合格成分股市值合計」的單一快照值，
  現在也變成真實歷史序列了）。

**「自己建資料庫」的釐清**：不需要另外蓋新資料庫——`industry_rotation_history.json`
＋既有的 `append_history()` 機制本來就會每週六累積一筆新資料，這次改動讓「資金規模」
這個欄位從「回填的假值」變成「當週真實算出來的值」，之後每週排程自然累積出真實的
每週歷史，不用新增任何基礎設施。

**⚠️ 這輪意外發現**：`daily-stock-board` 這個 repo 除了平常在改的這批 flat script
（`industry_rotation.py`/`board_html.py`/`main.py` 等根目錄檔案）之外，還存在一整套
`src/`/`apps/dsa-web`/`apps/dsa-desktop`/`api/`/`bot/`/`docker/` 的正式工程架構＋
`AGENTS.md` 治理文件（PR審查流程、CI gate、bilingual docs）——查證過這套新架構
**沒有**跟這次改的產業輪動頁重疊的功能，git log 也確認 `industry_rotation.py`
本來就是直接 commit 到 main 的模式（歷次「feat: 產業輪動...」commit 都是這樣），
跟這次的直接編輯+regenerate 工作方式一致，沒有衝突。但這是這個 session 第一次
發現這套新架構存在，值得讓 Leo 知道——如果之後有計畫把這批 flat script 併進
新架構，會是完全不同量級的工程。

## 2026-08-26（續九）· trade_plan.py 補齊目標價/風險報酬比/失效條件

Leo 拍板：只給有依據的策略補這三個欄位，不是全部策略一律套用同一個模板
（PDF那篇文章的通用checklist跟現有4個策略的出場哲學不完全相容，見前幾輪討論）。

**巴菲特價值**（`buffett_targets()`）：
- 目標價 = 貴價（`evaluate()`的`exp_price`，已經是既有邏輯，不是新算的）
- 失效條件 = `quality_ok` 翻 False（品質關沒過，`signal`變SKIP）——「公司變壞就該賣，
  跟股價無關」這個判斷本來就存在於 `buffett_screener.evaluate()`，這裡只是抽出來用
- 風險報酬比 = (目標價−進場價)÷(進場價−保險絲價)，保險絲價複用既有的
  `fuse_line()`（−30%），不是另外定義一個停損價

**三指標合流／產業鏈+趨勢**（`supertrend_invalidation()`）：
- 只補失效條件現況，不補目標價/風險報酬比——`EXIT_RULES`白紙黑字寫著
  「不設固定停利」，這是趨勢跟隨策略的既有決定，補目標價會推翻它
- 複用 `paper_portfolio.py::combo_signal_events()` 已經回測驗證過的規則
  （Leo 2026-08-11：「ST反轉賣一半、RS跌破60MA全出」是他自己回測出來最優的
  進出法，見 `backtest_position_sim.py`）——不是重新設計規則，是把「現在這檔
  觸發到哪一階段」講清楚
- `board_html.supertrend()` + `technical_indicators.mansfield_rs_series(closes,
  bench_closes, 60)` 兩個既有函式直接重用，範圍只涵蓋 Firstrade(美股)，
  跟 `ACTIVE_ACCOUNTS` 一致，沒處理台股後綴（那本來就不在這兩個策略的範圍內）

**產業鏈全**：三個欄位都跳過，維持原樣——出場依據是「每週重篩掉出守備清單」，
相對排名邏輯，本來就沒有目標價/停損/失效條件這種東西。

驗證：實跑 `build_plan()` 兩個真實案例——AAPL(巴菲特價值) 目標價$286.16
（跟這個月稍早俗貴價功能驗證過的數字完全一致）、風險報酬比1.44、失效條件正確顯示
已觸發(🔴高於貴價)；NVDA(三指標合流) 正確顯示「尚未觸發失效條件」（現況確實還在
多頭趨勢中）。無錯誤。

⚠️ 這次改動還沒 commit/push（跟產業輪動是不同功能，Leo 只要求先推產業輪動）。

## 2026-08-26（續十）· 投資晨報拿掉不能點的HTML附檔

Leo：「這個bb9e會推一份html給我，但都不能點，直接停止寄送吧」——比對訊息文字
「2026-08-26 產業鏈看板（美股+台股，7 鏈）」直接定位到 `alert_telegram.py:141`
的 `send_doc(html_path, ...)`。

**根因**：用 Telegram 傳原始 HTML 檔（`send_doc`）只會顯示成可下載的檔案，
不會渲染成網頁，裡面的連結自然點不了——這不是bug，是這個做法本來就不會有
「可點」的效果。

**修法**：拿掉 `send_doc()` 那行，改成跟「無訊號」分支一樣附 `PAGES_URL`
（GitHub Pages正式看板連結）。順手發現「有訊號」分支原本完全沒有連結替代方案
（連結只在「無訊號」分支才有）——單純刪掉附檔會讓看板入口直接消失，一併補上。

觸發源：`.github/workflows/tw-board.yml`，`cron: '0 1 * * 2-6'`（週二~六 UTC 01:00
=台灣09:00）。已 commit+push，明天(工作日)09:00 那次排程就會是新行為。

## 2026-08-26（續十一）· 產業輪動：動畫加速+修市值不變bug+hover顯示數值

Leo 反饋兩點：「播的速度加快一倍」「圈圈大小隨著每週市值改變...不然還是看不出市值的變化」。

**播放速度**：`ANIM_MS` 600→300、`PAUSE_MS` 120→60（同倍率），單週補間+停頓時長減半。

**市值「看起來沒變」根因排查**（實際查 `industry_rotation_history.json`才找到，不是猜）：
美股 8/26 從 SPDR ETF 換成 TradingView 產業分類後，舊的 XLB/XLC/XLE...（SPDR）跟新的
Commercial Services/Communications...（TV分類）用不同日期交錯留在同一份歷史檔裡，從沒
清掉——`_frames_data()` 用「現在」的籃子清單去對每一幀，對到只有舊SPDR key的那些日期
時找不到新分類、只能沿用上一幀的位置，結果變成每兩格有一格是複製貼上，圈圈大小/位置
看起來卡住不動。

**修法**：`main()` 疊歷史前先清掉「跟現在籃子完全對不上」的舊列
（`set(snapshot.keys()) & set(現在籃子keys)` 空的就丟），重跑後美股清掉 26 筆舊列，
52週歷史全部變成20個籃子、size逐週真實變化（驗證：Commercial Services 從 5月的
$543B 連續漲到 8/26 的 $686B，不再有 None/跳格）。

**hover顯示數值**：之前只有排行榜看得到 RS-Ratio/RS-Momentum/資金規模數字，圖表本身
hover只顯示象限文字。補：`_frames_data()` 的 pt 補回 `size`（原本只算了 radius 沒留原始
數值）、前端 `tweenWeek()` 補間時一併 lerp `size`、tooltip callback 改成三行
（產業名+象限／RS-Ratio+RS-Momentum／資金規模，用既有 `fmtSize()` 格式化），
動畫播放中 hover 也會即時顯示當下補間幀的真實數值,不只有靜態畫面才有。

⚠️ 這次改動還沒 commit/push（等 Leo 看過畫面確認）。

## 2026-08-26（續十二）· 研究員Agent總體層落地：researcher_macro.py

接續上面投顧報告/advisor.db討論，開始動手寫研究員第一塊——總體層（固定時間跑，
不像產業/個股層有規則可觸發）。

**技術驗證**：`claude -p` CLI 有 `--json-schema` 參數（`claude --help` 查到，不是猜的），
比現有 `llm_board.ask_json()` 的「拜託只回JSON+正則剝圍籬+失敗重試」硬——直接在CLI層級
驗證 schema，回傳的 JSON 帶 `structured_output` 欄位，不用自己 parse。

**成本查證**：Leo 問「不能用免費的嗎」。實測發現：連完全不用 WebSearch 工具、純靠模型
知識回答的測試，`total_cost_usd` 都不是 0（$0.14）——代表這個數字是 CLI 每次都會印的
「等值API成本」參考值，不是「這次真的扣了多少錢」，跟 `llm_board.py`/`earnings_call.py`
已經穩定跑好幾個月、記憶裡明確寫「Max plan零API費用」是同一套 `claude -p
--dangerously-skip-permissions` 機制。這不是100%官方證實的結論，先跑幾天回頭查
Anthropic Console 用量核對（比照 [[gemini_cost_estimate_reconciliation]] 的做法）。

**實測3次真實跑**：帶WebSearch真的查7天內總經事件，cost分別是 $0.686/$0.743/$0.706，
平均約$0.71/次。內容品質不錯——有把「已公布」跟「預期」分開講、主動標注資料缺口
（GDP第二次估計實際數字沒查到就老實說沒查到不瞎編）、還自己抓到一個背景異動主動提醒
（新任Fed主席人事變動），符合「事實/推論/假設分開」的鐵律要求。

**排程**：原本以為要接09:00那個時間點，後來查 `BoardAnalyzeDaily` 這個 Windows 排程
任務實際設定，發現真正做本機LLM判讀的是07:00台灣時間（週一到五，DaysOfWeek=62
bitmask），09:00是GitHub Actions讀取已推送結果產生網頁的時間，兩個不是同一件事。
直接加進 `board_analyze_daily.cmd` 既有批次（valuation_alert.py之後），沿用同一套
git add/push/失敗通知機制，不用另開排程。

**踩坑**：console print 遇到 cost 那行的中文字元，cp950 codepage 炸掉
（UnicodeEncodeError）——已知坑，套用 `industry_rotation.py` 同一招
`sys.stdout.reconfigure(encoding="utf-8")`修掉。

輸出先存本機 `state/research_notes.jsonl`（不進 advisor.db，schema設計好了但還沒建表，
先跑幾天驗證品質/成本穩不穩再收斂）。這次改動還沒 commit/push（等Leo確認）。

## 2026-08-26（續十三）· 研究員總體層降成本：拆出零成本行事曆表

Leo 問「可以降到0嗎」（原本平均$0.71/次、每天跑）。分析後發現真正該花錢查的只有
「有沒有意外消息」，FOMC/CPI/非農/台灣央行「哪天開會」是官方早就公布好的排定行程，
不需要每天叫AI重查一次。

**新增 `macro_calendar.py`**：FOMC（federalreserve.gov 2026暫定時程）、CPI（BLS時程，
usinflationcalculator.com轉引）、非農（BLS Employment Situation，MA州政府文件轉引，
11-12月官方未公布用「每月第一個週五」規則推算並標記confidence=low）、台灣央行
（cbc.gov.tw 115年理監事會日期公告）四份行事曆，這次用WebSearch/WebFetch查證一次，
之後純查表零成本零AI呼叫，每季手動核對更新即可。

**`researcher_macro.py` 重構**：先查 `macro_calendar.needs_verification()`——今天前後1天
內有沒有排定事件。沒有 → 直接存一筆零成本筆記（列出未來25天已知行事曆，source=calendar，
cost_usd=0）。有 → 才叫 claude -p + WebSearch，而且把「已知事件清單」餵給它當context，
不用重新查「有哪些事件」，只查「這些事件的細節/結果/意外消息」——範圍變窄，實測單次成本
從$0.71降到$0.33（近腰斬），而且平均一週大概只有1-2天真的有事件觸發，其餘完全零成本。

預估：原本 $0.71×5天/週 = $3.55/週 → 改完大概 $0.33×1.5天/週 ≈ $0.5/週，降幅約85%。

已測試兩種分支都正常運作（零成本查表分支用真實今天日期跑；AI查證分支用模擬日期
2026-09-04單獨測函式驗證，測試產生的假資料已從 state/research_notes.jsonl 清掉，
沒有混進正式紀錄）。這次改動還沒 commit/push（等Leo確認）。

## 2026-08-26（續十四）· 研究員接個股層 + 總體層補免費新聞

**個股層落地**（新增 `researcher_stock.py`）：不重寫訊號偵測，直接讀既有兩支腳本
的翻面結果轉存成跟總體層同格式的 research_notes.jsonl：
- `valuation_alert.py`（貴俗價翻貴，本機07:00跑，同一批次，資料即時）——小改：多存一份
  `state/valuation_flips_today.json` 給研究員讀，不影響原本Telegram推播邏輯
- `st_alert.py`（SuperTrend翻面，透過`alert_telegram.py`在GitHub Actions台灣09:00跑）——
  發現這個**跑在跟本機07:00不同的執行環境**，本機讀到的會是「昨天09:00那次」結果，
  落後一天，是老實標注的已知限制，不是bug。小改`alert_telegram.py`多存
  `state/st_flips_today.json`，`tw-board.yml`補上這個新檔案的git add，不然只活在
  GH Actions那次執行裡本機讀不到。

零成本優先（比照新加的全域規則）：翻面本身是既有決定論訊號不用AI；額外查
`yfinance upgrades_downgrades`（免費非LLM）當佐證，confidence固定medium（已知
漏抓風險，不能單獨支撐判斷）。用假資料(AAPL/NVDA)測過兩個分支都正常運作，
測試資料已從正式紀錄清掉；`valuation_alert.py`本身也真的重跑一次驗證（66/77檔算出，
今天無新翻貴，正確回空清單）。

**總體層補免費新聞**：Leo問「總體層還有新聞？」——查證後發現不用另外花錢，
鉅亨網除了 market_fetch.py 已經在用的 tw_stock/wd_stock，還有 **tw_macro/wd_macro**
這兩個總經導向分類，一樣公開免key。加進 `macro_calendar.macro_headlines()`，
兩個分支（零成本查表/AI查證）都附上最新5+5條標題，researcher_macro.py 平常
零成本的日子現在也會帶真實新聞，不用等有排定事件才有東西看。

這是這次順手驗證「先查零成本三次再用付費」新規則的實例——一開始以為「一般新聞
掃描一定要花錢查」，查了才發現既有的鉅亨網API本來就有總經分類，不用另外接AI。

還沒commit/push（等Leo確認）。

## 2026-08-26（續十五）· 研究員產業層：偵測OK，但免費中文新聞源不夠格

新增 `researcher_industry.py`：偵測RRG象限翻轉（比對 industry_rotation_history.json
最新兩筆，零成本）→ 查相關新聞 → 叫AI統整成研究筆記（`--tools ""` 關掉所有工具，
只讓它讀餵進去的材料，實測 web_search_requests=0，不會偷跑去查）。

**成本問題已澄清**：Leo 去 Anthropic Console 查過 Billing 沒有對應費用，確認
`claude -p` 走 Max plan 訂閱額度、不是API計費扣款。CLI 顯示的 `total_cost_usd`
是等值標價參考數字，不是實際扣款。降成本重構還是留著（額度不是無限、架構也更乾淨），
但不用再為這個糾結。

**偵測層驗證成功**：首跑真的抓到7個籃子翻象限（美股6+台股1，60日週期），
象限比對邏輯正確。

**但新聞源這塊失敗，兩輪都不行**：
1. 第一輪用中文產業譯名搜——美股籃子命中率極低。「通訊」搜到的全是台股個股
   （大立光/佳凌），跟美股 Communication Services（Meta/Alphabet/Netflix）語意
   對不上；「非耐久消費品」這種書面譯名直接查無新聞。首跑7筆**全部** confidence=low，
   AI 正確判斷「這些新聞解釋不了」沒硬掰（這點是好的，鐵律有生效）。
2. 第二輪改用前3大成分股代號搜——**更糟**，短代號在中文新聞被當子字串亂命中：
   KO→「KOSPI」、HCA→「CASHCAT」、PM→「DeepMind」、ELV→「VELVET」。

**根因**：鉅亨網是中文台股導向媒體，美股新聞覆蓋本來就淺，加上中文搜尋引擎不會做
ticker 精確匹配。這是資料源本身的限制，不是 prompt 或程式寫法能救的。台股籃子
（非能源礦業那筆）理論上比較有機會，但樣本只有1個不足以下結論。

**現況**：程式碼寫好可用，偵測層可靠，卡在新聞源。測試產出的7筆已從
research_notes.jsonl 清掉，沒有污染正式紀錄。尚未接排程、尚未commit。
待 Leo 決定方向（換英文新聞源／只做台股／先不接新聞只存翻象限事實／其他）。

## 2026-08-26（續十六）· 產業層新聞源定案：美股走yfinance英文、台股走鉅亨網代號

接續續十五的卡關，Leo 問「美股要不要直接找英文？台股鉅亨網資料夠新嗎？」——兩個都查了：

**美股 → `yfinance` 的 `.news`**（免費、跟現有套件同源、不用另接API）。實測 UNH/KO/META
都回傳當天/前一天的新聞，來源是 Zacks/WSJ/Motley Fool/Trefis 這些真財經媒體，
ticker 精確不會亂命中。

**台股 → 鉅亨網，但改用4位數股票代號**。查證結果：鉅亨網**資料很新**（當天17:45的
新聞都抓得到）、**代號在中文媒體是精確錨點**（2330/1101/2412 都準）。前一輪失敗的
真正原因不是資料源不好，是關鍵字用了「非能源礦業」這種 TradingView 翻譯分類名——
台灣市場根本沒人這樣講，當然查無結果；改用代號後，原本查無新聞的非能源礦業籃子
用 1101(台泥)/2002(中鋼) 抓到真的相關的新聞。

**結果**：confidence 分布從「7筆全 low」改善到「5 low + 2 medium」。AI 的判斷很到位，
它明確指出剩餘問題：抓到的多是「股息投資通論」「listicle」這類常青內容，不是驅動
這週相對強弱的產業級新聞。

**這是誠實的結果不是失敗**：RRG象限翻轉是「相對於大盤的資金流變化」，本來就常常
沒有單一新聞可解釋，AI 拒絕硬掰、明確列出缺什麼資料，正是鐵律要的行為。硬要它每次
都生出一個理由才是真的危險。

**已知限制（不修，記錄下來）**：查的是「成分股個股新聞」不是「產業級新聞」，
prompt 裡已明確要求 AI 考慮這個落差、不要把單一檔的消息當成整個產業的原因。

**自己踩的坑**：改 `search_news()` 簽名（keyword→flip dict）後忘了改 `run()` 裡的
呼叫，跑起來才 TypeError。測試產出的7筆已從 research_notes.jsonl 清掉。
尚未接排程、尚未commit。

## 2026-08-26（續十七）· 研究員補完：總體突發事件觸發 + 持股每日監控 + 產業層接排程

Leo 兩個提問補完了研究員最後兩個洞：「新聞不是每天都要更新？」「總體經濟大事件怎麼觀測？」

**1. 總體層：沒排定事件的觀測（macro_calendar.py + researcher_macro.py）**
原本的洞：行事曆只涵蓋排定好的事件，關稅戰/戰爭/制裁這種沒排定的完全沒在看——
新聞有抓回來存進 research_notes.jsonl，但行事曆說「沒事」時 AI 根本沒被呼叫，
等於抓了等於沒抓。補兩個零成本觸發器：
- `market_anomaly()`：讀既有 market_data.json（market_fetch.py 每天已經在抓），
  主要指數單日±2% 或 10年期殖利率±15bp 就觸發。這是最誠實的過濾器——真正重大的
  事件市場一定會反應，缺點是落後（市場先動你才知道）。
- `keyword_hits()`：精簡的中文警示關鍵字清單（關稅/制裁/戰爭/違約/崩盤…）。
  刻意不寫長——清單再長也會漏掉沒想到的詞（2020年沒人會事先放「疫情」），
  真正的防線是市場異常偵測，這只是輔助。

**首跑立刻證明價值**：今天市場正常（無異常），但關鍵字抓到3則原本會被完全忽略的
新聞（關稅家庭支出研究、關稅合憲性訴訟、IMF談能源衝擊）。AI 查證後追出
「最高法院2/20已裁定IEEPA關稅違法、川普改用Section 122替代、效期至7/24須國會表決
延長」這種真正有投資意義的來龍去脈，還老實標注「國會是否已表決延長查不到，是缺口」。

**2. 持股每日監控（新增 researcher_holdings.py）**
原規劃是「每天掃持股所屬產業」，實作時發現持股跟RRG籃子對不起來（61檔持股只有8檔
落在籃子前5大裡），改成**直接掃持股本身的新聞**更直接——你在乎的是「我的股票出事了嗎」，
產業層變化 researcher_industry.py 每週那條線已經在看。

**踩到兩個坑，都修了**：
- 子字串誤命中：首跑 61 檔全部命中（100%誤報）。ALL(Allstate)命中"ban"是因為標題有
  "**Ban**k of China"、"sue"會命中"is**sue**"。改用 `\b` 詞界正則，並拿掉太泛的詞
  （stake/miss/warning/ban）。
- yfinance 張冠李戴：`.news` 常回傳「相關但不是這檔」的新聞（查API回傳XPeng、
  查ALL回傳Bank of China）。加 `_mentions()` 要求標題真的提到代號或公司短名。

修完後：61則誤命中 → **4則全部真的相關**（JPM裁員、住友收購離岸風電、Tesla 298萬輛
召回、UHS 8.35億併購Talkspace）。AI 產出品質很高：逐檔分「事實/推論/資訊不足」三段、
抓到「Tesla召回但股價反而漲」的反差並老實說標題判斷不出原因、還識破"game changer"
可能是公司自評帶宣傳性質。單次等值標價 $0.08。

**3. 產業層接排程 + 去重**
researcher_industry.py 需要本機 claude CLI（GitHub Actions 沒有），所以放本機排程。
但象限資料每週六才更新（weekly-screen.yml），本機週一到五每天跑會重複報同一批翻轉
五次——加 `state/industry_flips_done.json` 用 (最新快照日期|週期) 當識別碼去重，已驗證。

**三支都已接進 board_analyze_daily.cmd**（每天07:00週一到五）。
researcher_holdings.py 掃61檔約10分鐘（yfinance 速率限制），排在較快的步驟之後。

## 2026-08-26（續十八）· SuperTrend即時同步

Leo：「即時同步先做」——照之前討論的方案做了：

1. `tw-board.yml` 排程從台灣09:00提前到08:15（本機07:00那批查過近10天穩定在
   07:44~07:54結束，20分鐘緩衝夠安全），**Telegram推播完全沒動，還在雲端**，
   可靠度不變，只是提前45分鐘。
2. 新增 `C:\Users\Mophy\AI\researcher_stock_sync.cmd`（repo外，本機專用）：
   git pull拉08:15那次GH Actions的push → 重跑 researcher_stock.py → commit+push。
   註冊成Windows排程「ResearcherStockSync」，週一到五08:45（跟BoardAnalyzeDaily
   同樣DaysOfWeek=62 bitmask）。
3. `researcher_stock.py` 補防重複機制（`state/stock_notes_reported.json`，記今天
   已經報過的 ticker|source|event）——一天內現在會跑兩次（07:00本批+08:45同步），
   07:00已經處理過的（例如貴俗價，一天只有一份資料）不該在08:45又寫一次。

**踩到一個坑，測出來才發現**：`git add -f a b` 如果 b 不存在，**整個指令失敗、
a 也不會被加進去**（實測確認，exit 128）——`researcher_stock_sync.cmd` 原本一次
`git add` 兩個路徑，第一天 `stock_notes_reported.json` 還沒建立時會讓
`research_notes.jsonl` 的更新也一起靜默漏掉。改成分開兩次 `git add`，
第二個用 `if exist` 保護。這正是這個 codebase 之前踩過的「靜默失敗」坑
（8/1 reports/被gitignore那次），這次先測出來而不是等它真的漏跑兩天才發現。

效果：SuperTrend資料從「落後一天」縮短到「當天08:45前就有」。三個部分都已驗證
（tw-board.yml改的cron語法對；researcher_stock.py去重機制用假資料測過兩輪確認
不重複；researcher_stock_sync.cmd手動跑過一次，log正常，git add bug修好後
沒再重現）。已commit+push（repo部分），Windows排程本機直接生效不用push。

## 2026-08-26（續十九）· 投資長 Agent 上線

跟 Leo 討論定案的設計：**兩個獨立角度，各自給進出場判斷，不合併成一個結論**——
長期價值角度（洪瑞泰，看估值年為單位）+ 中短期趨勢角度（SuperTrend+RS+產業輪動整合，
這三個本質上是同一種順勢哲學，整合不算違反「不同哲學不能混」的硬規則）。

期間有個規則衝突要澄清：Leo 提「整合過的資訊，做出進場出場建議」，一開始聽起來像
之前被否決的「3鏡頭投票」，先攤開來問清楚才確認是「AI讀完給質化判斷」不是「機械
合併算分數」——沒有自己默默套用，先確認過一次。

**新增 `investment_chief.py`**：
1. 觸發範圍：掃今天新增的 research_notes，收集「今天有新事件」的持股代號
   （stock層直接取scope；industry層反查RRG籃子裡有沒有Leo的持股，僅前5大成分股
   資料可查，已知限制）——不是每天全部66+41檔重跑。
2. 材料來源全部讀原始檔案（Leo明確要求「直接讀原始檔」不解析Telegram文字）：
   `state/signals.json`（AI綜合訊號）、`state/valuation_state.json`+
   `trade_plan.buffett_targets()`（長期價值角度）、
   `trade_plan.supertrend_invalidation()`（趨勢角度，美股適用）、
   `industry_rotation_history.json`（所屬籃子最新象限）、今天的research_notes（敘事）。
3. AI只做「讀材料寫判斷」，`--tools ""` 關掉所有工具（跟researcher_industry.py同一套
   已驗證模式），輸出強制schema：兩個角度各自status/judgment/reasoning，趨勢角度
   額外5個強化維度（失效價位/支撐壓力/量價背離/事件風險/多空辯論）。

**首測驗證（JPM，真實資料）**：兩角度確實給出不同讀法——**價值角度judgment=觀望**
（抓到「品質關未過(失效)」，正確推論出這代表貴價$750.21這個目標價的可信度存疑，
沒有因為現價遠低於貴價就無腦說便宜）；**趨勢角度judgment=續抱/可買**（SuperTrend
多頭、RS未跌破、所屬金融籃子領先象限）。兩者沒有互相干擾、沒有被強迫湊成一致結論，
正是設計要的行為。5個強化維度裡沒資料的誠實寫「材料不足無法判斷」（例如成交量、
具體支撐價位），沒硬掰。單檔等值標價 $0.20。

**排程**：加進 `researcher_stock_sync.cmd`（08:45那個，本來就是「研究員資料到這時候
最齊」的時間點），研究員四支+SuperTrend同步+投資長，一次跑完。投資長失敗不擋
researcher_stock的push（用WARN log不用FAILED累加，因為推播/資料同步比投資長判斷
更關鍵，不該互相卡住）。

輸出寫進新檔案 `state/advisor_verdicts.jsonl`（跟research_notes分開存，判斷跟原始
研究資料是不同性質的東西）。尚未commit/push，等Leo確認。

## 2026-08-26（續二十）· 投資長判斷加 Telegram 推播

Leo問「投資長的結論會推給我嗎？」——原本只寫檔案沒推播，漏了。加 `_send_telegram()`，
另開一則獨立訊息（不跟08:15投資晨報合併，Leo明確選這個選項），08:45投資長跑完才推。
格式：每檔列出兩角度judgment+理由前80字，標題提醒「兩角度各自獨立，不合併結論」。
跟valuation_alert.py同模式讀本機.env（researcher_stock_sync.cmd本機排程不會預先
export環境變數）。已測過訊息格式（沒真的推播測試訊息），語法正常。

## 2026-08-26（續二十一）· 產業輪動台股成分股補中文名稱

Leo問「台股可以顯示中文名稱嗎」。之前檔頭有一段舊註解明確寫「沒有現成、覆蓋夠廣的
中文對照表，寧可顯示英文全名也不要瞎猜」——不是沒想過，是當時真的沒查到夠好的源。

**這次查到**：證交所(上市)`openapi.twse.com.tw`+櫃買中心(上櫃)`tpex.org.tw`官方開放
資料，免登入免key，合計約2000檔中文簡稱（實測1985檔），覆蓋率遠比
`board_html_legacy.TW_NAME`（手動維護的產業鏈追蹤清單）完整。加`_tw_chinese_names()`
一次執行內快取，優先用官方中文簡稱，查不到才退回TradingView的英文全名。

**踩到一個對方伺服器的坑**：櫃買中心那個API這次連續3次都SSL憑證驗證失敗
（"Missing Subject Key Identifier"，是他們憑證鏈本身的問題，不是我這邊程式碼的
bug），但幾分鐘前單獨測試時明明成功抓到過——研判是他們那邊間歇性的伺服器/憑證
問題。已有的fallback邏輯正確處理：查不到就退回英文名，不會讓整頁掛掉。實測結果
85檔裡70檔（82%）顯示中文，其餘是這次連不上的上櫃股，之後重跑排程很可能就正常了。
沒有為了修這個去停用SSL驗證（資安風險），照實跟Leo報告這個限制。

## 2026-08-27 · 俗貴價對齊MIKEON官方盈再表（重大修正）

Leo 親自跑官方盈再表 5 檔（BMY/PSX/DAL/TROW/VZ）對照我們的巴菲特看板，發現
**我們的俗/貴價系統性偏高 20%~112%，5 檔當天全部誤判成「可買」（官方全是觀望）**。

**逆推官方數字破解兩條公式**（全部用 Leo 截圖的官方數值驗證）：
1. 預期常利 = 近2年平均×0.7＋近5年中位數×0.3 —— 5/5 檔對官方「預期常利」欄
   精確吻合（BMY算出11,186 vs 官方11,186）
2. 淑價 = 貴價÷1.15^8（=3.059）—— 4/5 檔精確吻合。**×12 是二手簡化，官方淑價
   從來不是 EPS×12**，真實定義是「8年後漲到貴價、年化15%」的折現價（跟他部落格
   「房子8年漲3.05倍=15%」同一邏輯）
   查證來源：mikeon88.blogspot.com 講稿13/21 IRR、預期ROE的取法、房子貴淑價的計算

**根因兩個疊加**：EPS基礎（我們用近四季實績/forward，把PSX煉油暴利年當常態）+
淑價倍數（12 vs 真實9.81）。

**實作**（buffett_screener.py）：
- fetch_fundamentals 加 changli_eps：TTM+近4年年度，優先 yfinance `Normalized Income`
  （剔除一次性損益的自動近似，PSX幾乎全中；BMY這種大額減損多的還差10-15%，已知限制），
  沒有才退 Net Income；至少3期資料才算，不硬湊
- evaluate() 改：exp=常利EPS×30、cheap=exp/CHEAP_DISCOUNT(1.15^8)，台美股都優先常利，
  算不出退舊鏈（basis欄標記）
- buffett_html.py 說明文字同步改（衍生檔教訓）
- TROW的NAV×2地板規則未實作（逆推不完全確定），影響方向=我們略保守，安全側

**驗證**：5檔訊號 5/5 跟官方一致（全部觀望）；台股 2330/2412/8069 常利路徑正常。
⚠️ 明天07:00 valuation_alert 可能一次噴一批新翻貴警示（舊線沒過、新線已過），
一次性現象不是故障。

記憶 hongruitai_method.md 俗貴價段落已改版（7/18「已定案」那段被推翻一半，
如實記錄查證過程）。

## 2026-08-27（續）· 產業輪動動畫絲滑化：連續時間軸重寫

Leo 反饋「很絲滑的移動不會有一張一張的卡頓感…是不是要換繪圖工具？」

**不用換工具**——20顆泡泡對canvas是小菜，卡頓是舊動畫結構自己造成的，三個源頭：
1. 「一週一段補間、段間 setTimeout(PAUSE_MS=60ms) 停頓」——動300ms→停60ms的節奏
   本身就是逐格感
2. 每段用 ease-in-out——速度週期性脈動（呼吸感）加重逐格感
3. 每一幀整組換掉 datasets 陣列——Chart.js 每幀重新解析全部資料

**改法**：連續時間軸（總時長=週數×ANIM_MS，等速跨週插值，段間不停、不加每段easing）
+ 播放中泡泡座標原地改值（新增 `fastUpdateBubbles()`，只改 dataset[0] 的 x/y/r 數字
不重建陣列）+ 軌跡 datasets 只在跨週邊界重算（本來就一週才變一次）。
PAUSE_MS/_easeInOutQuad/tweenWeek/stepPlay 全移除。

**驗證的誠實限制**：沙盒瀏覽器 pane 的分頁是 document.hidden=true（pane沒真的顯示
在螢幕上），瀏覽器對隱藏分頁會暫停所有 rAF——**動畫類功能在這個環境從根本上無法
目視驗證**（舊版一樣會凍住，這是環境限制不是新bug）。已做的替代驗證：
- 整頁 JS 過 node --check 語法檢查
- fastUpdateBubbles 直接呼叫實測：座標正確改值(x+1.5/y-0.8如預期)、
  window._rrgCurPts 同步（tooltip讀這個）、無console錯誤
- 播放按鈕點擊後狀態機正常進入播放態（btn文字/日期標籤有切）
絲滑與否最終要 Leo 開真實瀏覽器看。

## 2026-08-27（續二）· 動畫第二輪：直線插值→Catmull-Rom樣條

Leo 反饋上一輪改完「速度變快了但還是有明顯一週一張的卡頓感」，要「像棒球拋物線的
絲滑移動」。這輪的根因不是速度/停頓（上輪已除），是**折線角**：泡泡在週點間走直線、
到每個週點瞬間轉向，每300ms一個拐角＝逐格感。

改法：interp() 直線插值換成 **Catmull-Rom 樣條**（interpSpline，動畫業界標準過點
平滑法）——取前後各一個週點(P0,P3)決定進出方向，曲線**精確通過每個真實週點**
（不扭曲資料，只把拐角變成連續轉向的弧線）。半徑/資金規模仍線性（視覺無感差異）。
頭尾越界重複端點（端點段退化成近直線，正常）。

驗證：node離線測樣條數學——u=0精確回P1、u=1回P2（浮點誤差1e-14）、中點帶弧度；
整頁JS過node --check。目視絲滑度仍受沙盒限制無法親驗（見上一則的說明），待Leo確認。

## 2026-08-27（續三）· 播放速度放慢 + 四象限視覺強化

Leo：「移動速度可以慢一點嗎？然後四個象限可以明顯一些」

- ANIM_MS 300→500（樣條版體感太快；跟舊逐段停頓版的300不是同一基準）
- 象限強化：底色透明度 5-6%→10-14%、十字分隔線加亮加粗(rgba(143,176,214,.55)/1.5px)、
  象限標籤 11px小灰字→14px粗體各象限自己的顏色。底色不再往上加的理由：泡泡填色
  也是半透明，象限底色太濃會跟泡泡疊出混色（8/25配色重整教訓）。

**驗證方法突破**：沙盒瀏覽器pane不能截圖（不compositing），但 canvas.toDataURL()
照樣能拿到完整渲染結果——起一個帶POST的本機接收server(port 8767)，讓頁面把
dataURL fetch過來存檔，再用Read看圖。實際確認：四色底/粗體彩色標籤/亮十字線
都達成；泡泡名字標籤正常（第一張抓到渲染中途暫態少了標籤，重畫後即正常，非bug）。
這招之後驗證canvas類頁面都能用。

## 2026-08-27（續四）· 四象限換色：領先藍、落後紅

Leo 指定「領先用藍色、落後紅色」——推翻 8/25「保留台股慣例紅=強」的配色決定
（用戶明示優先）。新配置：改善=綠#2fbf71（往領先移動=轉好）、領先=藍#3987e5、
落後=紅#e5484d、弱化=琥珀#eda100（維持警示感）。QUADRANT_COLOR（餵給排行榜
象限字/多週期圓點的 QCOLOR）跟 quadrantBgPlugin 的底色/標籤同步改，兩邊同一套。
canvas.toDataURL 抽圖驗證過四象限顏色正確。

## 2026-08-27（續五）· 投資長推播格式改版（首日真實推播後的反饋輪）

今早08:45第一次真實排程推播（正好碰上新貴價線的一次性8檔集中翻貴），Leo 四點反饋：
①要有總經/產業整體狀況 ②個股結構不一致很混亂 ③先中短期再長期 ④更好的結構呈現。

**根因**：舊推播把 AI 的長篇 reasoning 硬截80字——句子斷在半空、有的檔有【事實】標籤
有的沒有，結構全看 AI 當次心情。

**改版**：
1. SCHEMA 每個角度加 `brief`（≤60字、強制完整句、專供推播）；reasoning 完整版照存
   jsonl 供深讀。趨勢角度移到 value 前面（schema+prompt+推播全部一致）。
2. 推播開頭加總經/產業總覽——Python 端從當天 research_notes 確定性組出（零AI呼叫）：
   總經=觸發原因或近期排定事件前3項；產業=當天翻象限清單（真實資料驗證：今天就有
   通訊 領先→改善、非能源礦業 弱化→領先）。
3. 每檔統一兩行：「趨勢 {icon}｜brief」「價值 {icon}｜brief」；兩角度同喊出場的
   排最前+標記‼️。
4. 超過3800字自動切多則（Telegram單則上限4096，原本會被硬砍）。

驗證：NVDA 真跑一次新 schema（brief品質正確：「產業籃子轉入落後象限且RS跌破60日
均線，趨勢材料明確建議剩餘部位全數出場。」）；完整版面用 mock+真實總覽渲染確認。
明天08:45排程自動生效。

## 2026-08-27（續六）· P0：投資長擴大到非持股（進場側）

Leo 分享老墨戰情室 Discord bot 截圖當升級藍本（拆解已寫進記憶
mofi_warroom_structure.md，含P0-P4路線），並指出「投資長不應該只看我手上持有的
股票」。P0 實作：

1. today_tickers() 回傳 {ticker: {held, triggers}}，觸發源擴充：
   - industry層拿掉 `if tk in holdings` 濾網：持股全收；**非持股只收轉入改善/領先
     的籃子成分股**（轉弱籃子對非持股沒行動意義，全收會噴雜訊）
   - 新增巴菲特到俗價（buffett_watch.json 41檔現價≤cheap且未持有），5天冷卻
     （state/chief_buy_cooldown.json）——這是「狀態」不是「事件」，不冷卻會天天重複
2. prompt 加持有狀態語意（非持股：續抱/可買=可考慮進場、考慮出場=避開）
3. 推播分兩區：💼持股（出場管理）/🆕進場機會（非持股）
4. 非持股估值材料退查 buffett_watch.json（valuation_state只涵蓋持股）

**首測抓到兩個bug都修了**：MU/LITE明明持有被標非持股（holdings.json 61檔 vs
trade_plan.load_holdings 66檔兩來源不一致→聯集）；台股帶後綴代號(5287.TWO)被
市場regex誤判成美股。

**真實跑24檔驗證（持股8+非持股16），設計價值立刻顯現**：BMY雙綠（唯一兩角度同意
進場）；DAL到俗價但趨勢雙紅→擋住價值陷阱；TROW價值綠趨勢紅→先不進；VZ雙綠且
趨勢brief自動引用當天「通訊產業轉強」。已推Telegram（1則）。

已知縮水因素：台股候選11檔趨勢全⚪（supertrend_invalidation僅美股，待擴充）；
今天16檔到俗價是舊×12線灌的，週六buffett_watch換常利新線後會大幅縮減。

## 2026-08-27（續七）· P1a財報快訊上線 + 盤點財報更新缺口 + Actions排程被跳過

**盤點（Leo問「發了預告但還沒更新的有哪些」）**：25檔發過預告的有24檔公布後沒更新
（19檔持股連卡片都沒產過、NVDA/2881/2891卡片停在財報前、只有AMD是8/13手動補的）。
根因：8/3 Leo指示「財報不用每天跑、每季一次」→ 加了季度閘門（本季8/19跑過就鎖），
副作用是「剛公布T+3」分支幾乎永遠等不到執行——8月初那批離8/19太遠、NVDA公布在
8/19之後，全漏。Leo 8/27表示預期是「提醒完之後就會做了」→ 依新預期修正。

**修法（兩邊都保留）**：季度重掃照舊（預告的份量感，8/3本意）；新增 `daily_followup()`
每日輕量跟催——**只檢查 state.upcoming 已發過預告、財報日在近AFTER_DAYS(3→7)天內、
還沒處理的那幾檔**（平常0~3檔），不是天天重掃全清單。確認 Reported EPS 真的出現
才處理（yfinance還沒更新就「明天再試」不硬寫）。

**推播升級成老墨四段式**：①實際vs預期（earnings_dates免費）②下季共識
（earnings_estimate免費）③指引vs共識+④盤後反應/供應鏈連動（_ai_flash：一次
AI+WebSearch呼叫，researcher_macro同款模式）+卡片自動重產。

**NVDA實跑驗證**：③抓到Q3指引$108B高於共識3.4%且未計中國收入（跟老墨數據一致），
還多抓到Q4毛利率下修71-72%；④「過去11季10次beat但近4季財報後都收黑」行為解讀+
老實說查不到8/27收盤價；💡供應鏈給到具體標的。已推Telegram、卡片已重產。
台股金控（2881/2891）yfinance沒有Reported EPS會一直「明天再試」到窗口過期，
已知限制（台股財報資料源本來就弱）。

**另一個生產事故（Leo發現首頁新聞停在8/26）**：tw-board.yml 改到 00:15 UTC 後
第一天排程**整個沒被GitHub觸發**（超過6小時非延遲）——GitHub Actions schedule
盡力而為、高負載時段會丟棄，整點/一刻鐘熱門分鐘容易中獎。連鎖：今天晨報沒發、
首頁新聞停昨天、08:45本機sync拉到舊資料。已手動補跑（workflow_dispatch）+
cron挪到冷門分鐘 00:19。

## 2026-08-27（續八）· 台股趨勢資料放開 + 補卡片 + 巴菲特提前重跑 + Discord提前

**台股趨勢「資料不足」真相**：Leo抓到矛盾「台股怎麼會跑不到資料，那財報卡怎麼跑
出來的」——查證後確認是**人為限制不是資料限制**：supertrend()/mansfield_rs_series()
對任何yfinance代號都能算（財報卡技術面就是同一套在跑台股），只是
supertrend_invalidation寫死^GSPC基準+investment_chief有一行`if not is_tw`跳過。
修法：台股RS基準自動切^TWII（跟錯大盤RS就沒意義）、純數字代號補.TW後綴、
investment_chief閘門拿掉。實測2330.TW/5287.TWO/2412都算出真實資料。

**其他**：miss 5檔（AGO/PAA/CPA/AES/HSBC）卡片背景補產中；weekly-screen手動觸發
（buffett_watch提前換常利新線，不等週六）；**Leo決定Discord提前到P1/P2/P3之前**
（「現在資料量有點多telegram很難讀」）。

## 2026-08-27（續九）· 晨報AI訊號只推轉買/轉賣

Leo看到晨報一排「⚪️買進解除→觀望」（MSFT/NVDA/AVGO同天解除）：「可以顯示
轉買進、賣出的就好」。alert_telegram.py拿掉「解除→觀望」分支——降級不推，
只推轉成🟢買進/🔴賣出的；訊號現況看板頁本來就有。連帶st_flips_today.json的
ai_alerts也不會再收到解除事件（researcher_stock跟著少一種雜訊，一致）。

## 2026-08-27（續十四）· buy_digest中文名+櫃買SSL自癒+投資長總覽加大盤/重大新聞

1. buy_digest 台股加中文名（Leo反饋）：重用 industry_rotation._tw_chinese_names()。
2. 櫃買中心 SSL 問題定性更新：**不是間歇是 Python 3.13 對缺SKI憑證的嚴格驗證**
   （requests連續10次失敗；curl一次就過=驗證器寬嚴不同，不是繞過驗證）。修法三層：
   requests重試3次→curl備援→磁碟快取 state/tw_names_cache.json（簡稱幾乎不變，
   舊快取堪用）。絕不用 verify=False。實測1985檔全通（含上櫃新麥/力泰）。
3. 投資長推播總覽加兩行（Leo指定）：📈大盤=market_data.json七大指數漲跌
   （S&P/納指/道瓊/費半/台股/美元台幣/美10年債）；📰重大新聞=警示關鍵字
   （打仗/升息/CPI類）命中的頭條才列，非全部頭條。零成本零AI呼叫。
4. 晨報訊號減噪（見上則）；miss 5檔卡片已補產推送（27份）。

## 2026-08-27（續二）· Discord「隆中對」Phase 1 雙發上線

伺服器=隆中對（Leo建），頻道：#每日戰情/#財報（webhook）+#軍議（文字頻道，
留給P4b互動bot，不走webhook）。**三國軍師團命名定案**：龐統=研究員（情報/獻策）、
孔明=投資長、仲達=風險官、陳壽=復盤官。

技術：`notify_discord.py`——webhook 單向推播，**per-message username** 讓各agent
在同一頻道用自己名字發言（不用一agent一頻道）；>2000字自動切多則（切換行處）；
`tg_html_to_md()` 轉換Telegram HTML；失敗吞錯不擋主流程（雙發過渡原則）。

六個發送點接好：alert_telegram(晨報→龐統/daily)、investment_chief(判斷→孔明/daily)、
valuation_alert(翻貴警示→仲達/daily)、buy_digest(週六買進機會→龐統/daily)、
earnings_watch push()一點接全通(預告+快訊→龐統/earnings)。

部署：本機走.env；**GitHub Actions 走 secrets**（DISCORD_WH_DAILY/EARNINGS 已用
gh secret set 設好，tw-board/weekly-screen 對應步驟 env 已加）。端到端已驗證
（孔明真實格式訊息發抵#每日戰情）。Telegram照舊雙發，Leo用幾天再決定TG留什麼。

## 2026-08-27（續三）· Phase 2 統一組報器 daily_warroom.py 上線

分工定案：**Telegram 維持逐則即時推播；Discord #每日戰情 收「一則五段式合成日報」**
（好讀版）——呼應 Leo「telegram 很難讀」的原始動機。

新增 `daily_warroom.py`：純讀檔+格式化（零AI呼叫零成本），五段結構借老墨骨架：
①大盤/總經（指數美台兩行+排定事件+警示關鍵字新聞）②今日訊號異動（SuperTrend/
AI轉買賣/翻貴，有變才列；st_flips_today.json 加date欄位供資料新鮮度判斷）
③投資長判斷（讀advisor_verdicts.jsonl當天筆，同檔多筆留最後）④研究員筆記
（產業翻象限/持股事件新聞一行一條）⑤近日要看（總經行事曆+未來7天財報）。
發Discord時署名孔明，超長自動切多則（notify_discord處理）。

**同時移除三個個別Discord雙發**（alert_telegram/valuation_alert/investment_chief）
——內容經state檔案進合成日報，個別再發會同內容出現兩次。earnings頻道（龐統）跟
週六buy_digest（跑在組報之後）維持個別發。排程：researcher_stock_sync.cmd 最後
一步（08:45後，一天資料最齊的時點）。

實測今天真實資料組報成功並發抵Discord：五段全populated（②8檔翻貴③24檔判斷
④2個產業翻象限⑤非農行事曆）。

## 2026-08-27（續四）· 日報拆公開/私人版＋台股中文名＋進場分段

Leo 三個反饋一次改：
1. **台股中文名**：daily_warroom/investment_chief TG推播的代號都補官方簡稱
   （新麥/大山/長榮航…），重用 _tw_chinese_names()。
2. **進場機會分段**：🇺🇸美股/🇹🇼台股分小節，各檔之間留空行（原本擠成一坨）。
3. **持股私人化**（Leo要開放#每日戰情/#財報給家人看）：
   - 日報拆兩份：**公開版**（大盤/守備清單訊號/進場機會/產業筆記/近日要看——
     零持股資訊）發#每日戰情；**持股密報**（持股訊號+翻貴+持股判斷+持股新聞）
     發私人頻道（DISCORD_WH_PRIVATE，未設定則跳過並印提示，Telegram照舊全都有）
   - #財報 Discord 版拿掉 ⭐/👦 持股標記與註腳（星號會反推持倉；TG版保留）
   - 進場機會（非持股）留在公開版——那是候選池不是持倉，公開無妨

另：台股趨勢「查無SuperTrend」是早上修復前的舊快取，明天新判斷生效（已跟Leo說明）。

## 2026-08-27（續十五）· P1 失效條件登錄+日檢上線（老墨核心概念）

**設計**：投資長每次給判斷時，schema 強制多產出 `conditions`（1-3條可證偽失效條件）：
- price_below/price_above：價格失效線，**必給數字**——thesis_check.py 每天對照收盤價，
  零成本三態輸出：🚫觸發（跨線，標記後不重複報）/⚠️逼近（≤2%）/✅健康
- metric：財報門檻（「毛利率跌破70%」），日檢只列「待財報檢」——**誠實標待檢，
  不假裝檢查過**；v1.1 要接 earnings_watch 的 AI 快訊在該檔財報時查證（還沒接）

登錄 state/thesis_conditions.json（同檔新判斷=新論點=覆蓋+狀態重置）；
日檢結果 state/thesis_check_today.json → 持股密報新增「⑤失效條件日檢」段。
排程：researcher_stock_sync.cmd 在 investment_chief 之後、daily_warroom 之前。

**實測（NVDA真跑）**：AI 給出兩條結構化價格線（跌破貴價148.32=翻貴訊號解除、
跌破俗價48.49=轉極端便宜），登錄+日檢+密報段落全通。
**踩坑**：yf.download 單一檔+group_by=ticker 時欄位仍可能多層，首測健康數變0——
改先試 data[sym] 再退平面格式。

**當天同場修的生產事故**（Leo連報三個）：①首頁停8/26=手動補跑的push被我同時段
commit撞掉且`|| echo`靜默吞掉；②巴菲特頁還是舊線=weekly-screen新線同樣被撞掉丟失
——三個workflow的push全改rebase重試3次+失敗推TG（portfolio-nav原本就會叫）。
③GitHub排程連兩天整批沒觸發（00:15/07:10都沒跑）——已手動補跑並確認資料上線
（首頁行情2026-08-27 16:44），排程可靠性問題持續觀察，若再犯考慮本機看門狗。
④財報分析27檔=歷史產卡存檔非選股清單（跟Leo說明）。

## 2026-08-27（續十六）· GitHub排程看門狗（明天保證晨報會到）

GitHub schedule 連兩天整批沒觸發（00:15/00:19/07:10全沒跑），不能再賭第三天。
新增 `actions_watchdog.py`：檢查「今天(UTC日)該workflow有沒有任何非failure執行」
（schedule或dispatch都算，避免重複觸發），沒有就 gh workflow dispatch 補，
--wait 模式會等完成（最多12分鐘）。兩個守望點：
1. researcher_stock_sync.cmd（08:45）開頭：`actions_watchdog.py tw-board.yml --wait`
   ——它下游要拉 st_flips_today.json，必須等補跑完成才 git pull
2. 新Windows排程「MarketHomeWatchdog」（15:40週一~五）：market-home.yml 不等待版
   ——首頁台股收盤行情的保險

實測兩個workflow都正確回報「今天已執行過，不用補」（今天手動補跑過）。
另：財報分析頁=歷史產卡檔案櫃的機制原因已跟Leo說明（index只掃檔案不篩選+卡片
只增不減+三個時代來源混疊），改善方案（來源分頁籤）記為待辦不急。

## 2026-08-27（續十七）· 修.TW誤導連結——台股代號被Telegram當網域

Leo反饋「1580.TWO連結有問題，其它有連結」——查出**我們從沒放過連結**：.TW是真實
頂級網域（台灣國碼），Telegram把「4763.TW」誤判成網址自動變連結（點了開
http://4763.tw 不存在/被搶註的站，有誤導風險）；.TWO非網域所以1580.TWO沒變連結，
反而是「正常」的那個。修法：buy_digest全部給真連結（台股→tw.stock.yahoo.com/quote、
美股→finance.yahoo.com/quote，<a>標籤蓋掉自動偵測）；investment_chief的TG推播
顯示改「1580 新麥」去後綴（有中文名時）。財報16檔=檔案櫃非選股再說明一次
（index掃檔不篩選+三時代來源+只增不減）。

## 2026-08-27（續十八）· 巴菲特掃描429全滅診斷+修復；刪5檔補產卡片

**weekly-screen 重跑結果診斷**：全部22檔都換新線（貴/俗比精確3.059✓），但清單
41→22檔，PSX/TROW/DAL整個掉出。追log發現 **SKIP=fetch_fundamentals回None=
抓資料失敗，不是品質淘汰**——236檔候選201檔SKIP（85%），每檔1.6秒快速失敗=
被Yahoo 429限流。嫌疑：常利計算新增的 quarterly_income_stmt 呼叫（每檔+1）把
Actions IP 推過限流線。**教訓（同cache_negative_result_bug家族）：抓不到資料
被當成篩選結果，限流變成「這些股票不合格」的假象。**

**修法三件**：
1. TTM淨利維持 quarterly Normalized 為主（試過改 eps_ttm×股數零呼叫版——PSX 俗價
   偏高16%，GAAP把煉油暴利算回來，正是常利要排除的，**不犧牲準確度**）；
   eps_ttm×股數降為備援
2. stage2 間隔 0.8→1.2 秒
3. stage2 加失敗重試一輪（冷卻60秒後補撿，2秒間隔）——429是暫時的，不重試
   等於把限流當篩選
已再次觸發 weekly-screen 驗證。

**刪5檔補產卡片**（Leo：「不是我想要的」）：AGO/PAA/CPA/AES/HSBC 卡片刪除、
索引重建（27→22份）。教訓：補歷史欠帳前該先問要不要，miss資訊當時用一句摘要
回報就夠，產整份卡片是過度服務。

## 2026-08-27（續十九）· 巴菲特掃描完整性防線（寧舊勿殘）

Leo：「限流的話有其它來源嗎？不然跑出來是錯的比沒跑出來嚴重」——原則完全對，
殘缺清單比舊清單危險（下游到俗價觸發/buy_digest/投資長都假設清單完整，缺PSX
=「PSX不存在」）。

**完整性防線（buffett_scan.py）**：任一市場評估成功率<70% → 整批不寫檔、
沿用上一版 buffett_watch.json + 推TG警告「本週清單未更新（寧舊勿殘）」。
刻意 exit 0——防線正常運作不是程式錯誤，不讓workflow紅燈。

**替代來源盤點**（誠實回報）：美股 SEC EDGAR（盈再率已在用，損益表也拿得到，
限流寬鬆）+台股 FinMind（已在用）都是現成候選——但 EDGAR 只有 GAAP 沒有
Normalized（剔除一次性損益要自己調整，工程大），列為後續工程不是今天能上。
今天的組合：重試一輪+完整性防線，已涵蓋「不會產出殘缺清單」這個底線。

## 2026-08-27（續二十）· 巴菲特掃描診斷翻案——空殼dict才是真兇

**修正我自己的錯誤診斷**（續十八寫的「SKIP=fetch失敗」機制錯了）：log裡的SKIP
其實是 evaluate() 的訊號名稱（品質關沒過），跟抓取失敗無關。真兇更隱蔽：
`fetch_fundamentals` 被限流時**不回None、回空殼dict**（price/roe全None）→空殼進
evaluate→品質關全掛→signal=SKIP→股票靜默消失。所以：
- 第一版retry（判斷 `if data:`）**永遠不會觸發**——run#3用了帶retry的15a33fe
  卻完全沒跑retry，追checkout SHA/時間戳/本機模擬才發現這層
- 完整性防線第一版**也看不到問題**——空殼算「評估成功」，成功率永遠~100%

**真修法**：stage2 加 `_hollow()`（連price都沒有=空殼，正常股票再爛都有價格），
空殼走FETCH_FAIL+重試路徑（跟品質淘汰的SKIP在log裡區分開）；重試同判準。
完整性防線現在數的是非空殼數，終於有效。本機模擬驗證：FAKEAAA→FETCH_FAIL→
重試→仍失敗→排除，全鏈正確。

**教訓（verification_self_check_limits家族）**：修完第一版時只驗了「程式碼有進
commit、checkout有拿到」，沒驗「行為真的改變」——run#3的log零retry輸出就是行為
沒變的鐵證，早一步對照就不用三輪。

## 2026-08-27（續二十一）· 產業轉強觸發從沒真的執行過（靜默失效）

Leo 問「七鏈以外的產業轉強會不會漏掉」→ 查證時發現更嚴重的事：**「產業轉強」
這個觸發源從 P0 上線到現在一次都沒真的觸發過**。
根因：researcher_industry 存的 scope 是**中文**產業名（「非能源礦業」），
RRG_HOLDINGS 的 key 是**英文** sector（Non-Energy Minerals），basket_matches_scope
比對永遠 False。整個路徑在跑、log 正常、沒有任何錯誤——典型的
[[unexecuted_code_paths]]（程式在跑≠那條路徑執行過）。

修法：basket_matches_scope 用 industry_rotation.SECTOR_TW_LABEL 做中英對照。
驗證（今天兩筆真實翻象限）：通訊→SPCX/VZ/TMUS/T/AMX/2412/4904/3045/6561/6170；
非能源礦業→BHP/SCCO/RIO/NEM/FCX/2002/1101/2027/1102。修好後明天起真的會觸發。

**教訓**：P0 當時「實測24檔」看起來全綠，但那24檔全是巴菲特到俗價觸發的——
產業轉強那條路徑一檔都沒有，我沒察覺「某個觸發源零產出」本身就是要查的訊號。

## 2026-08-27（續二十二）· 七鏈技術面上線 + Discord說明精簡

**chain_technicals.py（Leo拍板「當參考不當門檻」）**：對七鏈自己算 RRG+SuperTrend+
Exceed Charge，解決「守備清單用七鏈、產業輪動用TV20分類」接不起來的分類問題。
定位：七鏈看長期趨勢健不健康，個股訊號看買點到了沒。輸出進投資長材料當背景，
**不擋任何訊號**（維持多鏡頭獨立硬規則）。

**兩個踩坑都靠實測抓到**：
1. 上櫃股後綴：3324/3081 是 .TWO，只試 .TW → 兩條鏈整個掛掉。加 .TW→.TWO fallback
   （memory hongruitai_rules_coverage 早記過這坑，我又踩一次）→ 16/16 全通
2. **擠壓偵測全壞**：第一版拿收盤價當高低價，ATR 大幅偏小→KC通道太窄→BB幾乎不可能
   被包住→**16條鏈全部「未擠壓」**。原本註解還寫「訊號略敏感」——方向完全寫反。
   修法：用成員「當日high/close、low/close平均比例」合成籃子高低價。
   **對照證據**：AI伺服器鏈一年內擠壓天數 舊版0天 → 新版80天（平均日內振幅3.64%）。
   教訓：一致到不自然的結果（全部同一個值）本身就是 bug 訊號。

**TOP_HOLDINGS_N 5→10**（Leo：七鏈以外的產業訊號變好想看前10）。

**Discord**：說明訊息精簡（拿掉名詞速查、加投資站免登入網址與分頁清單）；
notify_discord 加 return_ids/edit/delete——`?wait=true` 才拿得到訊息 id，
不加的話事後無法編輯刪除（想改說明才發現）。id 存 state/discord_pinned.json。

## 2026-08-27（續二十三）· #財報改發卡片摘要

Leo：「摘要網站裡的財報分析就可以了，然後附上連結」。新增 `card_digest()`——
純解析已產生的懶人包HTML（**不重跑yfinance、不再叫一次AI**：卡片裡的敘事本來就是
AI寫的、數字本來就是真實財報，這裡只換個地方呈現+給連結）。抽：公司/季別、
營收/淨利/自由現金流+YoY、分析師共識、一句話結論、利多2條、風險2條、連結。
Discord #財報 發這份；Telegram 維持原本簡短快訊（即時通知不需長摘要）。
實測 NVDA 摘要正確（含「現價209.66已超過貴價193.2」這種洪瑞泰判定）。

## 2026-08-27（續二十四）· 巴菲特掃描搬回本機（EDGAR方向已否決）

Leo 同意開工 EDGAR 備援後問「官方有說明是用什麼資料嗎？」——**這個問題救了我們一次**。
查證結果 EDGAR 是錯方向：
1. **官方盈再表用的就是 Yahoo Finance**（stocks.ddns.net 明列資料來源：各證券商、
   MoneyDJ、Yahoo Finance、Morningstar、TradingView、Stockanalysis）。換 EDGAR 會讓
   常利/俗貴價**重新偏離官方**——EDGAR 只有 as-filed GAAP、沒有 Normalized Income，
   而剔除一次性損益正是常利的核心。等於自毀今天才對齊好的東西。
2. **真兇是 IP 不是資料源**：實測同一批在 Actions 全滅的 10 檔
   （PSX/TROW/DAL/GDDY/ZTS/AMP/HON/MPC/ALL/PGR）→ **本機 10/10 全成功**，
   平均 4.6 秒/檔。Yahoo 擋的是 Actions 雲端 IP 段。

**修法**：掃描搬回本機——新增 `buffett_scan_weekly.cmd` + Windows 排程
「BuffettScanWeekly」（週六 08:30，236檔約20分鐘），跑完產頁+push；
weekly-screen.yml 那步移除（註解寫清楚為什麼，含「舊標題說Actions不限流」的時空背景）。
代價：電腦沒開機那週不更新——但完整性防線會沿用上週清單+推TG，比殘缺上線安全。

**教訓**：Leo 問「官方用什麼資料」＝ [[feedback_verify_before_hardcoding]] 的實踐，
我當時已經要動工 EDGAR 了。動大工程前先確認「我們要對齊的那個東西自己用什麼」。

## 2026-08-27（續二十五）· 日報拆兩則（龐統情報／孔明判斷）

Leo：「研究員不會發日報對嗎？」→ 確認分工原本只有孔明發日報、龐統只在#財報發。
拆成兩則讓分工看得見：**龐統**發①大盤/總經②訊號異動④研究員筆記⑤近日要看，
**孔明**發③判斷（標題加「・判斷」）。持股密報同樣拆。
理由不只是分工——各則長度減半、之後判斷變多也不會撞Discord 2000字上限被切多則。
compose() 加 part 參數（research/chief/all，all 保留給預覽與日後改回）；
加 `_empty()` 判斷佔位語（「今日無新訊號」這類），**整則沒實質內容就不發**，
避免空訊息洗版。dry-run 驗證通過。

## 2026-08-27（續二十六）· 配息率缺值回退前次（Leo拍板）

**問題**：yfinance 的 payoutRatio 時有時無——實測同一檔 PSX 兩次呼叫，一次回 None、
一次回 0.282。缺值時舊邏輯「放行」＝矇混過關進清單，**同一檔股票因此在清單裡飄進飄出**
（不是它變好變壞，是資料時有時無）。

**Leo 拍板**：「漏資料就用上次的資料（但說明是上次的）」。實作：
- `state/payout_cache.json` 存每檔最後一次真的抓到的配息率（**只存抓到的值，
  失敗不寫快取**——cache_negative_result_bug 那條教訓）
- 缺值→回退快取值＋標 `payout_stale=True`
- buffett_scan 帶出這個旗標；巴菲特頁配息率後面標 `*` ＋滑鼠提示，註腳說明為什麼
- 實測：模擬 Yahoo 漏欄位 → 回退 0.282、stale=True、判定與有資料時一致

**這是改到選股判定的邏輯**（有些股票會因此從清單消失，例如 PSX 配息率28%不再靠
漏資料混進來），所以先問過 Leo 才動，沒有自己決定。

**同場的自我檢討**：為了查 PSX 而跑 `buffett_scan.py --markets us`，那會**用美股結果
覆蓋整份 buffett_watch.json**——把早上跑出來的 33 檔洗成 5 檔，git 還原也只回到上次
commit 的 22 檔（33檔那版沒 commit，救不回）。已重跑補回。教訓：診斷用的執行不該
寫進正式輸出檔，該用 --dry-run 或另存路徑。

## 2026-08-27（續二十七）· 手機版面重排＋七鏈每日模式＋進場機會篩選

Leo 三個反饋一起處理：

**① 進場機會太多**：非持股「兩個角度都是🟡觀望/⚪資料不足」＝沒有可行動資訊 → 不列出
（留🟢可進場與🔴便宜但別碰＝價值陷阱警示，那是有用的反向資訊）；持股不套此篩選。
段落標題註明「另有N檔觀望/資料不足未列」。

**② 手機排版難讀**：每檔原本 6-8 行（brief 60字在手機折3行）。
- 燈號 emoji 移到**行首**（掃視一眼看到紅綠，不用讀到句中）
- 拿掉全形空白縮排（手機上效果微弱、反而讓折行更亂）
- 觸發原因改用 Discord 小字體 `-#` 另起一行
- brief schema 60→38字，prompt 明說「手機一行約20字，寧可只講最關鍵那一個理由」
每檔壓到 3-4 行。⚠️ brief 縮短要下次投資長跑才生效（舊紀錄仍是長版）。

**③ 七鏈每日模式**（Leo：「每日只算SuperTrend+擠壓、RRG沿用週六」）：加 `--daily`，
沿用上次 rrg 值並記 rrg_from。**誠實修正我先前估錯的一句**：這樣「省不了一半時間」，
12分鐘幾乎都在抓120檔歷史價、SuperTrend/擠壓要用同一批資料，RRG 沿用只省2次大盤抓取。
真正的省時是新加的 `prefetch()` 批次抓取（yf.download 一次拉完，取代120次逐檔連線）。
RRG 沿用的真正好處是**跟週六產業輪動頁數字一致**，不會頁面說領先、日報說改善。

## 2026-08-27（續二十八）· 模擬倉影響評估＋策略賽馬頁加改版註記

Leo 問「剛做的更新會影響模擬倉嗎？」「產業鏈不會嗎？」——查 git 歷史逐日比對持股數：

| 日期 | 巴菲特價值 | 產業鏈全 | 產業鏈+趨勢 |
|---|---|---|---|
| 8/22~8/26 | **21 檔** | 100 | 63 |
| 8/27 | **3 檔** | 100 | 67 |

**巴菲特倉受影響**（21→3）：三個改動疊加——俗價定義改版（EPS×12→常利EPS×9.81，
低約18%）、候選清單縮水（41→33）、配息率快取補漏。進場規則是「現價≤俗價」，
俗價一降符合的自然剩很少。**這是規則改對之後的正常結果**，原本21檔有不少是靠
寬鬆舊線買進的。
**產業鏈兩倉不受影響**：讀 screen_result.json（守備清單三因子），跟今天改的完全沒交集；
63→67 是早上守備清單例行重篩造成的。

Leo 選 C（保留曲線+標注）：策略賽馬頁的巴菲特倉說明加醒目改版註記——寫清楚改了什麼、
新線低18%、當天21→3檔、**跨8/27的報酬率等於跨了兩套規則**、產業鏈兩倉不受影響。

## 2026-08-27　P2 大盤體溫計（電金比）

**破解公式**：老墨戰情室的「電金比」＝ 證交所 **電子工業類指數 ÷ 金融保險類指數**
（不是 ETF 價格比）。8/27 實測 2916.26 ÷ 3223.07 = 0.9048，對照他同期公布的 0.9054 吻合。
資料源 `twse.com.tw/rwd/zh/afterTrading/MI_INDEX?type=IND`，免登入免 key。

**資料源兩個坑（實測）**
1. 只提供近 2.5 個月 —— 回補 200 天只拿到 53 個交易日。
2. 會限流：連打上百次後整個 IP 被擋，回 HTTP 307 + 中文安全性 HTML 而非 JSON；換 UA 無效。
   → `fetch_day` 改回 `(狀態, 資料)`，`closed`(非交易日) 與 `blocked`(被擋) 分開。
   兩者都回 None 的話，被擋的日子會被永久當成「沒開盤」跳過，而 100MA 靠每日累積，
   **漏一天就永久少一天**。

**ETF 代理否決（有量化依據）**：FinMind 0053/0055 有完整歷史，與官方電金比相關 0.978，
但縮放還原後平均誤差 0.85%、最大 2.3%；訊號間距僅約 5% → 均線附近會翻錯邊。
Leo 選 A 案「每日累積」，約 2026-11 中旬滿 100 日，在那之前日報顯示「累積 N/100 日」。

**刻意不做**：只量測不下行動指令。老墨的「連 32 日轉弱→清倉」是他自己系統的規則，
我們沒有對應回測依據 → 不自行發明投資判定門檻。

**排程時間差**：`researcher_stock_sync.cmd` 08:45 跑，但類指數 13:30 後才公布。
只抓「今天」會永遠存不進任何一筆 → `run()` 改補「最近 14 天缺口 + 歷史舊缺口」，
順便自癒漏跑。放在 .cmd 最前面（漏一天不可逆）。

**驗證**：MA_DAYS 暫設 20 對現有 53 天實跑，均線值與連續天數皆與手算一致。

## 2026-08-27　P3 預估值 base-rate 檢查

**它回答什麼**：分析師說今年營收 X → 扣掉已公布的，剩下的月/季要跑多快？那個速度
這家公司自己做過嗎？意義是風險刻度不是買賣訊號——股價照共識定，共識若要求史無前例
的事，做到了未必漲（早算進去），沒到就會跌。

**兩個設計要點（實測打臉才發現）**
1. **先扣掉已實現的部分**。MU 的 FY26 共識 vs FY25 實際 = +247%，聽起來像天方夜譚；
   但 FY26 已公布 3 季，真正未發生的只有最後一季 +22%，而 MU 季增中位數就有 +48%
   → 其實輕鬆。扣不扣差了一個量級的結論。
2. **比成長率不比絕對金額**。2330 拿絕對金額比時「同期歷年中位 733 億」——那是十幾年
   前的台積電。公司規模長五倍，成長股「超越歷史最佳」必然成立，等於沒篩。

**分級用每檔自己的分布，不用全市場通用門檻**
- 不能用百分位：美股單季 n=5，「超過最高」必然 100%，二元值。實測 96 檔時 32% 觸發，
  CI(+1.8% 季節性) 與 SPCX(+105% 真異常) 被歸同一級。
- 不能用「超出 X%」：MU 要求 +22% 對它是常態，TFC 要求 +4.4% 卻已破自身最高 +4.0%。
- EXCESS_K / EXCESS_MIN_PP 是版面分界不是投資判定門檻。沒有 MIN_PP 時分布很窄的股
  會誤升級（ON 最高 +6.0% 中位 +5.6%，超出 1.4pp 就判 🚫）。

**修掉的靜默失效**
- `done==0` 防呆誤擋 MSFT/COHR/LITE（6/30 結算，新年度還沒任何一季，但整年都在前面
  本來就算得出來）→ 三檔持股無聲消失。
- 原本要掛週六 buffett_scan_weekly，但那支只跑週六、daily_warroom 在只跑週一至五的
  researcher_stock_sync 裡 → fresh_only 會讓⑥段永遠不出現。改掛平日 + --if-stale-days 7。
- run() 印出算不到的檔名——「沒有警訊」跟「根本沒算到」不能長得一樣。

**實測 88 檔**：🚫8 ⚠️4（14%）✅44 ⚪低覆蓋14 ⚪無共識18。
最緊的：AAOI 要求 +81%（自身最高 +27%）、COHR +47%（+7%）、CLS +26%（+16%）。

## 2026-08-28　P3 上線收尾 + ADR 幣別錯位修正

**做完的**
- P3 接進投資長**價值角度**（不是趨勢——它量的是市場對這檔的期待被堆多高，本質是估值前提）。
  prompt 加防誤讀三條：不是公司好壞是容錯空間、**不要讀成分析師亂喊**（被標記的多半共識
  長期偏保守）、跟貴俗價是兩件事兩個都要提。
- 範圍 93→186 檔（補七鏈守備清單；原本漏掉 2317/2330/2454 這些天天在盯的）。
  補進來的成分股佔據 🚫 榜首（AVGO/ALAB/CAMT/MRVL/8046/3037），證明擴範圍是對的。
- 推送改**獨立一則**：持股→🔒持股密報、非持股→#財報。週跑，釘週一（`--weekday 0`）。
  「滿7天就跑」會漂移，漏一個週一之後就永遠變週二。
- 頻率改**月更全清單（11-13號）＋週更只推燈號有變的**。同一份清單每週貼一次沒人看。
  降級（🚫→✅ 壓力解除）也算變化。
- 卡片只給「預估門檻」燈號。**「現PE÷預估PE」我一度當成「市場押注」是錯的**——
  攤開來 =(P/現EPS)/(P/預估EPS)= 預估EPS/現EPS，股價完全被約掉（實測四檔到小數點都一樣）。
  股價那軸找不到可自我校準的基準（本益比vs自身歷史只算得出2個點），所以不給燈號。
  用全市場相對百分位則**保證永遠40%紅燈**，燈號退化成排名比沒有更糟。
- 08:45 批次補失敗通知（原本是唯一沒有的，卻扛著五樣東西）。
- earnings_watch 的財報卡片摘要接上 #財報（原本組好卻沒發送）。
- tw-board 排程加回台灣週一。
- alert_telegram.py 檔頭加警告：**它是 signals.json / st_flips_today.json 的唯一寫入者**，
  精簡 Telegram 時只能拿掉 send_text，不能整支停。

**🔴 ADR 幣別錯位（今天最重要的發現）**
常利EPS 是財報幣別/每原股，股價是掛牌幣別/每ADR → 12 檔美股 ADR 全錯，
TM 俗價差 159 倍、8 檔顯示假「便宜」。修法用 yf `trailingEps` 推換算係數
（幣別＋換股比例一次修掉，不用查匯率）。TSM 從假 🟢 變真 🔴。

**⭐ 它為什麼躲過所有檢查**：每日排程、每週掃描、完整性防線——每一關看的都是
「有沒有跑完」，沒有一關看「數字合不合理」。最後是把三個數字並排在同一張卡片才看見：
單獨看「俗價 $3,244」不覺得怪，跟「現價 $418」擺一起就藏不住。
**把彼此該有比例關係的數字放同一行，比再多的存在性檢查有效。**

## 2026-08-28（續）　Telegram 精簡 + 死程式判斷教訓

**TG 精簡定案**：拿掉買進機會週報/財報快訊/投資長判斷（改只留 Discord，內容本來就
雙發是重複）、GDP觀察頁過期提醒/本週守備清單已重篩（直接刪，純狀態不影響決策）。
函式整段刪除不留半死不活分支。精簡後 TG 只剩投資晨報/早報/盈再表摘要三個心跳，
貴俗價翻貴/服務健康/各種失敗通知三類有事才發。

**⭐ 差點誤刪 st_alert.py**：判斷「死程式」時只查了排程有沒有直接執行它（沒有），
就準備刪掉。複查才發現 alert_telegram.py 有 `import st_alert` 並呼叫
`st_alert.detect_flips()`——SuperTrend 翻面偵測的核心邏輯，投資長與 Discord 日報
的訊號都源自這裡，真刪掉會斷整條訊號鏈。

**教訓**：判斷「這支是不是死程式」要兩層都查——① 排程/.cmd/workflow 有沒有直接
`python xxx.py` 執行 ② **其他程式有沒有 `import xxx` 當函式庫用**。只查第一層
是這次判斷失誤的直接原因。之後同類判斷都要先跑 `grep -rln "import xxx"` 再下結論。

（真正沒用的只是 st_alert.py 檔頭殘留的 TG 常數/docstring，已清掉；detect_flips() 留著。）

## 2026-08-28（續2）　財報懶人包卡片「永遠不重試」bug

Leo 問「nvidia 財報不是出來了嗎？怎麼財報分析沒抓新的」帶出來的。

**表層原因**：yfinance 的 get_earnings_dates(EPS數字) 比 quarterly_income_stmt
(完整三表) 更新快。NVDA 8/26 公布，EPS 抓得到，完整三表到 8/28 還停在上一季。

**真正的 bug**：daily_followup() 的 state["reported"]="sent" 標記不管卡片資料
新不新都會標——make_infographic()/fetch() 沒驗證拿到的季度是不是這次公告對應的
那季，只要不是空的就當成功。卡片被悄悄產成舊季別，state 卻永久標記完成，
要等 3 個月後下次財報才會自然更新。

**修法**：_infographic_is_fresh(path, ed) 比對卡片期末日離公告日的天數。
門檻用實測校準：AAPL/MSFT/TSLA/AMD 正常差距22~35天，NVDA差了118天。
第一版隨手設130天太寬鬆——實測才發現118天會被誤判「新鮮」，改75天（2倍安全邊際）。
不新鮮 → 記進 infographic_pending，EPS快訊只推一次不重複，卡片每天輕量重試，
30天沒跟上就放棄。實測驗證：NVDA正確留在佇列，沒誤標記、沒洗版。

**教訓**：資料源不同欄位更新速度不同是常見坑，不能假設「API有回應」＝「所有欄位
都最新」；跨欄位拼出的產出要驗證每個輸入各自的新鮮度。門檻數字要拿真實資料校準，
不要憑感覺設，這次差點用130天放過同一個bug。

## 2026-08-29　研究 tide-tw.app + 四項升級

Leo 給 tide-tw.app 要我研究能學什麼。實測抓它的 data/*.json 反推：純靜態 JSON
（跟我們同架構）、泡泡圖是**籌碼面**（近5日法人買超/加速度）不是我們的價格動能、
一句話總結**不是 AI 寫的**是排序套模板、110個題材分類是人工建+維護。

**做了四項**
1. 產業輪動一句話（chain_technicals.overview_line）零成本模板 → 日報①段
2. 全市場籌碼異常（chip_scan.py）→ 日報④段
3. 股價本地快取（price_store.py）→ industry_rotation 12分鐘變 9 秒
4. industry 細分類切換 → rotation.html 加「粗分類/細分類」按鈕（台17→77籃子）

**⭐ 最重要的結論：「擴大到全市場分類」對決策幾乎沒有增益**
Leo 追問「全市場覆蓋跟分類到底用在哪」，查資料流才發現全市場分類只有 3 個消費者，
而唯一驅動行動的（投資長）**只看得到 185 檔**——每籃只取市值前10大。所以
「Electronic Technology 翻紅」拉出來的永遠是台積電鴻海，不會是正在起漲的中小型股，
分類再細也拉不出來。真正缺的是把 chip_scan 的 1,964 檔異常跟七鏈對起來（未做）。

**三層分類不可互相取代**：sector(17,太粗)/industry(77,按生意分)/七鏈(7,按AI題材分)。
實測矽光子那條鏈散在 Semiconductors/Industrial Machinery/Electrical Products
三個 industry 裡——任何按生意分的系統都拼不出題材鏈。

**門檻校準（同週第二次犯同一個錯）**
籌碼異常倍數第一版設 3.0x → 25% 觸發（1/4 都「異常」就不叫異常）；看完實測分布
（第95百分位 5.88x）改 5.0x → 9.2%。連續天數抄 tide 的「連買5/連賣3」，實測
連賣3天出45檔 vs 連買5天13檔差3倍會洗版 → 統一5天。
**教訓：訂門檻前先寫 calibrate() 看分布，不要抄別人的數字。**

**price_store 技術決定**
存 pickle 不存 parquet（parquet 要 pyarrow，多一個依賴多一個雲端失敗風險）；
不進版控（可重建的衍生資料，1,246檔42MB每天變動，進版控 repo 不可逆肥大）。
實測逐檔 vs 批次 = 1.6秒 vs 0.2秒（8倍）。

**又一次「過寬 except 吞掉真錯」**（今天第六次）
細分類整個失敗，錯誤訊息只有 'Close' 兩字。追下去是 1,246 檔裡**有 1 檔**
yfinance 回 MultiIndex 欄位，KeyError 往上冒到 main() 的 except，
一檔壞資料吞掉 109 個籃子。兩道防線都補（price_store 攤平 + 取用時檢查欄位）。

**版面兩次反饋才定案**：加細項(vs_avg/days/shares 本來就有卻沒顯示) →
串成長串每類150字手機折7-8行、四類糊成31行 → 改一檔一行+空行分段+收到4檔 → 18行。
單位用「張」不用「千張」（「42千張」讀起來像42,000張但實際42,448張）。

---

## 2026-08-31（一）財報管線大修 + 毛利率循環位階

### 起點：一則沒送到的財報快訊

Leo 問「MRVL 財報出來了，Discord 會有摘要嗎？」→ 查完是**不會**，而且比想像嚴重。
稽核 30 檔追蹤清單，**只有 2 檔曾經推播過**（NVDA 8/26、富邦金 8/31），
7-8 月公布的 15 檔從頭到尾沒進過 Discord。三個原因疊在一起：

1. **T-7 登錄只在季度重掃當天發生** — 每日跟催只看 `state.upcoming`，而那只在
   QUARTER_DAYS 那幾天寫入。8/19 重掃只登錄到 3 檔，8/8~8/18 公布的整批
   落在兩次掃描的縫裡。MRVL 財報 8/27 是 T-8，**差一天**沒進去，下次重掃 11/19。
2. **AFTER_DAYS 在 8/27 前是 3 天**，認列窗口更窄。
3. **Discord 訊息把沒有卡片的股票壓成一行計數** — `push(msg, discord_msg=d_msg)`
   只要 d_msg 有值就只發它，四段快訊的 msg 整包丟掉。8/28 TG 精簡後
   msg 就沒有出口了，於是 MRVL 變成「另有 2 檔」一行。

修法：每日跟催改成直接掃 `build_universe("holdings")` 30 檔；Discord 訊息改成
**逐檔決定**（有卡片發卡片摘要、沒卡片發四段快訊），不再壓成計數。

⭐ **這件事的元教訓**：我先跟 Leo 說「快訊已經發了」，那是看**終端機輸出**得到的
結論，Discord 實際收到的是另一份。已寫進全域 CLAUDE.md（見下）。

### 美股財報分析從「等不到」變 D+1

Leo 要的架構是「Discord 快訊 D+0/D+1、網頁深入分析 D+2」。實測後：
- 台股走 FinMind 已成立（富邦金 8/28 公布，8/31 就有完整三表）
- **美股卡在 yfinance** — MRVL 8/27 公布，D+4 還停在上一季；NVDA D+5 也是

改接 SEC EDGAR（`sec_quarterly.py`）。11 檔實測 10-Q 申報時差：9 檔 D+0~D+1、
AVGO D+6、GLW 例外（10-Q 7/29 就送了但 XBRL 33 天沒進 companyfacts）。
照 `_tw_core` 既有模式接進 `fetch()`：**只在 EDGAR 真的比較新時才覆蓋**，
拿不到就維持 yfinance。兩個源失敗的公司不一樣，疊起來覆蓋率更高。

三個實測到的坑（打開 MRVL 資料看到的，不是抄文件）：
1. 同一期末兩筆，181 天是半年累計、90 天才是單季 → 靠 `frame` 欄位＋期間長度
2. **現金流在 10-Q 裡只有 YTD 沒有單季** → 自己相減（1244.3−638.8=605.5M）
3. 科目標籤各家不同且缺項正常：GOOG 沒有 GrossProfit（財報本來就不列）

⭐ **交叉驗證抓到自洽測試看不出來的 bug**：原本「照 TAGS 順序第一個有資料的就用」，
結果 NVDA 停在 2022-01-30、GLW 停在 2018-09-30——這些公司中途換過科目標籤，
舊標籤留著歷史資料。數字算得出來也自洽，是**跟 yfinance 比對同一季才現形**。
改成挑資料最新的那個。最終 10/11 檔的營收/淨利/OCF/EPS 差異全部 0.00%。

### 失效條件拆成趨勢／價值兩組

盤點 83 條，44 條價格類裡 39 條寫的是俗貴價 → 每天真正在檢查的只有估值一個角度。
SCHEMA 拆成 `trend_conditions` / `value_conditions` 兩者都必填，並新增兩種
**每天算得出來**的型別：`supertrend_flip`、`rs_below`（本來就不必等財報）。
日報每行掛 📉趨勢 / 💰價值 符號（不拆節，避免行數翻倍）。

順修 `thesis_check` 的印出迴圈：8/31 加 held 後 tuple 變 3 元素，
`for tk, msg in triggered` 只要有任何一條真的觸發就會 ValueError。沒炸只是因為沒觸發過。

### 簡體字驗收（做了三輪才對）

MRVL 的 AI 快訊整段簡體。第一版手寫字表漏掉「晶圆」的圆 → 改用 OpenCC。三個發現：
1. **設定要用 s2tw（台灣標準）不是 s2t** — s2t 會把 峰→峯、群→羣、為→爲
2. **OpenCC 是詞組級轉換** — 「發布日期」不變，「公布財報／布局」卻 布→佈
3. **白名單用實測 false positive 反推** — 試過用專案註解和記憶庫當語料自動推導，
   兩次都失敗（那些檔案本身就含引用的簡中內容）

`ask_json_traditional` 抽成獨立函式的理由：寫在 `narrative()` 裡面**測不到重試路徑**
（要一整包財報 facts 才跑得動）。抽出來後四條路徑都實測過。
納管範圍：財報卡片、AI 快訊、投資長判斷、tw_analyze/us_analyze、earnings_call。

### 毛利率循環位階（Leo 批准後才做）

base_rate 量「市場期待 vs 自己的歷史成長率」，量不到「現在的獲利能力在自己歷史的
哪個位置」。MU 就是缺這塊會誤判：✅normal（只要求季增+22%、自己做過+75%）看起來
寬鬆，但那個基期是毛利率從中位 37.7% 衝到 84.6% 撐出來的。

⭐ **位階和擺盪必須一起看**：MU 和 AVC 都是位階 100%，但 MU 擺盪 117pp
（雲霄飛車頂端）、AVC 只有 16.8pp（樓梯頂端），均值回歸風險差一個數量級。

首跑 186 檔抓到四個問題，全是實測出來的：
| 問題 | 案例 | 修法 |
|---|---|---|
| 63 檔算不出 | AMZN 2009 後不揭露 GrossProfit | 用「營收−成本」回推，123→140 |
| 假訊號 | PLUG 位階92%標循環頂，毛利率卻是 -0.9% | 加毛利率絕對值地板 |
| 擺盪 4943pp | CCL 2020 停航，營收 31M 對固定成本 | 剔除營收<自身中位20%的季度 |
| 擺盪 2072pp | MARA 營收標籤只抓部分、成本是全部 | 中位<-50% 整檔不判 |

門檻全部取自實測分布：MARA 中位 -112.5%、次低 -0.9%，中間空 111.6pp。

### 金控財報卡片整欄 N/A

Leo：「為什麼玉山點進去資料會是空的？」→ **科目名稱單複數不符**：
一般股是 `IncomeAfterTaxes`，金控是 **`IncomeAfterTax`**（少一個 s）。
四家金控的淨利全部抓不到。毛利/營業利益維持 N/A 是對的——銀行沒有毛利概念，
**刻意不拿 PreTaxIncome 去填營業利益欄**，標錯比留白更糟。

順帶抓到富邦金 Discord 上的 **+2700.4%** 假成長率：FinMind 的 2025-06-30 營收
只有 37.9 億（其他季 700~1400 億）。掃 17 檔共 304 個季度，最低的 6 個季度
**全是金控**（富邦金 2022-12-31 甚至是負營收 -36.7%），全體第 5 百分位是 70.8%
→ 門檻設 40%，落在 37.8% 與 70.8% 中間的空隙。

### 🔴 全域規則：出手前先查最終狀態

Leo：「可以不要一直出現錯誤的建議嗎？請都查清楚，記在全域裡」

同一天踩四次同一個錯——把**過程中看到的**當成**真實狀態**：
| 我看到的 | 實際的 |
|---|---|
| 終端機印出四段快訊 | Discord 只收到卡片摘要，MRVL 被壓成一行 |
| 我用寫死清單篩出的 base_rate 子集 | 答案剛好在被濾掉的標的裡 |
| 記憶裡的「88 檔」 | 檔案裡是 186 檔 |
| exit 0 + 卡片產出成功 | 資料源沒跟上，卡片是舊季 |

最痛的一次：Leo 問 HBM 是否 price in，我讀了 base_rate 卻用寫死的 HBM ticker
清單過濾（不含玻璃基板），然後用新聞敘事推論「機會點在玻璃基板」。實際查才發現
base_rate 早就算出欣興 +16.4pp／南電 +24.5pp 是 unprecedented（**比 NVDA 的
+6.9pp 還兇**），期待最低的其實是散熱 AVC（-82.5pp）。**推論結論跟自己的資料相反。**

已寫進 `~/.claude/CLAUDE.md` 與記憶 `verify_final_state_not_observed`。

### 新增檔案
`sec_quarterly.py`（美股單季財報←EDGAR）、`margin_cycle.py`（毛利率循環位階）、
`discord_guide.py`（推播總覽說明，可重跑更新）、`earnings_backfill.py`（一次性回補）

### FinMind 資料集盤點（2026-08-31 晚）

起因：財報卡片的法說會步驟卡住，Leo 問「爬資料有問題，可以用 FinMind？」

實測逐一探測 API（不看文件），結論：
- **33 個 dataset 免費可用，沒有任何一個回 402**
- 🔴 **沒有法說會/投資人會議資料集**（`TaiwanStockInvestorsConference` 等全部 422），
  `TaiwanStockNews` 只有標題+連結沒有內文 → 法說會摘要只能靠 claude+WebSearch
- **免費 vs 付費的唯一差別是「全市場查詢」**：不給 `data_id` 一律
  `400 Your level is register`。逐檔查詢正常，**歷史深度沒有限制**（2010 年拿得到）
- 而我們需要全市場的那個場景（籌碼掃描）8/29 已改走證交所 T86＋櫃買官方 API，
  免費且官方 → **付費買不到我們缺的東西**

⚠️ 第一次探測有一半回「0 筆」是**我的測法錯了**（用 8/25 當起始日查季報和月營收），
用正確日期區間重測後全部有資料。已在回報時更正。

三個沒用過的籌碼指標（融資融券／當沖比重／外資持股）做了說明與實測，
Leo 決定「先都不接…沒想到有什麼用處」。要接之前應先看實測分布有沒有鑑別力，
照 chip_scan 門檻校準那套，不要先假設有用。

### 平行化實測

Leo 問「為什麼跑這麼久」→ 拆解單張卡片 3 分鐘的組成：
抓財報 2.5 秒、評分 1.6 秒、**法說會逐字稿 ~90 秒、AI 敘事層 ~90 秒**
——資料處理只佔 4 秒，99% 是兩次 AI 呼叫。

平行跑 13 張：**總耗時 36 分鐘 → 8 分鐘，全部 rc=0 沒撞限流**。
個別卡片會互相拖慢（最慢 226s→450s）但不失敗。

🔴 **同時抓到我今天引進的回歸**：`llm_board.TIMEOUT=600 秒` × 我加的繁體驗收
重試 3 次 = 單張卡片的法說會步驟最壞 **30 分鐘**（2882 實測卡 20 分鐘沒回）。
改 `tries=1` 後 407 秒完成。教訓：**加重試機制時要看它乘在多大的 timeout 上**。

### 台股持股更新（2026-08-31 21:00）

Leo 給集保庫存明細截圖要求更新。先對帳：12 筆市值相加 = 8,929,366，
跟集保總計完全吻合 → 截圖中間的黑色區塊沒有藏持股（這步是必要的，
截圖被遮住的部分不能靠目視推斷）。

比對 `assets-dashboard/data/holdings.json`：**只有 00407A 主動凱基台灣 8,000 股
是新的，其餘 11 檔股數本來就一致**。只新增這一筆，其他欄位一個都沒動。
新增後 12 檔逐一對帳全部一致，`_fetch_tw_quote("00407A")` 回 9.76 跟集保收盤價相同。

⚠️ 台泥/佳格/五鼎/正新/鴻海 5 檔的 `cost_basis` 本來就是 0（查過唯一的備份確認
不是這次弄掉的），Leo 指示「為 0 就先空著」。

不用重啟服務：`/api/holdings` 每次請求才 `_load_holdings()`，不是啟動時快取。
備份 `holdings.json.bak-20260831-210243`。

### 排程驗收 + 三個修正（2026-08-31 晚，另一條 session）

8/28-29 大改版後第一次自然驗收（之前都是手動測）。兩批排程都 rc=0，
AI 判讀完整性守門沒誤擋（美股 59/59、台股 47/47），`signals.json` 103 檔
分布 ⚪84/🟢16/🔴3，看板報告 grep「資料分析失敗」0 筆。

新功能都有產出：籌碼異常 59 筆、七鏈技術面 16 條、產業輪動一句話進①段、
投資長 51 檔判斷、失效條件登錄 81 條。

#### 🔴 修一：大盤體溫計每天都在丟資料

`ef_ratio.json` 最新只到 8/27，8/28 和 8/31 被寫進 `ef_ratio_closed.json`
（非交易日黑名單），而那份黑名單**永不重試**。

根因：排程 08:45 跑，但證交所類指數 13:30 收盤後才公布。對「今天」證交所回
一個正常 JSON 但沒有價格指數表 → `fetch_day` 判成 `closed`。

影響遠大於漏兩天：**每一天都會在自己那天早上被判死，隔天也不回頭補**，
累積數會永遠卡在 53/100，100MA 永遠算不出來，A 案等於報廢。
檔頭註解其實寫對了（「排程在早上跑但指數 13:30 後才公布」），但**沒有寫進判斷式**。

修法：加 `_published()`（13:35 後才算已公布），未公布的日期完全跳過——不打請求
也不寫黑名單。誤標的兩天移出後補回，55/100。
實測 `fetch_day('20260828')`/`('20260831')` 都回 ok；6/19、7/10 回 closed 是真假日。

⭐ 通則：**「永不重試」的黑名單，寫入條件要比讀取條件嚴格得多**。
「查不到」有兩種——真的沒有 vs 還沒到時候，混在一起就是永久資料遺失。

#### 🔴 修二：上櫃 `.TWO` 後綴只修了一支（Leo 問「XQ 有機會嗎？」帶出來的）

Leo 看到 log 裡 15 檔台股抓不到價，問要不要換 XQ。實測 15 檔全部換 `.TWO`
就有完整報價 → **不是資料源缺，是後綴給錯**，XQ 不用出動。

順著掃：用櫃買官方名單（792 檔）比對，台股觀察清單 44 檔裡 **13 檔是上櫃（30%）**。
`data_tv`（2026-07-31 修過）、`industry_rotation`、`chain_technicals`、`paper_portfolio`
沒問題；`st_alert` / `screen` / `thesis_check` / `trade_plan` / `earnings_watch` 全部硬掛 `.TW`。

實測 `st_state.json`：**上市 31/31 有 SuperTrend 狀態、上櫃 0/13**——
守備清單三成的股票從來沒被偵測過翻多翻空。`screen.py` 那邊 `3105.TW` 的
marketCap 回 None（`.TWO` 是 362 億），被 except 吞掉變 mc=0，而市值是
`rank_score` 三因子之一，等於這 13 檔的市值分數永遠墊底。

新增 `tw_symbol.py` 當單一入口：`resolve()` 單檔（結果進 `state/tw_suffix.json`，
失敗不寫快取）、`batch_with_otc()` 批次（先 `.TW` 一次、漏的再 `.TWO` 一次，
不逐檔 probe）、`is_tw_code()` 給純比對用。修後上櫃 **0/13 → 13/13**。

⭐ 「0/13 vs 31/31」這種完全對立的覆蓋率，是靜默失敗的指紋。

⚠️ `trade_plan.py` 在 `.gitignore` 內（讀 assets-dashboard 的實際持股），
修改只在本機生效沒進 repo。`TradingBot/mike_crosscheck.py` 同樣的坑還沒修。

#### 🔴 修三：總經異常偵測用過期資料觸發（我第一版建議是錯的）

`researcher_macro` 判「市場異常」讀 `market_data.json`，但那份檔案由 GitHub
Actions 更新、卻被比它早跑的 07:00 批次讀。今天讀到**兩個交易日前**的資料：
拿 8/27 的費半 +2.33% 當今日異常去叫 AI 查證，而 8/28 實際是 -3.47%。
等於查一個已被隔日行情推翻的事件，AI 回來的筆記還把日期寫成 8/28，
**錯誤事實進了 `research_notes.jsonl` 被投資長讀走**。

我原本建議「改成自己抓 yfinance，擺脫 Actions 依賴」。**實測打臉**：本機
yfinance 對所有美股標的都只到 8/27（連指定日期範圍都拿不到 8/28），
而 8/30 還抓得到 8/28、隔天反而退化；Actions 上同一支 `market_fetch.py` 卻抓得到。

⭐ **換一個來源只是把過期換一個地方發生，而且新來源不會知道自己過期。**

改成兩個來源逐指數取較新的那一個，超過 3 天不採用並印警告；觸發原因帶上日期
（「費城半導體 08/28 單日跌 3.47%」），日報①段不會再跟同段行情自相矛盾。
守門實測過：8/28 通過、8/27（就是這次的 bug）被擋。

⚠️ 過程中另抓到：22:19 測試時 yfinance 給了一根日期 08/31 的 `^SOX`，
但美股才剛開盤，那是**盤中值不是收盤**。只比日期的話「取最新」會選到半截資料。
加了各市場收盤時間判斷，沒收完就退一根。

#### 沒動的兩件（Leo 決定）

- `INDEX_MOVE_PCT = 2.0%` 維持不動（Leo：「先不動吧 太吵再跟你說」）。
  實測過去 255 個交易日，現行設定會觸發 136 天（53%），費半一檔就佔 109 天
  （費半日波動中位數 1.69%，S&P 500 只有 0.49%）。分級門檻可降到 64 天（25%）。
- `base_rate` 今天沒跑不是壞掉：`--if-stale-days 7` 擋掉（8/28 手動測過），
  下次 9/7。也就是「週一才跑」這條路徑目前仍未拿到自然驗收。

#### 用詞更正

回報時把 `claude -p` 的 `total_cost_usd` 寫成「花了 $0.38」，Leo 反問
「不是都先走不花錢的嗎」。查證：`ANTHROPIC_API_KEY`/`AUTH_TOKEN` 都沒設，
`~/.claude/.credentials.json` 是 `claudeAiOauth` + `subscriptionType: max`
→ **走訂閱額度，現金支出 0**。那個值是等值標價。
以後一律照 `investment_chief` 的寫法標「等值標價／Max plan 走訂閱額度」。

## 2026-09-01（二）凌晨：TradingBot 靜默停擺 5 天

Leo：「8/27 之後就沒有盈再表的推送，幫我查」

### 根因：換憑證漏掉兩處

8/28 資安事件 revoke `@DT_tradebot`(8596987909) 換新 token，**只更新本機
`daily_stock_analysis/.env:44`**。當時記錄寫「這隻只有 daily_stock 在用，無 Zeabur 要改」
——**錯的**。同一隻 token 其實存在三個地方：

| 位置 | 8/28 | 後果 |
|---|---|---|
| 本機 .env | ✅ 更新 | — |
| **Zeabur** TradingBot | ❌ 漏 | 啟動 getMe 失敗 → 程序起不來 → 停擺 5 天 |
| **GitHub Actions** secret | ❌ 漏 | **靜默失效**（見下） |

`@DT_tradebot` 的顯示名就是 **BB-9E**——名字不同讓「誰在用」的判斷失準。

### 影響：8 個排程全停，而且沒有任何失敗通知

因為**那些失敗通知本身就要靠這隻 bot 發**。停掉的包含每日 06:00 巴菲特訊號、
07:30 情緒摘要、12:00 盈再表論壇、每週一三支報告、**每週六 03:00 SQLite DB 備份**
（最後成功 8/22，缺 8/29 那次）、以及 Sonia 的 `/stats`。

⚠️ **Actions 那個更陰險**：workflow 裡發 Telegram 那行結尾是 `|| true`，
token 失效也照樣 `conclusion=success`。查 tw-board 最近 5 次全 success，
**但那不證明 Telegram 有送出去**。用 `gh secret list` 看 updatedAt 才驗得出來。

### 🔴 我在診斷過程中犯的錯

Leo 更新 token 之後我才測 `/version`，看到有回應就**跟他說「我的診斷錯了」**
——其實是我驗證的順序反了，推論本來是對的。
**診斷時要先問「你剛剛動了什麼」**，這是 8/31 才寫進全域 CLAUDE.md 的
「不要把我看到的當成實際的」同一類錯誤，隔天就又犯一次。

另外 `/stats` 的 502 我當成了關鍵證據，但程式碼註解明寫它是背景 thread、
「失敗不影響 bot 本體」——**它可能本來就是 502**。事後回頭看那是運氣好猜對，
不是推論嚴謹。

### 修復（9/1 04:20 全部完成）

1. Zeabur `TELEGRAM_BOT_TOKEN` → 手動 Redeploy → `/stats` 連測 4 次 200
2. Actions `TELEGRAM_BOT_TOKEN` → `gh secret list` 驗 updatedAt
3. Zeabur `GITHUB_TOKEN`（舊的叫「巴菲特價值清單」，fine-grained 只綁
   daily-stock-board）→ 建新替換 → Redeploy 驗 200 → 刪舊

**GitHub 手機版建 fine-grained token 的坑**：Settings 在右上角頭像不在漢堡選單；
權限要按 Repositories 那列右邊的 **＋** 叫出清單、搜尋 `contents` 勾起來，
**Read/Write 下拉選單要勾完之後才出現**在主畫面——不是在清單裡直接選。

### ⏳ 仍未換：Google OAuth（4 系統共用）
memory 寫「能讀 Drive/日曆/Gmail」，但實查 TradingBot 的 scope 只有 `drive.file`
（僅限自己建立的檔案）——各系統 scope 要分別確認，不要一概而論。

### 09-01 08:45 batch：push 失敗（競爭條件，非改動造成）

Leo 收到「Failed steps: push」。查 log：

```
今天觸發 6 檔（持股 0、非持股 6）
[private/chief] 今天沒有實質內容，不發     ← 投資長沒東西是「正常」
! [rejected]  HEAD -> main (fetch first)   ← 真問題在這
```

**投資長沒推是對的**：今天觸發的 6 檔全是非持股，持股密報的投資長段只放持股。

**push 失敗是競爭條件**：batch 開頭 pull、跑 13 分鐘、最後 push，期間
GitHub Actions 推了 `75b6d88`（首頁行情），batch 的 `2a5d2a2` 就被拒。
→ 當天研究筆記/投資長判斷/籌碼資料全部沒進 repo，只留一則失敗通知。
**原本只警告不重試 = 每撞一次掉一天資料。**

修法：兩支 .cmd 的 push 失敗改成 `git pull --rebase` 後重試一次。
**實測驗證**（故意製造分岔跑測試 .cmd）：第一次被拒 → rebase → 第二次成功 →
`FAILED=[]` 不誤報。

🔴 改的時候我又寫了中文 REM 註解，那是專案的 **ASCII-only 硬規則**
（cmd.exe cp950 解析會誤判），**這條我犯第二次了**，當場改成英文並驗證新增區塊
中文字數 = 0。

### 9/1 自然驗收：兩個修正生效、一個我造成的回歸（2026-09-01 上午）

昨晚三個修正的第一次自然執行。

✅ **體溫計**：黑名單仍只有 `2026-06-19`/`2026-07-10` 兩個真假日，**9/1 沒被誤標**
（08:45 跑時當日未公布 → 跳過不寫黑名單，正是修的那個行為）。累積 55 筆。

✅ **上櫃 `.TWO`**：`st_state.json` 上櫃 **0/13 → 13/13**、上市 31/31。
這次是在 GitHub Actions 上跑的 `st_alert`，昨晚只在本機驗過批次邏輯。

✅ **總經異常帶日期**：觸發文字「費城半導體 **08/31** 單日跌 2.92%」，
用的是最新交易日，AI 筆記的日期也標對，不再出現昨天那種前後打架。

🔴 **回歸：`valuation_alert` 整支掛掉（我造成的）**

```
trade_plan.py:352 in compute_valuation
    "market": "TW" if is_tw else "US"
NameError: name 'is_tw' is not defined
```

昨晚把 `compute_valuation` 裡的 `is_tw = bool(re.match(...))` 換成
`tw_symbol.resolve()`，但**同一個函式後面還在用 `is_tw`**——刪了賦值沒刪用法。
07:00 批次因此 exit 1，當天貴俗價翻轉警示沒發出。

已補回（改用 `tw_symbol.is_tw_code(tk)`），全檔七處 `is_tw` 掃過其餘都有賦值，
實跑 `valuation_alert.py` exit 0：算出 66/78 檔（美股 59/66、台股 7/12），
跟出事前的 66/77 一致，沒有其他副作用。

⭐ **教訓**：`py_compile` 抓不到 NameError，`import` 模組也抓不到——
只有**執行到那一行**才會炸。昨晚我把 `st_alert` 的批次路徑跑到底驗證，
卻只對 `trade_plan` 做了編譯+import 就收工。
→ **換掉一個變數的賦值時，要 grep 整個函式看它後面還有沒有被用到**；
   改完的每一支都要真的跑一次，不是只有「我重點驗證的那一支」。
（同 memory `unexecuted_code_paths`）

⚠️ 附帶發現：`.TWO` 對 `compute_valuation` 這條路徑其實沒有實質差別——
Leo 的 12 檔台股全是上市。算不出俗貴價的 5 檔是 4 檔 ETF（無 EPS，本來就算不出）
加 1 檔個股，不是後綴問題。這個修正的實際受益者是 `screen.py`（守備清單 13 檔上櫃）
和 `st_alert`。

⚠️ `trade_plan.py` 在 `.gitignore` 內，這次修正同樣只在本機、不進 repo。

#### 另一個新問題：08:45 批次 push 被拒（不是我造成的）

批次開頭 `git pull --rebase`，但 Actions 在批次執行期間（08:45-08:58）又推了一次，
結尾 push 就被 rejected。log 裡第一次出現。會自癒（明天 pull --rebase 後補推），
代價是當天 state 檔晚一天上遠端 + 一則 Telegram 失敗通知。
根治要在 push 失敗時自動 `pull --rebase` 重試一次——**還沒做，等 Leo 決定**。

（08:45 只跑 13 分鐘 vs 昨天 79 分是正常的：今天 6 檔候選，昨天 51 檔。）

---

### 2026-09-01 15:30　模擬倉 push 失敗：8/27 修過的問題漏了一支 workflow

**現象**（Telegram）：`🔴 模擬倉 push 失敗｜數字算好了但沒推上線，網站看到的還是舊的。`

**查證**（`gh run view 33475700017 --log-failed`）：

```
[main b0957fa] chore: 模擬倉每日淨值更新 [skip ci]
 2 files changed, 641 insertions(+), 593 deletions(-)
 ! [rejected]        main -> main (fetch first)
```

數字**算對了也 commit 了**，只有 push 掉了 → runner 是拋棄式的，
`portfolios.json` 跟 `docs/portfolios.html` 兩份產出當場消失。

**撞車對象是我自己**（全部 UTC）：

| 時間 | 事件 |
|---|---|
| 05:59:10 | portfolio-nav 排程啟動，checkout 拿到當下的 main |
| **06:00:15** | **我本機 push `046e709`（趨勢失效條件修正）** |
| 06:01:33 | nav 的 `git push` → rejected |

**根因不是撞車，是這支沒有重試。** 8/27 已經為了同一個問題加過 rebase 重試，
但只加了三支：

| workflow | 8/27 rebase 重試 |
|---|---|
| `tw-board.yml` | ✅ |
| `market-home.yml` | ✅ |
| `weekly-screen.yml` | ✅ |
| **`portfolio-nav.yml`** | ❌ 漏 |

諷刺的是 `portfolio-nav.yml` 是防呆寫最厚的一支（8/19 加了資料過期警示、
`if: always()` 強制發佈、FAILED 累積、失敗推 TG），唯獨 push 那段停在
「失敗就通知 + exit 1」。**通知寫得越完整，越容易在下一輪修補時被當成「這支處理過了」跳過。**

**修法**：把 tw-board 的 rebase 重試 3 次原封不動搬過來（`d1e5b18`），
再 `gh workflow run portfolio-nav.yml` 補跑當天淨值。

⭐ **收尾動作要固定成覆蓋率檢查，不是「修完手上這幾支」**：

```bash
for f in .github/workflows/*.yml; do grep -L "git pull --rebase" "$f"; done
```

跟 `otc_suffix_coverage_gap`、`earnings_card_stale_formula_bug` 是同一種病：
改核心做法時沒有掃全部呼叫點。

---

### 2026-09-01 15:40　資產中控台 Firstrade 小計 vs Firstrade App 對不上（查證結果）

Leo：「為什麼跑出來的跟我在 FIRSTRADE 看到的價值不一樣?」

| 來源 | 數字 |
|---|---|
| 資產中控台「Firstrade」小計 | US$66,870 |
| Firstrade App　Total Account Value | US$67,546.85 |
| 差額 | **US$676.77** |

**實跑 `_quote_one_holding` 重算全部 66 檔**（不是看畫面推論）：

```
Firstrade 持股即時市值合計 = US$66,870.08  (66/66 檔有報價)
抓不到價: []
```

→ **排除掉三個常見嫌疑**：
- ❌ 不是抓價失敗落回舊值（66/66 全有報價）
- ❌ 不是匯率問題（小計走 `live_value_native`，是原生 USD 不經匯率）
- ❌ 不是零股沒記到（`holdings.json` 有 11 檔是小數股，AAPL 記的是 `5.00991`，
  跟 App 的 5.00991 一致；畫面上的「5」只是四捨五入顯示）

**結構性差異（部分成立，但不是主因）**：`renderHoldingsSummary()`
（`static/index.html:568`）的小計是 `Σ 持股即時市值`，定義上不含現金。
但實際查證後，**現金只有 US$64.01**，解釋不了 676.77。

#### 🔴 我第一版的結論是錯的：「差額是現金」

Leo 打開 Balance details 後推翻：

```
Total Account Value  $67,536.51
Cash & Equiv.            $64.01     ← 不是 676.77
Long Stocks          $67,473.00
Margin / Short / Open Orders  全部 $0.00
```

**我犯的錯**：算出一個「跟股價無關的固定金額」，就套上最順的解釋（現金），
沒有去要能證實它的那個數字就寫進結論。這正是 `verify_final_state_not_observed`
講的——**推論寫成結論**。正確講法應該是「差額 676.77 與股價無關，可能是現金，
要看 Balance details 才知道」。

#### 重算後的真實缺口

先校準基準。Firstrade 顯示**即時價**，yfinance 給的是**前一交易日收盤價**，
用截圖裡的兩檔驗證：

| | Firstrade 即時 | 當日變動 | 回推前收 | 我的價 |
|---|---|---|---|---|
| AAOI | 107.40 | −0.29 | **107.69** | 107.69 ✅ |
| AAPL | 316.63 | −0.22 | **316.85** | 316.85 ✅ |

兩檔完全吻合 → 兩邊的差別就是 Day Change，把它扣掉才是同基準：

```
Firstrade Long Stocks (即時)  = 67,473.00
  減 Day Change               =     34.71
Firstrade 持股（前收基準）     = 67,438.29
我算的（yfinance 前收）        = 66,870.08
真實缺口                       =    568.21   ← 0.84%
```

**已排除的原因**（都是實跑不是推論）：

- ❌ 現金 —— 只有 64.01
- ❌ 抓價失敗落回舊值 —— 66/66 全有報價
- ❌ 匯率 —— 小計走 `live_value_native`，原生 USD
- ❌ 零股沒記到 —— 11 檔小數股，AAPL 記的是 `5.00991`，與 App 一致
- ❌ 價格基準不同 —— 已用 Day Change 校準
- ❌ **代號抓到別的標的** —— 66 檔逐一比對 yfinance `longName`，全部指向正確公司
- ❌ 最近誤刪 —— 對 `holdings.json.bak-20260831-210243` 做 diff，檔數/股數皆無變化

#### 結案：68 vs 66，四處差異全部定位

Leo 提供網頁版完整截圖，右下角 **「1 至 68 的 68」** 是關鍵——檔數就對不上。
把 68 列全部抄進來做完整比對（不是只找那兩檔）：

| 差異 | Firstrade | holdings.json | 前收金額 |
|---|---|---|---|
| **MRVL** 未收錄 | 2 股 | 無 | 423.32 |
| **C158889** 未收錄 | 6 股 | 無 | 0.00（無報價） |
| **SPCX** 股數 | 6 | 5 | 143.69 |
| **XYLG** 股數 | 9.38292 | 9.35889 | 0.71 |

```
我原本算的            66,870.08
  + MRVL 2股@211.66      423.32
  + C158889                0.00
  + SPCX 補 1 股          143.69
  + XYLG 補 0.02403 股      0.71
  ---------------------------------
修正後                 67,437.80
Firstrade（前收基準）   67,438.29
殘差                        0.49   ← 截圖只到小數兩位的四捨五入
```

⭐ **交叉驗證**：另外把 66 檔的 `cost_basis` 全部對一遍（`= USD 成本 × 32.3810`，
12 檔反推匯率完全一致），**不一致的正好只有 SPCX / XYLG 這兩檔**：

- SPCX 成本多 US$143.62 → 加碼 1 股買在 143.62，單位成本 176.72 → 171.20 ✅
- XYLG 成本多 US$0.71 → 配息再投資 0.02403 股 @ 29.55（該列有 `D` 標記）✅

**兩條獨立的線索（股數、成本）指向同一個結論**，不是硬湊出來的。

#### 修正內容

`data/holdings.json`（備份 `holdings.json.bak-20260901-155110`）：
補 MRVL / C158889 兩筆、SPCX 5→6、XYLG 股數與成本校正。
重算後 68 檔 = **US$67,437.80**，唯一抓不到價的是 C158889（落回 market_value=0，
不影響總額）。

#### 連帶處理：C158889 不能進策略母體

`trade_plan.py` 的 `HOLDINGS_PATH` 直接指向這份 `holdings.json`，所以：

- ✅ **MRVL 自動進了策略部位**（67 檔）與監控母體（93 檔），`owner_mark` 為空字串
  ＝Leo 核心部位。它上週才出財報，之前完全不在任何清單裡。
- ❌ **C158889 也會跟著進去**——無報價、非個股，會讓 base_rate／貴俗價／財報
  各自多一筆永遠失敗的雜訊。

加了 `NON_TRADABLE = {"C158889"}`。⭐ 但原本 `load_holdings` /
`monitored_holdings` / `legacy_tickers` / `kids_tickers` **四個函式各自
`json.loads(HOLDINGS_PATH...)`**，要排一個代號得改四個地方——這正是今天上午
才記進 `silent_failure_pattern` 的同一種病。所以收成單一入口 `_read_rows()`，
四處統一走它。

驗證：策略部位 67（MRVL ✅ / C158889 ❌）、監控母體 93、繼承台股 36、小孩 24。

⚠️ `trade_plan.py` 在 `.gitignore` 內，這次修改同樣只在本機。

#### 這次做對的地方（相對於前一節的錯誤）

拿到 68 列資料後**沒有只去找那 2 檔就收工**，而是全部 68 列逐項比對股數與成本——
才會抓到 SPCX / XYLG 這兩處「不在原始假設裡」的差異。
如果只找漏掉的檔，缺口會停在 423.81 對不上 568.21，然後又要開始猜。

---

### 2026-09-01 16:00　Firstrade 月度對帳提醒上線

Leo：「每月對一次吧 給我提醒」。

**為什麼是「提醒」不是「自動對帳」**：Firstrade 沒有公開 API，本機永遠拿不到
券商端的權威數字。硬做成自動對帳只會做出一個**不知道自己對不對**的東西。
所以機器算好自己這邊、推 Discord，由 Leo 對一眼。

- `assets-dashboard/reconcile_reminder.py`
- `firstrade_reconcile_monthly.cmd` → 排程 `FirstradeReconcileMonthly`
  每月 1 號 09:30，`StartWhenAvailable=True`（電腦沒開會補跑，
  這是 `ArkReportBiweekly` 從沒執行過的那個坑）
- 推 Discord `private`（持股密報），persona 仲達-風險官
- state：`data/reconcile_state.json`，下次會顯示「上次對帳 X 天前、當時幾檔幾元」

訊息內容刻意設計成 30 秒對得完：**先看檔數**（最快的判別，這次就是靠
「1 至 68 的 68」破案）、再看總額、附上小數股清單（最可能漂的）。
並寫明 **0.5% 以內算正常**——那是即時 vs 前收的落差，不寫的話 Leo 會去查
根本不是問題的東西。

#### 踩到三個坑，都是驗證抓出來的

**1. `sys.path.insert(0, daily_stock_analysis)` → `import main` 抓錯檔**
那邊也有一支 `main.py`，而且會連鎖 import 一堆這裡沒裝的套件
（`fake_useragent` 直接炸）。改 `append`，讓本專案優先。

**2. 🔴 heredoc 寫出來的 `.cmd` 是 LF-only，cmd.exe 執行失敗**
第一次用排程器觸發：`LastTaskResult=1`，而且 **log 檔連建都沒建**——
連 `echo. >> "%LOG%"` 都沒跑到，等於整個檔案沒被執行。轉成 CRLF 後 rc=0。

⚠️ **我一度把原因說成「多行括號區塊 + LF」，那是推論而且是錯的**：
`board_analyze_daily.cmd` 同樣 LF-only、同樣有括號結構，卻一直正常在跑。
**確定的只有「LF→CRLF 修好了」，確切機制我沒查出來**——不要把它寫成定論。

**3. 🔴 推送失敗會被吞掉（自己造的綠燈假象）**
`send_discord` 失敗只印警告、回 False、**不 raise**。我第一版忽略回傳值，
實測用壞掉的 webhook：`404 Unknown Webhook` → 照樣印「已推送並記錄」、
**exit 0、而且把 state 寫進去**。等於把一次沒送出去的提醒記成做過了，
下個月還會顯示「上次對帳＝這次」。
→ 改成看回傳值；失敗時 **rc=1 且不寫 state**。兩條路徑都實測過：

```
失敗路徑：[discord] 發送失敗 404 → [reconcile] 推送失敗 → exit 1 → state 未變動 ✅
正常路徑：排程器觸發 LastTaskResult=0，NextRunTime 2026/10/1 09:30 ✅
```

這條完全是自己造的坑，而且跟今天上午才寫進 `silent_failure_pattern` 的
「`|| echo` 是靜默失敗製造機」是同一件事——**只是這次是 Python 版的**。

#### 順帶發現（尚未處理）

`BoardAnalyzeDaily` 今天 07:00 `LastTaskResult=1`，log 裡兩個原因：

```
error: cannot pull with rebase: You have unstaged changes.
NameError: name 'is_tw' is not defined   → [WARN] valuation_alert failed
```

- **unstaged changes 卡住 pull --rebase**：工作區長期有沒 commit 的產出物
  （`docs/earnings.html`、`state/chain_technicals.json`、`chief_buy_cooldown.json`、
  `valuation_state.json`），每天的 `git pull --rebase` 都會失敗。
- `is_tw` NameError 在 `valuation_alert.py` 之外的模組（該檔本身沒有這個名字），
  時間點在今天早上那次修正之前，**是否已修待實跑確認**。

---

### 2026-09-01 16:30　修掉「每天 git pull --rebase 都失敗」

**現象**（`board_analyze.log` 07:00 那輪）：

```
error: cannot pull with rebase: You have unstaged changes.
error: Please commit or stash them.
```

**根因**：這個 repo 被好幾支排程＋手動執行同時寫，工作區**常態是髒的**
（`docs/*.html`、`state/*.json` 這些每輪重新生成的產出物）。
`git pull --rebase` 遇到未提交變更就直接死。

⭐ **而且它一直是靜默的**——先做覆蓋率檢查（不要只修眼前那支）：

| 檔案 | `pull --rebase` 處數 | 有錯誤檢查嗎 |
|---|---|---|
| `ark_report_biweekly.cmd` | 1 | ❌ |
| `board_analyze_daily.cmd` | 2 | ❌ |
| `buffett_scan_weekly.cmd` | 2 | ❌ |
| `researcher_stock_sync.cmd` | 2 | ❌ |

**7 處全部沒檢查 errorlevel**。所以 rc=1 其實來自別的步驟（valuation_alert 的
`is_tw` NameError），pull 失敗從頭到尾沒有任何人知道。

#### 修法兩層

1. **`--autostash`**（7 處全加）：git 內建的 stash→rebase→pop，治本。
2. **開頭那次 pull 加 errorlevel 檢查**（4 支）：進 `FAILED` 累積器
   （buffett 沒有這個機制，且它 push 階段有 rebase 重試會自我修復，只記 `[WARN]`）。

#### 實測（前後對照，不是推論）

工作區當時真的髒、且落後遠端 1 個 commit：

```
舊：git pull --rebase           → error: cannot pull with rebase: You have unstaged changes.
新：git pull --rebase --autostash → Fast-forward ... Applied autostash.
```

本機修改**完整保留**（6 個檔逐一比對）。`dev_log.md` 一度顯示「有差異」，
查下去是 **LF→CRLF**（行數相同、位元組正好多 3077＝每行一個 `
`），
去掉行尾後內容完全相同。那是 `core.autocrlf=true` 的既有行為，
**任何 checkout/stash 都會這樣，不是 `--autostash` 引入的**。

`.cmd` 語法也另外實測過（`set FAILED` 在括號區塊裡是 cmd 的延遲展開雷區）：
成功的 pull 不加東西、失敗的 pull → `FAILED=[ badpull]` ＋ log 有 `[WARN]`。

#### 順帶：`dev_log.md` 是髒工作區的常態來源之一

它**不在** `board_analyze_daily.cmd` 的 `git add -f reports tw_analysis.json state docs`
清單內 → 永遠不會被自動提交 → 工作區永遠帶著一個未追蹤變更。
這次一併 commit（`47ec5a8`）。

#### 🔴 順帶抓到一個隱私問題（assets-dashboard）

`.gitignore` 寫著「資產資料(個人財務隱私,絕對不進版控) `data/*.json`」，
但我建的備份檔叫 `holdings.json.bak-20260901-155110`——**副檔名不是 `.json`，
規則擋不住**，持股明細真的被 commit 進去過一次。
已撤回並改成整個 `data/` 不進版控。**這個 repo 沒有 remote，所以沒有外洩。**

⭐ 通用教訓：**副檔名式的 ignore 規則擋不住 `.bak` / `.tmp` / `.1` 這類後綴**。
目錄裡全是敏感資料時，就該擋整個目錄而不是擋副檔名。

#### 尚未處理（回報 Leo）

`.gitattributes` 寫了 `*.sh text eol=lf`，但工作區的 `docker/entrypoint.sh`、
`scripts/ci_gate.sh` 實際是 **CRLF**（git status 乾淨 → index 裡存的就是 CRLF）。
這些檔案看起來不屬於本專案（`scripts/` 底下是 feishu/longbridge/tushare 的東西），
在 Linux/Docker 會壞，但如果沒人在跑就沒有影響。**未動，等 Leo 判斷。**

---

### 2026-09-01 17:00　清掉上游 ZhuLinsen/daily_stock_analysis 的 908 個檔案

Leo：「查怎麼混進來的，不需要的就拿掉」。

#### 怎麼混進來的

本 repo 2026-06-27 以 **[ZhuLinsen/daily_stock_analysis](https://github.com/ZhuLinsen/daily_stock_analysis)**
（MIT，中國的「股票智能分析系統」，A股/港股/美股/日股/韓股）為起點建立，
第一個 commit `de488d5` 一次帶進 **920 個上游檔案**。
判讀層 2026-07-31 改為本機 headless claude 後上游程式碼全面停用，但從未清理。

先前那些「查不出來源」的東西一次全部解釋了：
簡體中文的 `.gitattributes`、`docker/entrypoint.sh`、`scripts/` 底下的
feishu/longbridge/tushare、還有 repo 根目錄那個會連鎖 import
`fake_useragent` 的 `main.py`。

#### 🔴 我的第一個判斷基準是錯的

原本用「首次出現在 `de488d5` ＝上游的」，結果**把每天在跑的
`.github/workflows/tw-board.yml` 也列進移除清單**。
原因：`de488d5` 的訊息是「daily_stock_analysis **＋ TW 美股晨會看板**」——
那個 commit 同時包含上游程式碼和 Leo 自建的初版，兩者混在一起。

改用**跟上游 repo 實際比對**才可靠：

```
gh api repos/ZhuLinsen/daily_stock_analysis/commits?until=2026-06-28  → a2f19f6（936 檔）
gh api .../git/trees/{a2f19f6,HEAD}?recursive=1                       → 聯集 1155 條路徑
```

取 clone 當時與現行 HEAD 的**聯集**，是因為上游後來改名/刪除過檔案——
只比 HEAD 會把 `src/services/alphasift_service.py` 這種誤判成「Leo 的」。
（920 帶進 vs 6/27 版 936 檔，數字吻合。）

⭐ **「從沒改過」不能當作「沒在用」的證據**：`tw_report.py` 只有 1 個 commit，
但被 `alert_telegram.py` / `board_html.py` 引用、`weekly-screen.yml` 在跑。
判斷歸屬要看**引用關係**，不是修改歷史。

#### 移除範圍與驗證

908 檔（保留 `.gitignore`——Leo 改過 3 次、`requirements.txt`——Actions 在用）：
11 個全上游目錄、`docs/` 的 109 份上游文件（Leo 的 38 個網站頁面保留）、
`.github/` 的 14 個上游設定（Leo 的 5 個 workflow 保留）、
`main.py` / `server.py` / `webui.py` / `LICENSE` / `README.md` / `AGENTS.md` /
`CLAUDE.md` / `SKILL.md` / `pyproject.toml` / `setup.cfg` / `.gitattributes` 等。

驗證（三層）：
1. **反向引用檢查**——Leo 的 65 支 .py 對 `api`/`bot`/`src` 的 import 數為 0
2. **靜態 AST 掃描**——移除後全數 import 可解析，僅剩的 `db`/`litellm` 兩項
   是移除前就存在的問題
3. **實跑** `portfolio_html.py`（86KB）與 `earnings_index.py`（30KB）產出正常
4. **真 Actions 跑一次** `portfolio-nav.yml` → ✓，**2m43s 降到 1m10s**

移除 `.gitattributes` 沒有造成行尾重新正規化（實查 git status 只有 1 個
無關的 M）。

#### requirements 42 → 9 個套件

清單由 AST import 掃描產生。移掉 efinance / akshare / tushare / pytdx /
baostock / longbridge 等中國券商行情源、lark-oapi(飛書) / dingtalk-stream /
discord.py 等推播 SDK、fastapi / jinja2 / newspaper3k 等。

🔴 **一併移除這行**——每次 CI 都從上游作者的 GitHub 直接安裝程式碼：

```
git+https://github.com/ZhuLinsen/alphasift.git@377049...#egg=alphasift
```

⚠️ `weekly-screen.yml` 不走 requirements.txt（自己 `pip install` 一串），
不受影響。

#### 順帶發現（未處理，待 Leo 決定）

1. **`import db` 必定 ImportError**——`db.py` 在 repo 和本機都不存在，
   但 `buffett_screener.py` 有 2 處呼叫（`get_db_position_tickers` /
   `write_to_db`）、`buffett_sp500.py` 有 2 處。這幾條路徑從沒成功執行過。
2. **repo 是 PUBLIC**，根目錄的 `holdings.json`（監控代號清單）、
   `portfolios.json`（模擬倉）都公開可見。GitHub Pages 免費方案需要 public，
   算是必要代價，但 Leo 應該知道範圍。

---

### 2026-09-01 18:30　復盤官前置：判斷補記基準價 ＋ 臨時單檔評估

Leo：「先做 1、2」。

#### 1. `advisor_verdicts` 補記判斷當下的收盤價

**查出來的洞**：`advisor_verdicts.jsonl` 有 `judgment`／`ts`／`held`／`triggers`／
甚至 `invalidation_price`，**就是沒有判斷當下的股價**。沒有基準價就算不出
「照這個判斷做會賺賠多少」，陳壽無從做起。

⭐ 而且**事後補不回來**：回頭抓歷史收盤價，等於把盤中做成的判斷用收盤價重新定錨。
同一個坑已經踩過兩次（RRG 資金規模、`analyst_price_targets` 都只有現在沒有歷史）。
目前才 89 筆，趁早補。

新增 `price` / `price_asof` / `price_symbol`；抓不到明確寫 `None`
（分得出「當天沒抓到」與「舊版沒這欄位」）。

兩個實作陷阱：

- ⚠️ **`period` 必須維持 `3y`**：`price_store._write_cached` 是 `to_pickle`
  **直接覆蓋不合併**，為了省事寫 `5d` 會把 `industry_rotation`(RRG) 依賴的
  三年快取洗掉。
- ⚠️ **代號格式是混的**（`1229` / `1580.TWO` / `1615.TW` / `NVDA` 並存），
  一律過 `tw_symbol.resolve()`，否則上櫃股靜默抓不到。四種格式全實測通過：
  `1229→1229.TW`、`1580.TWO` 原樣、`2330→2330.TW`、`NVDA` 原樣。

⭐ 實測還驗證了一件事：**NVDA 的 `asof` 是 08-31、台股是 09-01**——
各市場最後收盤日不同，這正是要記 `price_asof` 而不是只記執行日的原因。

#### 2. `ask_advisor.py`：臨時把任一檔丟給軍師團

    python ask_advisor.py 9914           # 台股裸代號自動補後綴
    python ask_advisor.py NVDA --push    # 同時推 Discord
    python ask_advisor.py 9914 --watch   # 登錄失效條件，進每日日檢

⚠️ **不能直接呼叫 `investment_chief.gather_material`**：它的材料全部從既有
state 檔讀（signals / valuation_state / buffett_watch / screen_result），對不在
追蹤清單的標的會一路回「查無」——**看起來有跑，實際沒有判斷價值**。
所以這支**當場算**四個角度再餵同一套雙角度 prompt。

判斷帶 `source="ad-hoc"`：臨時問的（我挑的）跟每日自動觸發的（系統挑的）性質不同，
復盤混在一起統計會失真。

開發時踩到三個，都是實測抓出來的：

1. `fetch_fundamentals("9914")` 裸代號直接 **404**，要傳 `9914.TW`
2. `mansfield_rs` 回的是 `{"short":…, "long":…}` **dict 不是單一數字**
3. `evaluate` 的欄位是 `cheap_price` / `exp_price` / `quality_ok` / `trap_flags`，
   不是我猜的 `cheap` / `expensive`
4. 🔴 **我自己把 `price_asof` 寫死成執行日**——正是同一則訊息裡才剛強調過的錯，
   美股會差一天。已改用實際收盤日。

#### 實測 9914 美利達（Leo 朋友推薦）

    俗價 41.92｜貴價 128.23｜現價 82.1（中段）　品質關 ⬜ 未通過 SKIP
    ROE 近四年 6.2% / -3.6% / 8.2% / 16.5%（現 5.1%）
    EPS 近四年 4.01 / -2.34 / 5.66 / 11.34（TTM 3.44，常利 4.27）
    配息率 116.4%（高於當期獲利）　盈再率 8.6%（ideal）
    毛利率位階 67%（中段）
    SuperTrend 多頭（綠）　RS 30日 -4.46%、250日 -29.78%

兩角度皆「觀望」。趨勢角度點出的矛盾值得記：**SuperTrend 翻多但 RS 長期落後
大盤近 30 個百分點 → 可能只是跟著大盤漲，不是資金偏好的強勢股。**

---

### 2026-09-01 19:30　陳壽-復盤官骨架（A：判斷準確度）

`verdict_review.py`。回答「**這套系統的判斷有沒有價值**」，不是「Leo 有沒有
照著做」——後者要逐筆成交紀錄（Firstrade/IB/台股），Leo 說之後看怎麼匯出。

四個評估設計借鑑上游 `backtest_engine`：評估窗口用**交易日**不是日曆天、
中性帶 ±2%、`first_hit`（失效價有沒有真被摸到，用當日高低點）、對照組。

⚠️ `EVAL_DAYS=20` / `BAND=2.0` **沿用上游預設，還沒校準**，程式碼裡已註明。
累積夠樣本後要照 `chip_scan_thresholds` / `margin_cycle` 的紀律重訂——
先看實際分布的天然斷點。台美股波動不同，最後可能要分開設。

`--backfill` 回填舊判斷的基準價，一律標 `price_src="backfilled"`
（判斷是盤中做的、回填只能取當日收盤，混進統計會讓結果看起來比實際精確）。
實跑 89/89，系統性驗證 0 異常。

**現況：151 筆可評估、0 筆到期、最快還要 17 個交易日。**
樣本不足時仍輸出累積狀況與浮動分布並明講「這不是準確度」——
只印一句「樣本不足」看起來跟壞掉沒兩樣。

#### 🔴 順手抓到第四個「上線但從沒真的執行」

`invalidation_price`（趨勢角度的失效價位）**90/90 全是字串，而且內容是
「無法給出失效價」的說明文字，沒有一筆是數字**。

不是 AI 的錯——schema 定義就是 `{"type": "string"}`，而且孔明自己講了原因：

> 「材料未提供 SuperTrend 的具體翻空價位或 RS 基準線價位，無法給出精確失效價位數字。」

`gather_material` 給的趨勢材料只有**文字描述**（「SuperTrend 多頭」），
沒有那條線的**數值**。所以這個欄位從上線到現在**從沒產出過可用的價格**，
而 `first_hit` 這個評估設計正好依賴它 → 目前 0 筆能算。

**修法很小**：`board_html.supertrend()` 回傳的 `st` 就是線的數值
（實測 2330 現價 2440、線在 2293.77），餵進材料即可。

前三個同型案例見 memory `investment_advisor_architecture`。

#### ⚠️ 外流評估的補漏

今天做 PUBLIC repo 外流盤查時**漏查了 `state/advisor_verdicts.jsonl` 與
`research_notes.jsonl`**——兩個都在版控裡，內容比 `holdings.json` 詳細得多：
90 筆判斷含完整推理，其中 **14 筆 `held=True` 明確標示持有哪些**。

實查：**GitHub Actions 完全沒用到這兩個檔**，只有本機 8 支 .py 在讀
→ 技術上可以移出版控。

⚠️ 但光加 `.gitignore` 沒用：`board_analyze_daily.cmd` 是
`git add -f reports tw_analysis.json state docs`，**`-f` 會繞過 .gitignore**。
兩邊都要改，否則就是「加了 ignore 但沒作用」的靜默失效。
（歷史已公開，移除只能防未來。）

---

### 2026-09-01 20:00　修好 invalidation_price（第四個「上線但從沒執行」）

**根因不是 AI 亂寫**：schema 定義就是 `{"type":"string"}`，而 `gather_material`
給的趨勢材料只有文字（「SuperTrend 多頭」）沒有那條線的**數值**——孔明不是不肯寫，
是真的沒有數字可寫。而 `trade_plan.supertrend_invalidation` 內部
**早就算出 `st["st"]`**（SuperTrend 線的數值序列），只是沒回傳。

改四處：

1. `supertrend_invalidation` 多回 `st_line` / `price` / `gap_pct` / `rs60`
   ⚠️ 一律轉 Python 原生型別——原本回的是 numpy `bool_`/`float64`，
   直接 `json.dumps` 會 TypeError（實測時抓到）
2. `gather_material`：「SuperTrend 多頭」→「**線在 2293.77，現價 2440.0，距離 +5.99%**」
3. `SCHEMA` 新增 `invalidation_level`（`number|null`）＋ prompt 明講只填數字。
   **保留原本的 string 欄位**：兩者用途不同——string 講「為什麼是這個價、跌破代表
   什麼」，number 給程式做 first_hit 比對
4. `verdict_review` 改讀新欄位，保留舊欄位相容

⭐ **schema 改了不等於 AI 會照填**（跟 [[simplified_chinese_guard]] 同一條教訓：
prompt 寫了不等於做到），所以實跑 2330 驗證：

    invalidation_level: 2293.77   型別 float   ← 正是 SuperTrend 線值
    invalidation_price: 「SuperTrend線目前在2293.77，是個股趨勢多空的分界；
                         若價格跌破此線…趨勢角度應直接視為失效轉空。」

順帶確認簡體字驗收機制正常運作（log 出現「⚠️ 2330 判斷含簡體字 价减，重寫一次」）。

⭐ **這個 bug 的形狀值得記**：兩邊都「正常」——`supertrend_invalidation` 算對了、
孔明也誠實說了「材料不足」，**沒有任何一端報錯**。壞在中間那段沒把數值傳過去，
而且欄位型別是 string 所以填什麼都合法。
要不是做復盤官去用這個欄位，不會有人發現它 90/90 都是廢話。


## 2026-09-02（續）趨勢倉 ATR 定案：Wilder vs SMA 5.7 年回測

- 背景：同日顯示層/進出燈號/st_alert 為對齊老墨改成 SMA 版 ATR，順手把 `paper_portfolio._supertrend_dir`（週線趨勢倉）也改了——**沒先讀 `supertrend_backtest_findings`（標「動前必讀」）**。
- 重算：`backtest_trend_2026.py` 參數化（`--start/--end/--trade-start/--atr/--weekly/--pool`），先用 Wilder 重現 2026-07 證據四（QQQ 週線 MDD −17.6%/9 次、日線 −15.5%/45 次，一致），再跑 {QQQ, 守備清單99檔} × {wilder, sma} × {日, 週} 共 8 組。
- 結果：週線趨勢倉設定下 Wilder 報酬略優（QQQ +79.4 vs +75.6；守備清單 +364.6 vs +315.8），SMA 沒有優勢，差距在雜訊範圍。
- 定案：趨勢倉改回 Wilder，做成 `TREND_ATR` 常數（一行可切）；顯示層/燈號維持 SMA。等價測試 200 組隨機序列 `paper_portfolio._supertrend_dir` 與回測腳本 wilder 版方向全一致。
- 修的坑：`yf.download([單一代號], group_by="ticker")` 仍是 MultiIndex，不能用 `len(pool)>1` 判斷；`global` 宣告晚於使用 SyntaxError（8 個背景工作全死，20 秒後才看到）。
- 順帶發現：`chain_select_union` 給 5483/3081 接 `.TW` 404（上櫃股），沒走 `tw_symbol.py`——待修。
- 回測 json 輸出移到 scratch，不進公開 repo。

## 2026-09-02（續二）三指標合流倉 → 進出燈號倉

- 為什麼換：三指標合流要 ST 翻多＋RS30>0＋擠壓剛噴出「同一天」發生，8/18 開倉到 9/2 零觸發、10000 現金全趴著。換成老墨 COMBO 四燈的「狀態」判斷。
- 做法：`rebalance-combo` 不再自己算指標，直接讀 `state/combo_result.json`（combo_scan 07:00 產出、push 後 Actions 09:00 讀），跟 docs/combo.html 同一份資料，頁面與倉不會漂移。超過 4 天沒更新就不動作。
- 規則：進場＝≥3 燈成立且風報比≥1，依「亮燈數↓、風報比↓」排序吃現金，每檔 1/8 倉；出場沿用老墨不對稱兩階段＝ST 翻空賣半（狀態判斷、不重複）、RS60 跌破 60MA（rs_short<0）全出，全出後 7 天冷卻。
- 倉名「三指標合流」→「進出燈號」由 load() 自動改，$10,000 與歷史線沿用（14 個點都是 10000）。舊 combo_signal_events 留著不呼叫。
- 順手修：`is_tw` 只認 .TW，上櫃 .TWO 會被當美金——combo_result 的 symbol 是走 tw_symbol 的，會有 .TWO；`STORE` 可用 PORTFOLIO_STATE 環境變數改，旁邊真跑不碰正式檔。
- 驗證：scratch 複本真跑 → 正式檔 md5 不變、買到 UEC/PAAS/UHS/TM/BAYRY/SSUMY/1504.TW/MSFT 正是打點清單前 8、各 12.5%。之後正式跑一次、產賽馬頁、推上去。
- 沒有歷史回測：目標價沒有歷史，風報比算不回去。這個倉跟原本一樣是累積樣本的器材。
- 注意：風報比排序偏好「ST 線貼近現價」的標的（分母小，UEC rr=15 就是這樣），這是規則本身的性質。

## 2026-09-02（續三）進出燈號頁：類股象限欄位 + 篩選鈕改站內標準

- Leo 問「進出燈號前要不要多看產業輪動」→ 結論：不當門檻（燈四已是個股層級 RS；9/1 三週期象限互相打架，美股 20 類股只 3 個一致；個股原本對不到 TV 類股）。Leo 定案：做成顯示欄位＋顏色＋篩選鈕，不進燈號。
- 個股→類股：用 TradingView 篩選器**按代號查**（跟輪動頁 `_sector_members` 同源，類股名稱與 `industry_rotation_history.json` 的鍵 100% 對得上；OTC ADR 也有），快取 `state/sector_map.json` 30 天。179 檔對到 170，缺的 9 檔全是 ETF。象限讀輪動歷史最新快照（index 基準），顯示 60 日、tooltip 給 20/60/120＋細分類。
- 頁面按鈕同步站內標準（Leo：「同步投資網站的設計風格跟按鈕方式」）：拆掉這頁自己寫的 `.cbfilter/.fbtn`，改用 `board_theme` 的 `.ctrl`（置頂）＋`.seg`（市場）＋`.sc`（圓角籤，色點＋計數）。計數語意跟看板頁一樣＝「其他組維持現選、改選這顆會剩幾檔」，點了會連動更新。象限四色直接 import `industry_rotation.QUADRANT_COLOR`，兩頁永遠同色。
- 今日分布：77 檔 COMBO 成立中 60 日象限 領先 34／弱化 23／落後 8／改善 5／無分類 7；36 檔打點裡落後只有 5。
- 驗證：本機 http.server 端出 docs/ 用瀏覽器實開，點「落後」→ 8 檔、其他組計數連動、空區塊收起；`.fbtn` 殘留 0。`.claude/launch.json` 放在 `C:\Users\Mophy\AI\`（不是 repo 內）。
- 未併入：`industry_rotation.py` 與兩個 history json 仍是稍早 RRG 每日化的未提交修改（frames_tw 56 個日期問題未解），不混進這個 commit。

## 2026-09-02（續四）產業輪動：52 幀硬上限的真因 + 幀資料緊湊化 + 改每日跑

- 「台股 frames 只有 56 個日期」真因：`_frames_data(max_frames=52)` 是週頻時代「52 週＝一年」的硬上限，9/1 回填改每日後沒跟著改，歷史檔 250 天但頁面只切最後 52 格。教訓：改單位（週→日）要 grep 所有寫死的 52/26/13，不只改自己看到的。
- 直接放大到 250 幀會讓頁面 6MB→27MB（每點物件 key/name/quadrant 每幀重複）。做 `_compact_frames()`：序列化時壓成 `[鍵索引, ratio, momentum, size百萬, radius]` 陣列、key/name 只存表頭；前端 `rrgExpand()` 在任何讀 RRG_DATA 的程式之前原樣還原，其餘 JS 不動；quadrant 由 ratio/momentum 重算。Python 端（_axis_bounds）仍拿完整物件。壓縮/還原對照測試通過。
- 實測：250 幀 ×（粗 20+17 籃、細 109+76 籃）× 2 基準，整頁 6.32MB（原 5.99MB）。瀏覽器實開：250 幀、物件格式正確、圖表正常、console 0 錯誤。
- 排程：原本只有週六 Actions 跑，「現在」泡泡最多舊 6 天。加進 `board_analyze_daily.cmd` 07:00，排在 researcher_industry 之前，兩個 history json 一起 git add。頁面副標改「每個交易日 07:00 更新」（明早重產後生效）。
- 9/1 每日化的原始碼（step 5→1、250 點、tailDays、速度/直線度）之前一直沒 commit，線上頁面卻已是它產的；已補 commit 8f054e0。

## 2026-09-02（續五）圖表統一到全站 + 對齊配對式排版 + 縮放平移

- 背景：Leo「圖表格式樣式可以統一嗎？產業鏈、燈號、財報分析」——board.html（產業鏈看板）原本自己另寫一套簡化版圖表（純收盤線＋Wilder SuperTrend紅/綠），跟燈號/財報頁共用的 technical_indicators.py（今天已改蠟燭圖+黃紫配色）長得不一樣。
- 統一做法：board_html_legacy.fetch_us_charts 加開盤價、SuperTrend 改 SMA（跟顯示層對齊）、算雙重颱風三態；board_html.py 台股段同步；board.html 的 JS drawChart() 改真的蠟燭圖，沒有開盤價（tw_analysis.json 這次才補，要等下次排程）時優雅退回原本收盤線，不會整段掛掉。tw_analyze.py 補 opens 欄位。
- 實測：board.html 美股（MSFT）蠟燭圖成功；台股（2382，還沒有 opens）正確退回收盤線、無錯誤。財報卡（--no-llm 測 MRVL，零成本，只是本機 headless claude 上網查法說會摘要，走 Max plan 額度不算現金支出）蠟燭圖與並排字卡都正常。
- Leo 追加：「文字可以跟圖表上下對齊嗎？可以做放大縮小功能？調整橫軸？」——
  - 對齊：原本左邊 5 張字卡整欄疊、右邊 4 張圖整欄疊，各自堆疊行數不同步、越往下越錯位。改成配對式 `.techrow`（SUPER TREND+雙重颱風K線 對 價格圖、成交量卡 對 成交量圖、EXCEED CHARGE卡 對 動能柱圖、RS卡 對 RS圖），一組一列，兩欄永遠對齊。
  - 縮放/平移：四張圖統一改成同一個 linear index x 軸（原本 cv/c2/c3 是 category 軸），用 chartjs-plugin-zoom（Chart.js 官方組織維護，跟已用的 chartjs-chart-financial 同信任層級）加滾輪縮放＋拖曳平移，`onZoom`/`onPanComplete` 互相同步其餘三張圖的 x.min/max，加「重置縮放」按鈕。
  - 踩坑：拖曳平移完全沒反應——chartjs-plugin-zoom 的 pan 其實是靠 Hammer.js 辨識手勢（peer dependency），沒載入它 pan 那條路徑根本沒啟動（滾輪縮放不受影響，那條路徑不經過 Hammer）。補 hammerjs@2.0.8 CDN（MIT，2016年最後一版但月下載740萬次，Chart.js 官方 zoom 外掛指定依賴，純前端手勢辨識不碰資料）後拖曳同步正常。
  - 驗證：滾輪縮放（真實 wheel 事件）、拖曳平移（真實 mousedown/mousemove/mouseup 序列）、重置縮放，四張圖 x.min/max 全程一致，逐一測過。
- 順手：portfolio_html(_legacy).py 不 import technical_indicators、用不到蠟燭圖外掛，先加了又移除，維持精簡。
- 全部載外掛的頁面：board_html.py / board_html_legacy.py / combo_html.py / earnings_infographic.py，CDN 順序 chart.js → chartjs-chart-financial → hammerjs → chartjs-plugin-zoom。
- 正式重產：board.html（MSFT 蠟燭圖確認）、combo.html（77 檔全部，308 個 techrow 配對正確）、ark.html（重跑補上 combo/chip 導覽連結，零成本，只是 yfinance+本機 headless claude 法說會摘要，沒有 Anthropic API 花費）。earnings.html 是索引頁不含圖表；28 檔個股卡片維持現狀，下次該檔案自然更新（財報公布或手動指定）才會套新樣式，避免為了改版樣式觸發全部 28 檔的敘事重算。

## 2026-09-02（續六）判斷失誤：誤以為財報卡敘事層要花錢，其實走本機 Max plan

- Leo 給截圖指出「財報分析的圖表沒改到」——查了才發現：稍早我判斷「28 檔財報卡若要重新產生會觸發 AI 敘事層的付費 API 呼叫，所以不主動全部重跑，等下次自然更新才套新樣式」，這個判斷沒有查程式碼，是憑印象猜的。
- 實際查 `earnings_infographic.py`／`llm_board.py`：AI 敘事層 `narrative()` 跟法說會摘要 `EC.build()` 預設都走 `BOARD_LLM=claude`（本機 headless Claude Code，Max plan 額度），Gemini 只是不可用時的備援。**兩次 AI 呼叫都不是現金支出**，跟稍早我以為的「花錢」正好相反。
- 修正：直接背景重跑全部 28 檔財報卡（含完整 AI 敘事層，不用 `--no-llm`），零現金成本，只是花時間。
- 教訓：「這會不會花錢」是可以直接查程式碼（`BACKEND = os.environ.get("BOARD_LLM", "claude")`）確認的事實，不該憑「AI 呼叫＝花錢」的刻板印象跳過查證就下判斷保守行事——保守過頭一樣是誤判，會讓使用者看到不一致的半吊子結果（部分頁面新樣式、部分舊樣式）。跟 `feedback_verify_before_hardcoding` 同一類：先查是不是查過的事實，不是「我覺得我知道」。

## 2026-09-02（續七）lamp_lookup.py：查任意股票的燈號 + RRG 象限（Discord bot 前置）

- Leo：「進 discord 輸入某隻股票，跑出 rrg 狀況跟燈號」。查了才發現 Discord 目前只有 webhook 單向推播，沒有監聽使用者輸入的能力；真正有互動指令的是 TradingBot 那邊的 Telegram bot（/menu）。跟 Leo 確認後，仍要做 Discord（不是改接 Telegram），代表要從零申請 Discord Bot Token、寫常駐監聽程式——這是後續步驟，Leo 需要先去 Discord Developer Portal 申請。
- 先把不依賴 Bot 架構的核心邏輯做好：`lamp_lookup.py`，輸入任意代號回四燈+風報比+RS+類股象限，複用 combo_scan.py 同一套判定（不重算第二份、不會跟燈號頁漂移）。
  - 先查 `state/combo_result.json` 今天的快取（守備清單/持股/自訂觀察 179 檔）命中就秒回。
  - 沒命中（真正任意輸入）才即時抓算，走跟 combo_scan.scan_one() 同一條路，只算這一檔。
- 實測：2454（快取命中）秒回；COST（即時查詢，不在清單裡）2.77 秒；BRK.B 正確轉 BRK-B；亂打代號優雅回「查無資料」不出錯。
- 已知限制：即時查詢路徑沒有公司中文/英文全名（母體清單的 name 是預先存好的，即時查詢沒有等價免費來源）——小瑕疵，先不補。
- 下一步（等 Leo 申請好 Discord Bot Token）：寫常駐監聽程式（discord.py），部署位置待定（獨立輕量 Zeabur service，或掛進 TradingBot 現有服務——待討論），指令邏輯直接呼叫 `lamp_lookup.lookup()` + `format_discord()`。

## 2026-09-02（續八）三國軍師團下一階段路線圖（規劃，未動工）

Leo 一次問了風險官（仲達）、復盤（陳壽）、Discord 即時討論、投顧報告失效線、交易紀錄+下單理由五件事。查了 mofi_warroom_structure 記憶（8/27 已定名四個角色）、investment_chief.py 現有的失效條件欄位、ib-dashboard 專案現況、13 份投顧報告 PDF（`C:\Users\Mophy\Documents\Investment\投顧報告\`），完整規劃寫進新記憶 `advisor_next_phase_roadmap.md`（分五項，各自階段步驟、依賴關係、Leo 要做什麼／我要做什麼）：

1. **Discord Bot**（地基，其他四項的共用出口）——查詢邏輯 `lamp_lookup.py` 已完成測試，等 Leo 申請 Bot Token。
2. **advisor.db decisions/executions 交易紀錄表**——理由跟成交分兩張表（借鑑老墨戰情室的「對帳落差」設計），待 Leo 確認 IB 帳戶 Pro/Lite、查統一證券能否匯出 CSV。
3. **風險官 P1（失效條件日檢）+ P2（大盤總開關，電金比要等 2026-11 才有 100MA）**。
4. **陳壽 B 型（交易紀律復盤）**——依賴第 2 項，排最後。
5. **投顧報告失效線監控器**——實測抽取國泰金 2882 那份 PDF（目標價 117／前次 93、2026F/2027F EPS 8.27/8.61、風險因子），格式標準化可用，沿用 investment_chief.py 現成的失效條件 schema。

建議優先順序（未拍板）：1 → 2 → 3 → 4/5。這五項今天都還沒動工，只有 lamp_lookup.py 是查詢功能的前置已完成。完整內容見記憶檔案，不重複貼在這裡。

## 2026-09-02（續九）earnings_watch.py 靜默失效修復完成

- 修復驗證：17 檔台股財報卡用正確方式（抓資料帶 .TW、檔名去後綴）重跑，17/17 成功，無孤兒檔殘留，索引頁連結全部對得上，抽查 3037 蠟燭圖/對齊/縮放外掛都正常。
- 28（美股+首輪台股）+17（台股修正版）共 45 次卡片產出，含 AI 敘事層跟法說會摘要，全程走本機 headless claude（Max plan 額度），零現金支出。
- 這個 bug（8/31 起台股財報卡自動更新持續失敗但排程不中斷）比今天的樣式更新本身更重要——`earnings_watch.py` 每日排程從 8/31 起產出的「財報卡已更新」推播其實都是誤報（`make_infographic` 回 None 時該路徑直接跳過不推播，不算誤報，但代表財報卡片本身已經 3 天沒真的更新過，過這幾天財報公布的台股可能只有卡片沒配上最新數字）。已修復並全部重新產生。

## 2026-09-03 產業鏈看板加縮放 + 導覽列改順序

**產業鏈看板縮放**（Leo：「產業鏈也能做縮放嗎?」）：board.html 用自己那套 drawChart/drawExtra，昨天加的 zoom 沒自動套過來（CDN 已載，只差 options）。三張圖要同步得先統一 x 軸——主圖昨天改蠟燭圖時已是 linear index，動能柱/RS 還是 category，這次一起改成 {x:i,y:v} + linear（category 跟 linear 的 min/max 語意不同，混用同步不了）。加 ZOOM_GROUP 收同一檔三張圖、syncZoom 防迴圈旗標、每組一顆「↺ 重置縮放」。
- 驗證：美股 MSFT 派發真實 wheel 事件 → 0-251 縮到 11.99-237.89，三張圖範圍一致（差 2e-15 浮點末位，我第一次的嚴格字串比對誤判成 false，改容差比對確認同步）；重置鈕按下三張圖回 0-251；台股 2382 同樣通過。

**RS 核對**（Leo：「rs 對不起來?」）：查證結果是**基準指數不同，不是算錯**。老墨那張 MSFT 圖右上角下拉選單是「加權指數」（台股），我們用 ^GSPC。同一批資料重算：^GSPC → 短線 +3.95%／長線 +1.44%；^TWII → −1.26%／−22.95%，對得上他畫面的 −2.1／−19.35（殘差是日期差一天＋他「短：一月」可能是曆月非 30 交易日）。其他項目本來就對得上（多方支撐 481.41 vs 481.35、9/01 量 21,032,500 vs 21,046,893）。Leo 決定不換基準。
- ⚠️ 順帶查到真問題：卡片顯示「成交量 1.5M」是**盤中未完成 bar**（昨晚產卡時美股 9/02 剛開盤），9/02 收盤實際 6.6M。就是 stale_source_swap_fallacy 記過的「盤中 bar 日期是今天但數字沒定案」。尚未處理，待辦。

**導覽列順序**（Leo 指定）：產業輪動→進出燈號→籌碼異動→策略賽馬→產業鏈→財報分析→GDP→ARK。首頁維持第一（站台慣例）；巴菲特清單 Leo 沒列到，先擺八項之後待確認。
- 改 board_theme.NAV（治本）+ 寫 renav.py 對既有 10 頁做 navlinks 區塊字串替換（讓線上立刻生效）。**不整頁重產**：rotation 要重抓全市場約 20 分鐘、ark 約 10 分鐘，而這次資料一個字都沒變，重跑純屬浪費。28 檔財報個股卡沒有導覽列，不受影響（省下最貴的一輪）。
- 驗證：10 頁順序完全一致、每頁剛好 1 個 cur 且是自己、連結數 10→10 沒漏。

## 2026-09-03 老墨「AI 供應鏈三段時程」查證報告 + Discord 盤點

- Leo 給老墨的三時段趨勢表（2026-27 供給瓶頸／2027-28 效率分化／2028+ 下一段架構），要求補完整＋佐證＋整合方案。用 mckinsey-report skill 的 SCQA＋金字塔寫成報告：`報告檔\老墨產業趨勢三段時程\老墨產業趨勢三段時程_完整報告_20260903.md`，副本在 obis 04_AI Report/Investment。
- 查證結論：三段時程全部有外部佐證（TSMC N2 五廠爬坡／HBM4 2026 產能預訂完、缺到 2027H1／1.6T 2026 大量出貨／M9 CCL 卡在 Nittobo 玻纖布；A16 晶背供電已滑到 2027、800V HVDC 3Q26 首批出貨、MCL 微流道 3kW；CoPoS 2H28、Feynman CPO 2028、SpaceX 軌道算力 2028 規模化）。
- ⭐ 關鍵發現：21 檔點名台廠**用代號比對**只有 5 檔在守備清單（欣興/景碩/鈦昇/台積電/上詮），覆蓋率 24%。第一次用中文名比對全部 ❌ 是錯的——screen_result 存英文全名；改用 `_tw_chinese_names()` 名→代號再比代號才對。缺口集中在兩塊我們沒有的鏈：「AI 材料／被動元件」（記憶體/CCL/MLCC/功率）、「AI 電源／散熱」（台達電/光寶/貿聯/健策），加上玻璃基板鏈沒收面板廠路線（群創/友達/TPK/群翊/山太士）。山太士查無代號待 Leo 確認。
- 整合方案五項（報告內建議行動表）：補自訂觀察清單 16 檔（零成本但會擴大燈號倉買進母體，要 Leo 點頭）→ 新增兩條鏈 → 玻璃基板鏈擴成分 → 看板時程軸 → 驗證日程接風險官 P1。
- Discord 盤點：這幾天的產出 HTML 全上了；唯一缺口是進出燈號倉買賣動作沒推 Discord，已補（`state/lamp_trades_today.json` → 日報第②段，一天延遲標日期，無動作也寫檔以區分「沒交易」與「沒跑」）。

## 2026-09-03（續）老墨三段時程報告：行動 1–3 落地 + 白話版

- **更正**：昨報告寫「16 檔缺口」算錯——21 條點名裡 5 條是產業類（記憶體/CCL/銅箔/MLCC/功率元件）沒有個股；17 檔個股裡 5 檔在守備清單、**3 檔（台達電/群創/群翊）其實早在 screen.py TW_POOL 只是本週沒過篩**、9 檔真的不在系統（光寶科/貿聯/健策/昇陽半/友達/TPK/大立光/光聖/山太士）。教訓：`screen_result.json` 是篩選後的子集，「在不在守備清單」要看 `screen.py` 的池子，不是看 screen_result。
- **行動 1**：10 檔加進自訂觀察清單（`combo_scan.py --add`，只寫清單不掃描）。山太士查無代號待 Leo。清單同時是進出燈號倉買進母體，Leo 明確說「做 1-3」才動。
- **行動 2**：第 10、11 鏈「AI 材料/被動元件」（2408/2344/2383/6213/6274/8358/1815/2327/2492/2481/8028；美 MU）、「AI 電源/散熱」（2308/2301/3665/3653/3017/3324；美 VRT/ETN/MPWR/NVTS）。成分來源：CCL 三雄與 HVDC 供應商是報告引用的 DIGITIMES/TrendForce 點名，DRAM/MLCC/銅箔/玻纖布/功率是一般已知龍頭（守備池成分不是判讀規則，錯的代價是多盯一檔，可接受）。登記四處：screen.py 池、board_html_legacy CHAIN_ORDER/CHAIN_ICON、board_html ICONS；CHAIN_MAP 讀 screen_result 不用登記；RACE_EXCLUDE 不動→新鏈進賽馬從加入日起跑。
- **行動 3**：玻璃基板/TGV 補友達 2409、TPK 3673（群創、群翊本來就在）。
- chain_themes.json 兩條新鏈的題材層**手寫**（內容取自報告已查證來源）——chain_themes.py 走 Gemini 付費 API、週六 Actions 才跑，不為兩條鏈手動觸發付費呼叫；週六會被 Gemini 版覆蓋。
- 本機重跑 `screen.py --market both` 產新 screen_result.json（零成本）。⚠️ 看板要到**明早 07:00** tw_analyze/us_analyze 跑完才會出現這兩條鏈（board_html 對「沒個股判讀也沒深度報告」的鏈直接跳過）。
- 白話版報告：`老墨產業趨勢三段時程_白話版_20260903.md`（obis 也有）。OpenCC 驗收唯一命中「划」是「划算」正常台灣用法，不是簡體。
- 行動 4（看板時程軸）、5（時程風險做成驗證日程）未做：5 併入 advisor_next_phase_roadmap 第 3 項（風險官 P1 失效條件日檢＝同一個機制）。

## 2026-09-03（續二）山太士 + 行動 4/5 + 家人版 + 觀察名單去向

- **A**：山太士＝3595.TWO，加進自訂觀察清單與玻璃基板/TGV 池。大立光 3008、光聖 6442 補進矽光子/光通訊池（老墨 2028+ CPO/光學路線；上詮本來就在）——這樣 Leo 要求加的每一檔都落在某條鏈，不只在自訂清單。⚠️ 這兩檔是在本機 screen.py 跑完之後才加的，要等週六 Actions 重篩才進 screen_result。
- **本機 screen.py --market both 跑完**（約 35 分鐘，零成本）：screen_result 日期 2026-09-03，AI 材料/被動元件 台 8／美 1、AI 電源/散熱 台 6／美 4、玻璃基板 8 檔（含友達/TPK/群創）。⚠️ 第一次等待用 grep "Error" 當結束條件被 yfinance「HTTP Error 404」雜訊誤觸，改用「程序結束」才對——等長任務別用 log 關鍵字當 sentinel。
- **行動 4 看板時程軸**：`board_html_legacy.CHAIN_PHASE`（單一來源，跟 CHAIN_ORDER 同處）→ board_html 每條鏈標題旁 `.phase` 標籤（⏱ 2026–27 供給瓶頸 / 2027–28 效率分化 / 2028+ …）。只標老墨表有涵蓋的 6 條鏈，其餘不標。
- **行動 5 驗證日程**：`state/roadmap_milestones.json` 11 筆（id/due/chain/claim/verify/src/status），`roadmap_milestones.py` 列出／更新狀態（pending/confirmed/slipped/broken）；daily_warroom 第⑤段到期前 21 天列「📌 驗證日程」、逾期未驗持續提醒。**行事曆型、零成本、不自動上網查證**（那是付費呼叫）——驗證在對談中做完用工具記回去。跟風險官 P1 是同一個「每天對照條件」機制，等 P1 做時把這份併進去。
- **D 觀察名單去向（查證）**：研究員/投資長的母體＝①守備清單與持股的訊號翻面（來自 screen_result 的鏈成分）②產業層翻象限時該鏈成分（非持股只收轉入改善/領先）③巴菲特到俗價；**自訂觀察清單只餵進出燈號頁/燈號倉，不進研究員/投資長**。所以「加進鏈」才是讓研究員/投資長看得到的路——今天加的每檔都已落在某條鏈。
- 家人版報告：`老墨產業趨勢三段時程_家人版_20260903.md`（7,600 字，完整證據＋白話，obis 也有）。

## 2026-09-03（續三）交易記錄 v1：trade_journal.py（路線圖第 2 項 decisions 表）

- Leo：「想寫交易記錄，html 還是 discord 方便？」＋給了今天四筆（IB 買 MSFT/MRVL/CEG、統一掛單 2454×5）。結論：**輸入用打字**（現在跟我講、Bot 上線後 Discord `!trade`），**檢視用私人 HTML**（obis `04_AI Report/Investment/交易紀錄.html`，手機開得了、不在公開網站）。
- `trade_journal.py quick "<券商> <buy|sell|order> <代號> [量] [@價] | 理由、理由"`：每筆自動附**系統當時的看法**（燈號數/亮哪幾燈/風報比/RS60/類股象限/最近 3 天投資長判斷）——沒有這個快照，三個月後復盤只剩「當時覺得不錯」。`fill <id> --qty --price` 對帳單回填、`cancel`、`list`、`html`。
- 🔒 `state/trade_journal.jsonl` 進 .gitignore（跟 advisor_verdicts 同一批），`git check-ignore` 驗過；HTML 不進 docs/。
- IB 截圖回推成本（P&L÷股數）：MSFT 1@497.03、MRVL 3@210.46、CEG 2@280.28，跟系統快照價對得上；標「回推」，Leo 對帳單確認後可 fill 覆蓋。2454 掛單 pending。
- 快照已經看得出復盤素材：MRVL 理由「產業鏈評分高」但當下象限落後、RS60 −11.6、投資長無判斷；MSFT 四燈全亮＋象限領先＋投資長續抱。這不是現在下結論，是留給陳壽 B 型比對。

## 2026-09-03（續四）discord_bot.py：路線圖第1項階段2（即時查股殼）

- 背景：Leo 開另一個視窗要做隆中對的 Discord bot，要「可以即時送出我想查的股票」——正是 `advisor_next_phase_roadmap` 記憶排定的「Discord Bot」第1項階段2（`lamp_lookup.py` 已於 9/2 寫好測試過，等 Bot Token）。
- 做了：`discord_bot.py`——`/查 代號:2454` slash command，呼叫既有 `lamp_lookup.lookup()`+`format_discord()`，不重造判定邏輯。`interaction.response.defer()`+`asyncio.to_thread()` 處理即時抓算 3-8 秒（lamp_lookup 文件明講不能同步等），拿到 15 分鐘 followup 窗口而非 Discord 互動預設的 3 秒逾時。
- 刻意只用 slash command、不讀一般訊息：`discord.Intents.default()` 就夠，不用申請 `MESSAGE_CONTENT` 這個 privileged intent，Leo 階段1（申請 Bot）可以少勾一步。
- `requirements.txt` 補回 `discord.py`——⚠️ 9/1 才把上游帶的 discord.py 當死依賴移掉，這次是真的在用，之後清依賴前要先 grep `discord_bot.py`。`.env` 補 `DISCORD_BOT_TOKEN`/`DISCORD_GUILD_ID` 兩個空值佔位（都還沒填，階段1還沒做）。
- 驗證：`import discord_bot` 正常、command tree 註冊不報錯、無 token 時 `main()` 正確印錯誤訊息並回傳非 0（不會裝作成功）；`lamp_lookup.py 2454` 對照原邏輯仍正常（3/4燈、風報比2.27、弱化象限）。**還沒實跑 Discord 端**——沒有 Bot Token 沒辦法連線測試。
- 部署位置留給 Leo 選（本機常駐排程 vs 獨立 Zeabur service，兩種都寫進檔頭 docstring）：這是要保持 WebSocket 常駐的服務，跟 07:00 batch 排程性質不同，不能塞進 `board_analyze_daily.cmd`。
- 待 Leo：去 Discord Developer Portal 建 Application+Bot、拿 Token 填 `.env`、用 OAuth2 URL Generator（勾 `bot`+`applications.commands`）邀進隆中對伺服器，才能實測 `/查`。

## 2026-09-03（續五）命名：bb9e = 投資相關全部（含本專案）

Leo 定案聊天代稱：「bb9e」指投資相關工作全部，涵蓋本專案（`daily_stock_analysis`／
隆中對）＋ `TradingBot`——bb9e 其實是 TradingBot 的 Telegram bot（`@DT_tradebot`）
原本就有的顯示名稱，這次借來當投資側的統稱，跟本專案是完全獨立的 repo/資料庫/
部署，只是共用聊天代稱。跟 bb8（BB-8 個人助理）分開，避免聊天講 bb 開頭的代稱時
兩邊搞混。詳見全域 `~/.claude/CLAUDE.md`「專案代稱」段落與記憶 `project_codenames.md`。

## 2026-09-03（續四）家人版簡報 PDF + 完整報告 PDF（風格 A 暖紙感）

- Leo：「用 skill 做成簡報 pdf（附說明圖）＋完整報告 pdf，設計風格先看」→ 先出 A（暖紙感）／B（夜間深藍）兩風格樣板各三頁，Leo 選 A、簡報要橫式、頁數可加。
- 做法：`報告檔/老墨產業趨勢三段時程/deck_A.html`（20 頁 16:9，`@page{size:1280px 720px}`）、`report_A.html`（A4 直式流式排版）＋ 9 張 inline SVG 示意圖（三段時程軸／玻纖布→CCL→PCB 供應鏈／800V 供電對比／微流道散熱／ABF 夾心／CoPoS 圓改方／CPO 光纖到晶片／17 家覆蓋方塊／晶背供電時程滑動）。轉 PDF 走 **Edge headless**（`render_edge.py`，跟 md_to_pdf_tool 記憶同一條路：零安裝、CJK 完整），另存 PNG 抽樣人工檢查。
- 產出：`老墨三段時程_簡報_20260903.pdf`（20 頁，960×540pt 橫式）、`老墨三段時程_完整報告_20260903.pdf`（12 頁 A4），obis `04_AI Report/Investment` 各一份。
- slides skill 的 HTML template 是給網頁互動簡報用的（Chart.js、導覽鈕、transition），做 PDF 只借它的版型觀念（一頁一重點、Action Title、metric card），不用它的 JS。

## 2026-09-03（續六）discord_bot.py 實跑驗證通過（路線圖第1項階段2 完成）

- Leo 完成階段1：Discord Developer Portal 建好 Application+Bot、OAuth2 URL Generator 勾 `bot`+`applications.commands`（權限勾傳送訊息/使用斜線指令）邀進隆中對伺服器，把 Token 直接貼給我。
- 伺服器 ID 沒有讓 Leo 手動去開發者模式複製——改用拿到的 Token 呼叫 Discord API `GET /users/@me/guilds`，bot 剛好只在一個伺服器裡，直接讀到 ID `1542432712852377630`，兩個值都寫進 `.env`。
- **實跑抓到一個真 bug**：第一次啟動，log 顯示「指令已同步到伺服器：0 個」——`/查` 是用 `@tree.command()`（無 `guild=`）宣告的全域指令，只呼叫 `tree.sync(guild=guild)` 不會把全域指令帶進去，那只同步「本來就綁定這個 guild」的指令。要先 `tree.copy_global_to(guild=guild)` 複製一份才會生效。改完後查 Discord API（`GET /applications/{app_id}/guilds/{guild_id}/commands`）直接確認真的有 1 個指令註冊上去，不是只信 log 說成功。
- 順手修：`nohup ... &` 背景啟動時 `print()` 預設是整批緩衝（不是 tty），log 檔遲遲看不到 on_ready 那兩行，一度誤以為卡住——改用 `python -u`（無緩衝）+ 每個 print 加 `flush=True` 解決，之後偵錯不會再遇到「明明在跑但 log 看不到」。
- **端到端驗證通過**：Leo 在隆中對伺服器實際打 `/查 代號:2454`，收到「聯發科 現價4315.0 四燈🟢⚫🟢🟢(3/4) 風報比2.27⭐打點成立 RS60 6.11% 弱化(電子科技)（今日掃描快取）」，跟 9/2 `lamp_lookup.py` CLI 測試結果完全一致。
- **目前只是暫時跑起來測試**：這個 process 是背景手動啟動，不是常駐服務，機器重開或這次對話結束就會停。下一步待 Leo 選部署方式（本機工作排程器常駐 vs 搬去 Zeabur 跟 `meeting_summary_bot` 同帳號）——路線圖第1項到這裡才算真正上線可長期用。

## 2026-09-03（續五）推送總覽 + Bot 功能 → Discord #持股密報

- Leo：「discord 幫我推送新的推送總覽跟 bot 功能到個人記錄區」→ 個人記錄區＝私人頻道（DISCORD_WH_PRIVATE）。一則訊息（戰情室 persona）列：Telegram／#每日戰情五段（含新加的 🚦燈號倉動作、📌驗證日程）／#持股密報／#財報／網站更新時間與新鏈／Bot `/查` 與下一個 `/交易`／obis 私人檔案位置。送達證據：message id 1544780964633378817（return_ids=True 拿回來的，不是看發送端 log）。

## 2026-09-03（續六）推送總覽重貼到正確位置（#每日戰情 釘選）

- 教訓：Leo 的「個人記錄區」＝ `discord_guide.py` 推到 #每日戰情 給他釘選的那幾則總覽（8/31 建立，`state/discord_pinned.json` 記 id），不是持股密報、也不用開新頻道。我先推到持股密報（已不存在）、又誤開 `notes` 頻道位置（已撤）。⭐ 以後「推送總覽／Bot 功能」這類文件一律改 discord_guide.py 重跑，它本來就是為此設計的單一維護點。
- 更新 SECTIONS 到 9/3 狀態（#每日戰情五段實際標題含 🚦燈號倉動作／📌驗證日程、Bot `/查`與下一個 `/交易`、網頁導覽順序與時程標籤、兩條新鏈、財報卡修復註記），WHATS_NEW 換成 9/2–9/3 七項。共 2 則（1803＋190 字元），id 1544782867811278949／1544782875822526474。OpenCC 命中「布」是玻纖布/公布正常用字。

## 2026-09-03（續七）「個人記錄區」到底在哪：查證結果

- 用 Bot token 列伺服器頻道：#每日戰情／#財報／#軍議／#持股密報／**#個人記錄區(1542610510875328582)**——它是獨立頻道，不是我以為的持股密報、也不是 #每日戰情 的釘選。三個 webhook 各綁：daily→#每日戰情、earnings→#財報、private→#持股密報；8/28「使用說明」與 8/31、9/3 兩版總覽都是 daily webhook 發到 #每日戰情。
- #個人記錄區 是私密頻道：Bot 讀取/列 webhook 都 403，沒有任何既有 webhook 綁它，所以 Leo 說「研究員先前推過 Telegram 專屬清單」那則我從程式端查不到——可能是 8/28 當時的 webhook 在 9/1 憑證輪替時被撤了，或是手動貼的。查不到就說查不到。
- 處理：notify_discord 放回 `notes` 位置（讀 DISCORD_WH_NOTES），discord_guide 預設改發 notes、沒填時退回 daily 並印警告。待 Leo 在 #個人記錄區 建 webhook 填 .env。
- ⭐ 教訓：頻道歸屬要用 webhook 的 channel_id／Bot 的 guild channels 查，不要用「私人的＝private」這種名字推論；今天為此來回三次。

## 2026-09-03（續八）discord_bot.py 部署定案：本機常駐，不放 Zeabur

- Leo 問「Zeabur 先前才外流過我的 api key，搬上去有什麼風險？」——查了 `security_incident_2026-08-28` 記憶完整內容：8/27 根因是 **Zeabur 平台自己內部服務憑證被偷，攻擊者連進他們資料庫一次撈走所有客戶的環境變數**（不是 Leo 的憑證管理問題），且 Zeabur 官方揭露不完整（信只列 2 把 key，實際外流範圍更大）。評估這次要放的 `DISCORD_BOT_TOKEN` 跟 Anthropic key 風險量級不同：查證過 `lamp_lookup.py` 整條依賴鏈不吃任何 API key，且 Discord bot token 範圍限定（只能在隆中對一個伺服器冒充發言，沒給讀訊息/管理伺服器權限），外洩可自助 2 分鐘重置，跟能直接燒錢的 Anthropic key 不同量級。給了「有條件可用」的評估，但 Leo 最終選**本機常駐**（規避這個風險類別，不用再評估）。
- 同時討論了「查任意股票」能不能做成純網頁——結論：179 檔內（今日快取）可以做成純靜態網頁零風險，但**任意輸入的股票一定要有後端即時算**（yfinance+combo_scan 算 3-8 秒，瀏覽器 JS 做不到），跟 Discord bot 面臨同一個「要有常駐後端」的問題，換介面沒有解掉需求。Leo 決定：查詢功能維持 Discord+本機，HTML 資訊頁面先擱置（提到「現有 XQ 付費就有類似的」，優先度降低，之後要做只是還沒排上）。
- **本機常駐落地**：
  - `start_discord_bot.ps1`（跟 talentxtrend/assets-dashboard/Sonia 同款式），`-u`（無緩衝）避免踩到 9/3 稍早那個「明明在跑但 log 看不到」的緩衝坑，log 分流 `logs/discord_bot.log`/`.err.log`
  - `discord_bot.py` 加內建健康檢查端點（aiohttp，`127.0.0.1:8030`，`client.is_ready()`），改用 `asyncio.run()` 讓 discord.py client 跟這個小 web server 在同一個事件迴圈跑，取代原本的 `client.run(TOKEN)`
  - `service_health_check.py` 的 `SERVICES` 清單加「隆中對DiscordBot」一列，掛掉會自動跑 `start_discord_bot.ps1` 重啟，跟另外三個本機服務同一套「掛了先救、狀態改變才通知」機制——不然這支 Discord 連線斷了會沒人知道
  - `register_autostart_tasks.ps1` 加 `AutoStart_DiscordBot`（開機自動啟動）
  - ⚠️ **卡在權限**：`Register-ScheduledTask` 需要系統管理員權限，我的工具環境跑不動（`Access is denied`，HRESULT 0x80070005），待 Leo 自己用系統管理員身分跑一次 `register_autostart_tasks.ps1` 才會真的開機自啟；目前先手動背景啟動頂著，`/查` 現在能用，但機器重開會停
  - 順手修一個小坑：`register_autostart_tasks.ps1` 新增的註解裡用了全形括號（），PowerShell 5.1 用 `-File` 讀檔時解析成別的位置導致 parse error（"Missing expression after ','"，錯誤指到的行號跟實際內容對不上，是編碼誤讀的典型癥狀）——改用半形括號後解決，之後這個檔案裡的中文註解要避免全形符號

## 2026-09-03（續九）電金比「只有 2.5 個月」是誤判，重新回補到 241 個交易日，100MA 當場上線

- Leo 問「電金比可以做 60MA 嗎？沒有其它資料可以用了嗎？」——沒有直接回答 60MA，先去查「真的沒有其它資料」是不是站得住。
- 用 curl 單獨測證交所 `MI_INDEX` 這支 API 的多個舊日期（2026-04-01、2025-01-02、2023-01-03、2020-01-02、2018-01-02），**每一個都查得到電子工業類指數／金融保險類指數**，測到 2018 年都還在。這跟 8/31 記憶寫的「只提供近 2.5 個月」直接矛盾。
- 查原始程式碼才發現真因：8/27 那次 `--backfill 200` 一次打太多請求中途被限流（HTTP 307），只補到 53 個交易日就停在 2026-06-12；而 `run()` 的每日回補只在「既有資料範圍內」找缺口，不會主動往更早的日期擴展——53 天的邊界就這樣被誤認成 API 上限，卡了快一週沒人發現。
- 直接重跑 `python market_thermometer.py --backfill 400`（code 裡本來就有的函式，只是沒人再跑過），逐日打（`pause=0.4`）不會整批被擋，一次補到 **241 個交易日、回溯到 2025-08-01**。100MA 當場算出來：電金比 0.8573／100MA 0.9584／🔴 連 41 日低於均線。24 天暫時限流擋住，`run()` 每天自動重試，不影響已有的 241 天。
- 結論：不用做 60MA，也不用退而求其次用 ETF 代理近似（8/27 因誤差 0.85-2.3% 已否決過）——官方資料本來就夠，只是舊批次腳本的限流 bug 被誤讀成資料源限制。已接進 `daily_warroom.sec1_market()`，明天 08:45 每日戰情會顯示這行。
- 記憶 `market_thermometer_ef_ratio.md` 已更正；⭐ 通則：「資料源不夠」這種結論要用同一支 API 自己重新單獨測過，不能只看舊批次腳本跑出來的殘缺結果——批次腳本自己的限流/範圍 bug，很容易被誤讀成資料源限制。

## 2026-09-03（續九）規劃：`/查` 升級成視覺化網頁（先記計畫，等限流解除再動工）

**背景**：Leo 給 `C:\Users\Mophy\Documents\Investment\8996_高力_3家券商共識.html` 截圖，問「可以做成像 html 的圖這樣嗎？（現在燈號就很相近了）」——目前 `/查` 回的是純文字（燈號 emoji + 一行數字），這次要看的是能不能升級成有圖表的視覺化卡片，任意股票都能查（不受 179 檔限制）。**Leo 指示先記開發流程，等等限流解除再實作**，這篇只記計畫、沒動工。

### 目標畫面已經有現成產生器，不用重做

截圖那份報告的技術面卡片（SUPER TREND PRO MAX／雙重颶風K線／EXCEED CHARGE 充能爆發／RS STRONGER 相對強弱／COMBO 連鎖反應燈號＋K線+成交量+動能+RS 四張圖）**就是 `technical_indicators.py::build(ticker)` + `build_html(ticker)` 產生的**——這支已經在 `board_html.py`／`combo_html.py`／`earnings_infographic.py` 共用（9/2 才統一過蠟燭圖+SuperTrend配色+縮放平移，見 9/2「圖表統一到全站」那篇）。所以「畫面長什麼樣」這塊不用新設計，直接呼叫既有函式。

### 待查（動工時第一件事）：畫面左上「目標價與風報比」區塊的資料源

截圖左側「投顧目標 1500.00（+25.5%）」「停損3倍/4倍」這幾個數字，**還沒查證是哪支函式產生的**——`trade_plan.py::buffett_targets()` 有目標價/風報比概念但語意是「巴菲特貴俗價」不是「投顧目標」，這份報告檔名是「3家券商共識」，很可能是當時針對這一檔的客製分析（手動彙整券商報告），不是 combo_scan/lamp_lookup 現成會吐出來的通用欄位。**不能假設這塊是既有功能**，動工時要先查證（trade_plan.py 全文 + 那份 html 產生當時的 session/腳本），確認是重用既有函式還是要另外做。

### 規劃的接法（沿用今天已經搭好的本機服務，不用新決定部署方式）

1. `discord_bot.py` 已經在本機常駐一個 aiohttp web server（`127.0.0.1:8030`，今天為了健康檢查加的）——加一個新路由 `/lookup?ticker=xxx`，呼叫 `technical_indicators.build_html(ticker)`（或包一層取得跟 lamp_lookup 一致的資料，待查 `build()` 內部是否已經算了燈號/象限，或要另外接 `combo_scan`／`lamp_lookup`）
2. 任意代號都能查（跟 `/查` 走同一種「先查快取、沒有就即時算」邏輯，此頁預期比 `/查` 慢——`technical_indicators.build()` 抓 `disp_days=756`（約3年）數據＋算多組指標，比 lamp_lookup 的單純燈號判定重）
3. 手機存取：現有 Cloudflare Tunnel（`start_tunnel.ps1`）可以順手加一個路由對外開，不必等這頁做完再另外決定

### 尚未決定/待查清單（動工時要先過一輪）

- [ ] 左側「目標價/風報比/停損倍數」區塊資料源查證（見上）
- [ ] `technical_indicators.build()` 效能：756 天資料+多組指標，任意股票即時算要多久（比 lamp_lookup 的 3-8 秒可能更久，要不要也做 defer+背景執行的非同步 UX）
- [ ] 這個網頁要不要也顯示 COMBO 燈號跟 RRG 象限（目前 `/查` 有的兩項，`technical_indicators.build()` 卡片本身未必包含 RRG 象限，需要另外拼接 `lamp_lookup` 的 quad 欄位）
- [ ] 是否要做成「輸入代號→秒開結果頁」還是「先給連結、背景算完再通知」（取決於上面效能查證結果）

## 2026-09-03（續十）交易紀錄資料源定案：IB Pro 可接 API、台股改元大走截圖

- Leo 確認 **IB 帳戶是 Pro**（有 API 讀取權限）→ `ib_dashboard_project` 那支暫緩中的程式之後可以接上自動拉取。
- 台股券商**從統一改成元大**（「統一下單有問題，改用元大」）。Leo 問「不能給截圖就好嗎？」——**可以，定案走截圖**：一次幾筆的用量不需要 Excel 匯入器，截圖我直接讀數字進 `trade_journal.quick()`，跟打字給我同一條路徑。截圖要有：日期／代號／買賣別／成交股數／成交價（手續費+交易稅有更好）。
- ⚠️ 有講清楚界線（不是勸改，是讓 Leo 知道取捨）：一次幾十筆（補整年歷史）才需要檔案匯入——逐筆用眼睛抄數字抄錯不會有任何地方報錯，是典型靜默錯誤；「我記的股數 vs 券商實際成交股數」的對帳落差檢查（老墨那套）也只有檔案做得到。有量之後再說。
- 最終形態：**IB 自動、元大手動截圖，兩邊進同一張 executions 表**。
- ⚠️ 附帶查證：Leo 訊息裡附的路徑 `C:\Users\Mophy\Desktop\證券_庫存損益.xlsx` **實際不存在**（Desktop 上只有 `91848754-positions.xlsx`，內容是 IB 持倉快照不是統一/元大的交易明細，且是「庫存」不是「逐筆成交」）。已回報請 Leo 確認，沒有拿唯一存在的那個檔案硬當成他要給的東西。

## 2026-09-03（續十一）我重複造輪子：風險官 P1 早就存在——但查出兩個真問題

**我的錯**：路線圖記憶寫「仲達（風險官）完全沒做，P1 失效條件日檢還沒開始」，我照著它動手寫了 `risk_officer.py`（含條件判定、public/private 分流、新舊觸發比對，還做了驗證測試），寫完接排程時才看到 `researcher_stock_sync.cmd` 裡早就有 `python thesis_check.py`，註解寫明「P1: daily check of thesis invalidation conditions」。

`thesis_check.py`（8/27 上線）做的就是同一件事，而且比我寫的成熟：同樣的條件型別、同樣的 held/watch 分流、同樣「登錄當天就成立不算新觸發」的判斷（我當成自己的發現寫進註解，人家 9/1 就處理過了），還多了我沒做的「⚠️ 逼近失效線 2% 內」提醒。**已刪除 `risk_officer.py` 並還原 `daily_warroom.py`**——不留兩套會漂移的系統。

⭐ 教訓：**動工前要查的是程式碼，不是記憶檔**。記憶是寫下當時的觀察，會過期；「查得到的事別用推論代替」這條規則我有，但這次是「用記憶代替查證」，同一個坑的變形。正確順序是先 grep 專案有沒有現成的，再看記憶怎麼說。

### 真問題 A：21 檔有判斷但沒有失效條件，其中 4 檔是持股

| | 檔數 |
|---|---|
| 孔明有下判斷（advisor_verdicts） | 137 |
| 進了登錄簿（thesis_check 檢查得到） | 116 |
| **有判斷卻沒進登錄簿** | **21** |
| 其中**持股**：BAYRY／COHR／LITE／MU | 4 |

原因查證：這 21 檔（除 9914 外）的 verdict **本身就沒有任何 conditions**（trend=0 value=0 old=0），大多停在 2026-08-27——那是 conditions 欄位剛上線的時間點。`_register_conditions` 的 `if not conds: continue` 正確地跳過它們，不是 bug。真正的問題是**投資長是事件驅動才重判，這 4 檔持股從 8/27 起沒被任何事件觸發過，於是它們的失效條件永遠是空的、永遠不會被檢查，而且沒有任何地方會講**。9914 是另一種：9/1 的判斷有 4 條條件卻沒進登錄簿，原因未查清（不是 .TW 後綴問題，登錄簿裡完全沒有含 9914 的 key）。

### 真問題 B：持股清單一直留在公開 repo（已止血）

9/1 為隱私把 `advisor_verdicts.jsonl` / `research_notes.jsonl` 移出版控時，**漏掉 `state/thesis_conditions.json`**——它同樣每檔帶 `held: true`。實查遠端 `board/main` 的版本確有 12 檔 held=true，也就是持股清單一直公開著。

- 處理：`git rm --cached` + 加進 .gitignore，已 push，實查 `git show board/main:state/thesis_conditions.json` 回 does not exist ✅
- 動之前先查過：只有本機 4 支 .py 讀它，GitHub Actions 沒用到，各 .cmd 的 `git add -f` 清單也沒有它 → 移出版控不影響管線
- ⚠️ **歷史仍在公開 repo 的 commit 紀錄裡**，要不要改寫歷史強推是 Leo 的決定（而且 repo 公開過，第三方可能已有快取，改寫不保證清乾淨）
- ⭐ 同 9/1「撤憑證漏掉消費端」的模式：**做隱私移除時要 grep 全部帶同類欄位的檔案，不是只處理想到的那兩個**

## 2026-09-03（續十）🔴 discord_bot.py 掛掉、自動重啟也失敗：`start_discord_bot.ps1` 從沒被真的跑過一次

**觸發**：Leo 轉來 `service_health_check.py` 的 Telegram 通知——「🔴 隆中對DiscordBot 掛了…重啟後仍失敗…需人工介入」。

**查證**：`ps -W` 沒有任何 discord_bot.py 行程、`curl 127.0.0.1:8030` connection refused。`logs/discord_bot.err.log` **根本不存在**——照 `start_discord_bot.ps1` 的邏輯，只要 `Start-Process` 真的被執行到，這個檔案（就算內容是空的）就一定會被建出來。檔案不存在＝這支 `.ps1` 從頭到尾沒有真正跑起來過。

**根因**：`start_discord_bot.ps1` 裡的中文註解用了全形括號（），跟今天稍早 `register_autostart_tasks.ps1` 撞到的**同一種**坑——Windows PowerShell 5.1 用 `-File`讀沒有 UTF-8 BOM 的 .ps1 檔案時，會用系統的 Big5 內碼去解，某些全形符號的位元組序列剛好被誤讀成引號/終止符，導致整支腳本**在執行任何一行之前就 parse error 死掉**，而且**不會產生任何 log**（因為連 log 都還沒寫就死了）——看起來就像「什麼都沒發生」，比顯性錯誤更難抓。

**⭐ 我自己的判斷失誤**：稍早改完這支 `.ps1` 之後，我只用 `python -u discord_bot.py` **直接測試底層 Python 腳本**（有效），從沒真的執行過 `start_discord_bot.ps1` 這個檔案本身——這正是自動化重啟真正會呼叫的東西。等於「驗證了引擎，沒驗證真正會被開的那把鑰匙」，同一天在 `register_autostart_tasks.ps1` 才踩過同一種全形符號的坑，卻沒有回頭去檢查 `start_discord_bot.ps1` 是不是也有同樣風險——沒有把「這類坑」類推到同類檔案。

**修法**：`start_discord_bot.ps1` 整支改成純 ASCII（註解也是），不再靠猜哪個符號安全——這次直接用 `-File` **實際執行**這支腳本驗證（不是只測底層 python），確認 log/err.log 正常產生、health endpoint 回 `{"ok": true}`。

**現況**：bot 已手動重新啟動，`/查` 應該恢復正常。**開機自動啟動仍卡在同一個待辦**：`register_autostart_tasks.ps1` 需要 Leo 用系統管理員身分跑一次才會登記；但這次修完之後，就算沒登記，`service_health_check.py` 下次跑（每 2 小時）也能**真的成功**自動重啟，不會再靜默失敗。

**通用教訓**：這系列 `.ps1` 檔案（本機常駐服務的啟動腳本）今後一律避免全形標點，寧可用純 ASCII 或英文註解；改完自動化腳本要**執行腳本本身**驗證，不能只驗證它呼叫的底層程式——兩者是不同的失效面。已寫進通用記憶 `powershell_fullwidth_parse_bug`。

**✅ 收尾（同日稍晚）**：Leo 用系統管理員身分跑過 `register_autostart_tasks.ps1`，`Get-ScheduledTask AutoStart_DiscordBot` 確認狀態 Ready——開機自啟登記成功，路線圖第1項（Discord Bot）到這裡才算真正落地：`/查` 能查、掛了會被健檢真的救回來、開機也會自動起來。

## 2026-09-03（續十二）/lookup 視覺化查股頁上線

Leo 指示做路線圖的 3、4 兩項，這是第 4 項。按平行對話（續九）寫好的規劃做，沒有重新規劃。

- **新檔 `lookup_page.py`**：`render(ticker)` → (html, status)。數字全部來自 `lamp_lookup.lookup()`（跟 Discord `/查` 同一支，網頁與 Discord 不可能給出不同燈號），圖表用 `technical_indicators.build_html()`（跟看板/燈號頁/財報卡同一套產生器）。頁面樣式用 `board_theme.BASE_CSS`，不自己寫一套。
- **`discord_bot.py` 只加最小掛載**：`app.router.add_get("/lookup", _lookup_page)`，跑在既有的 127.0.0.1:8030 aiohttp server 上（不另開服務）。用 `asyncio.to_thread` 包住 render——那是同步的抓資料+算指標，直接在事件迴圈跑會把整個 bot 卡住連 Discord 心跳都停，反而害健檢判定失聯重啟。
- 檔案衝突處理：先用 SendMessage 通知平行對話我要接手，對方 commit 完 discord_bot.py（577297e）並回覆不再動它，我 rebase 後才改。

### 刻意沒做：投顧目標價與 3倍/4倍停損

參考畫面（`8996_高力_3家券商共識.html`）左側那塊，**實查整個 `C:\Users\Mophy\AI` 沒有任何 .py 產生 `stop3`/`stop4`/`to_flip_pct`**，`trade_plan.supertrend_invalidation()` 也只回 `st_bearish`/`rs60_broken`。確認是那份報告的客製內容，不是通用功能 → 這頁不畫，也在頁尾明講「本系統沒有對應資料源，不做假的欄位」。

頁面能給的目標價是 `combo_scan.price_targets()` 的**市場共識**（yfinance analyst_price_targets 均值），語意不同，所以標籤寫「分析師共識目標價」不寫「投顧目標」。要做 ATR 倍數停損是另一個功能，倍數要 Leo 訂（不自行發明投資門檻）。

### 驗證（四條路徑都實跑，不是只驗成功那條）

| 路徑 | 結果 |
|---|---|
| 守備清單內（2454） | 200，走今日快取，現價 4,275／四燈 4/4／風報比 2.71 ⭐打點成立 |
| **圖表真的畫出來** | 瀏覽器實測：4 個 canvas 都有像素、Chart.js/candlestick/zoom/Hammer 全載入、**零 console 錯誤** |
| 非清單內美股（WM） | 200，走即時計算，1.6 秒（price_store 快取有資料時很快） |
| 亂打代號 | 404 + 友善說明，不是白畫面 |
| 空代號 | 200，只給搜尋框 |
| 健康檢查沒被弄壞 | 200，`ok:true` |

重啟：`start_discord_bot.ps1`（bot 不會 hot-reload，改完一定要重啟才生效）。

## 2026-09-03（續十一）決定：`/lookup` 對外開放，用 token+cookie 門檻

**背景**：`/lookup` 上線後問 Leo 手機存取要不要走 Cloudflare Tunnel 對外開——這代表把無驗證頁面放上公網，兩個 AI session 都刻意沒自己決定。**Leo 拍板：要開**，但要有門檻，不能真的完全公開。

**設計（`.env` 已加 `LOOKUP_TOKEN`）**：手機第一次用 `?key=<LOOKUP_TOKEN值>` 開頁面，成功後種 90 天 cookie，之後開不用再帶 key；沒帶 key 也沒 cookie 一律回 404（不是 403——不透露「這裡有東西但你沒權限」，查無此頁面更安全）。`LOOKUP_TOKEN` 留空＝不設防（只有本機/區網用才可以留空）。

**現況**：`.env` 設定已加好，`discord_bot.py`／`lookup_page.py` 的 gate 邏輯正在改（另一個 session 進行中，尚未 commit）——這篇先記決策本身，實作細節與 Cloudflare Tunnel 路由設定等那邊做完再補。

🔜 未做（要 Leo 決定）：手機存取要不要走 Cloudflare Tunnel 對外開——那是把一個無驗證的頁面放到公網，屬於對外曝險決定，不自己動。

## 2026-09-03（續十三）查股頁對外開放，手機可用

Leo：「希望手機能開，幫我推上線（投資html 有這個分頁嗎? 還是做一格輸入欄在燈號?）」

### 為什麼不能做成投資站的一個分頁

投資站是 **GitHub Pages 靜態託管**，只能放事先產好的檔案。查任意代號要即時抓 3 年 OHLC＋算指標，一定要有後端在跑，靜態頁做不到（只有把 179 檔預先產好才辦得到，但那就不是「任意股票」了）。所以採用 Leo 提的第二個做法：**入口放燈號頁、運算留在本機服務**。

### 上線內容

- **Cloudflare Tunnel 加 `stock.talentxtrend.com` → `127.0.0.1:8030`**，沿用既有 tunnel（原本只有 sonia/assets 兩條），config.yml 已備份成 `config.yml.bak-20260903`，DNS CNAME 用 `cloudflared tunnel route dns` 建。
- **`LOOKUP_TOKEN` 門檻**：`?key=<token>` 進來一次種 90 天 cookie（HttpOnly/Secure/SameSite=Lax），之後手機直接開。沒 key 也沒 cookie **一律回 404 不是 403**——403 等於告訴對方「這裡有東西只是你沒權限」。
  - 為什麼要設防：頁面本身沒個資（只有公開市場資料），但**每次查詢都會在 Leo 的電腦上抓 3 年資料＋算指標**，完全不設防等於把運算資源開放給任何掃到這個網域的人。
- **健康檢查端點一併擋掉對外**：`/` 本來會回 bot 名稱，接上 tunnel 後就跟著曝光了。判斷方式是 **`CF-Connecting-IP` 標頭有沒有**——不能用來源 IP，因為 cloudflared 也是從 127.0.0.1 連進來的，用 IP 判斷會把外部請求誤判成本機。
- **燈號頁加輸入欄**（`combo_html.py` 的 `LOOKUP_BOX`）：網址寫在公開頁面上等於公開，但服務端有 token，沒 cookie 的人點進去只會拿到 404。

### 驗證（全部打真實對外網址，不是本機）

| 情境 | 結果 |
|---|---|
| 沒帶 key | 404 ✅ |
| 帶正確 key | 200 + `Set-Cookie: lk=…; HttpOnly; Secure; SameSite=Lax; Max-Age=7776000` ✅ |
| 只帶 cookie（模擬手機第二次開） | 200 ✅ |
| cookie 值錯 | 404 ✅ |
| 對外打健康檢查 | 404（不再露 bot 名稱）✅ |
| 本機打健康檢查 | 200（service_health_check 自動重啟不受影響）✅ |
| 手機尺寸（375×812）實測 | 卡片自動排成兩欄、無橫向捲動 ✅ |

⚠️ 服務跑在 Leo 的電腦上——**電腦關機或 bot 沒跑時這個連結會連不上**，這是本機常駐部署的必然取捨（見路線圖第 1 項的 Zeabur vs 本機評估）。

---

## 2026-09-03 — 技術圖加老墨 RS/EXCEED CHARGE marker(看板+燈號同步)

### 需求(Leo,看老墨 XQ 看盤教學後)
把老墨的 RS 顏色代號(黃點=長線翻正/藍點=短線創新高/粉紅=資金比股價先動)
與 EXCEED CHARGE 的釋放瞬間 marker,做進技術圖。

### ⚠️ 關鍵發現:RS 與 squeeze 其實早就有了
`technical_indicators.py`:`mansfield_rs()`/`rs_signals()`(含 lead=資金比股價先動,
註解已寫「老墨自己說最有價值」)、`squeeze_momentum()`/`squeeze_intensity()`(名字就叫
EXCEED CHARGE)。差的只是把「只算最新一根的 badge 訊號」畫成「圖上跨時間的點」。

### 改動
1. `technical_indicators.py` 新增 `rs_signal_series()`(把 rs_signals 三訊號算整段);
   RS 圖加 🟡翻正/🔵創新高/🩷資金領先 三個點 dataset;EXCEED CHARGE 圖加釋放★
   (sq_on True→False,pointStyle:star,依動能上色)。訊號定義沿用 rs_signals,**沒動門檻/算法**。
2. 🔴 **`board_html.py` 有自己一套獨立圖表碼(不走 ti.build_html)**——同一組 marker 要**移植第二次**;
   且 board 原本 `rs_l=[]`(只算短線RS),黃點需要長線 → 補算 `mansfield_rs_series(cl,bench,250)`。
   ⭐ 通則:改技術圖要 grep `technical_indicators.py` + `board_html.py` 兩處(圖表碼重複)。
3. 燈號(`combo_html.py`)走 `ti.build_html` → 自動生效,只需重生。

### 同步重生 + 部署(Leo 指定)
- 重生 `docs/board.html`(產業鏈)+ `docs/combo.html`(燈號),grep 確認 marker 進輸出。
- 財報卡走 ti.build_html,**本次不重生**,下次排程自動帶到(Leo 指定)。
- commit da7cca5 → push board/main(GitHub Pages)。

## 2026-09-03（續十四）策略賽馬巴菲特倉 -37% 是假的：換匯改了一半

Leo 看截圖問「策略賽馬是不是有做錯」。查證結果：**是真的錯了，而且是我 9/2 的修改造成的。**

### 現象與真因

頁面顯示巴菲特倉 $6,264（-37.36%），四倉合計 $35,194（-12.01%）。查淨值歷史發現關鍵線索：

```
9/1: 10,056　9/2: 10,039　9/3: 6,263  ← 只有今天崩
```

不是建倉就錯，是**今天才壞**。真因：9/2 我修 `paper_portfolio.is_tw()` 讓它認得 `.TWO`（上櫃），於是 `_refresh_current()` 的**現價**開始正確換成美元；但 8/18 建倉時寫進 `portfolios.json` 的**進場價 `eu` 還是舊制**（台幣數字直接當美元）。舊制下進場價與現價都是「台幣當美元」，比例一致所以看起來正常；新制只改了現價那一半 → 市值變成原本的 1/32。

只影響兩檔，都是 `.TWO`：`1580.TWO`（eu 106.5，應為 3.3281）、`5520.TWO`（eu 83.2，應為 2.6）。

### 修法

保持「當初投入的美元」不變（實測五檔都是 $1,981.33，等權重），用正確的美元進場價重算股數：
- 1580.TWO：18.60 股 → 595.33 股
- 5520.TWO：23.81 股 → 762.05 股

交叉驗證：修正後巴菲特倉 $10,030，對得上 9/2 的 $10,039（單日小幅變動）✅。
同時修掉 history 裡 9/3 那筆假紀錄（**只改今天這筆**——9/2 以前在舊制下前後一致，比例是對的，動它等於竄改歷史）。

修正後：巴菲特 $10,030（+0.30%）、四倉合計 $38,960（-2.6%）。

### 加了防線 `_check_basis()`

每日 `update_nav()` 開頭檢查台股部位的 `eu` 是否 ≈ `en / fx`，不一致就印紅字警告（**只警告不自動改**——自動改可能把對的改壞）。正向測試過：故意還原成壞掉的值會亮警告，正常的部位安靜。

⭐ **通則：改了「怎麼算」的規則，要一併檢查已經用舊規則存起來的資料。** 只改計算不遷移資料＝改了一半，而且壞掉的樣子會**偽裝成正常的市場波動**（-37% 看起來就只是跌很多），沒有任何地方會報錯。同 [[otc_suffix_coverage_gap]]、[[derived_files_stale_after_correction]]。

## 2026-09-03（續十五）賽馬頁加「各倉選股與進出方式」總表

Leo：「幫我把各倉選股與進出方式寫在網頁最下面」。原本底部已有說明，但**缺 9/2 才加的進出燈號倉**，而且「怎麼進出」散在各段落裡不好對照。

- 新增總表（倉別／選什麼股／什麼時候買賣／頻率）五列，含 8 條鏈明細倉
- 新增進出燈號完整說明：四燈定義、進場門檻（≥3 燈且風報比≥1）、不對稱兩階段出場（ST 翻空賣半、RS60 跌破全出）、7 天冷卻，並明講**這個倉沒有歷史回測**（目標價只有現在的快照沒有歷史，風報比回不了頭），它是累積樣本的器材不是已驗證策略
- 手機版總表改堆疊式（四欄表格在窄螢幕會擠爆）

⚠️ **排程時間查自 workflow 的 cron，不是照抄程式碼註解**——結果發現 `paper_portfolio.py` docstring 寫的「每週日調倉」是錯的：實際 `weekly-screen.yml` 是 `0 0 * * 6`＝台灣**週六 08:00**（產業鏈全/巴菲特/8鏈），`portfolio-nav.yml` 是 `0 1 * * 1-6`＝**週一~六 09:00**（趨勢倉/燈號倉/淨值）。docstring 已一併更正。

---

## 2026-09-03 — RS 顯示短線 30→20 對齊老墨

### 起因
Leo 拿老墨 XQ 的 MSFT RS 跟我們對:同基準(S&P500)下長線對得上(我們2.01 vs 老墨2.34),
但短線對不上(我們30日=4.52% vs 老墨0.82)。實測我們的 **20日**=0.98% 才對上老墨。

### 改動(只動顯示層)
`mansfield_rs` 預設 short 30→20;`mansfield_rs_series` 顯示呼叫 30→20
(technical_indicators build、board_html 台股、board_html_legacy 美股);標籤「短線30日→20日」、
RS 卡「短線RS(30日)→(20日)」。長線維持 250。重生 board+combo 部署(commit 220644d)。

### 🔴 重要:系統裡有三個「短線 RS」,別搞混
- **顯示層(財報卡/board/combo 圖表+RS卡)**:現在 20 日(本次改的,對齊老墨螢幕)
- **燈四訊號(combo_scan RS_SHORT)**:60 日(RS_LONG=240),combo_scan:47 註明「老墨的預設」
- **backtest**:30/60(backtest_*.py,不動)

### ⚠️ 未解矛盾(留給未來)
combo_scan:47 說「60/240 才是老墨預設」,但我實測老墨螢幕短線 0.82 對應的是 **20日**不是 60日
(60日算出來 14%)。可能是老墨究極版公式≠我們的 Mansfield,只是在不同窗口跟我們交會。
→ 以 MSFT 單檔對齊短線=20;**別檔可能有出入(方向會對),老墨確切公式未公開,別當定論**。
   燈四維持 60(那是 Leo 既有訊號,沒動)。若要更嚴謹,多對幾檔老墨數字再定。

## 2026-09-03（續十六）補監控機制、電金比回測初探、Sonia 查股卡、交易紀錄落位

### 補監控：`investment_chief.py --fill-missing`

投資長是事件驅動，沒被事件碰到的持股永遠沒有失效條件 → `thesis_check` 永遠檢查不到它們。做成**可重複執行的模式**（不是拋棄式腳本）：讀 `thesis_check.coverage_gap()` 算出缺口 → 餵回 `run(targets=...)` → 判斷完自動登錄條件。之後新增持股也走同一條路。

先用 2 檔驗證才整批跑（006208.TW / 2303.TW → 條件登錄成功、型別都是每天檢查得動的、涵蓋率 16→18/78）。**走本機 claude CLI（Max plan 訂閱額度，不是付費 API）**——程式自己的輸出訊息就寫著「Max plan走訂閱額度」，我先前跟 Leo 說「62 檔約 $8.7」是錯的，已更正。剩 60 檔背景跑中。

### 電金比回測初探（`ef_backtest.py`）

**結果：掃 N=1..40，沒有任何一個 N 贏過買進持有。** N=32（老墨用的）+62.52% vs 買抱 +69.80%。

⚠️ **但這不是結論**：樣本只有 207 天（扣掉 100 天暖身），而且這段是單邊多頭——任何會空手的規則在多頭裡都一定輸。要證明有沒有用必須涵蓋一段真正的空頭。

唯一現在就看得出來的事實：N≤7 時最大回撤從 -16.4% 壓到 -10.4%，代價是少賺 5-15%——是「用報酬換平穩」的交換，不是「又賺又穩」。**在補深之前不建議接成行動指令。**

🔴 **回補踩到限流**：`--backfill 1200` 用預設 0.4 秒間隔，補到第 45 天就被證交所擋掉，**528 天全部拿不到**。已加 `--pause` 參數，1.6 秒重跑中。⭐ 補深歷史要放慢，快反而拿不到。

### 投顧報告：先查涵蓋率才決定值不值得做

13 份報告涵蓋 9 檔，**只有 2 檔在我們系統裡**（2882 國泰金＝持股、4979 華星光＝掃描母體內），其餘 7 檔（7750/2449/6944/2883/6919/6415/6187）完全在視線外。

→ 這個功能的價值**不在「多一個監控」，而在那 7 檔視線外的股票**，以及「券商當初憑什麼假設給這個目標價、那個假設現在還成立嗎」——我們現有系統算的是市場共識目標價，沒有報告的推導過程這一層。

### Sonia 加 LOOKUP 卡片

`jarvis/static/index.html` 加一張卡＋`CARD_LINKS` 一條。本機開連 127.0.0.1:8030、外部走 tunnel（跟 ASSETS 卡同一套 `IS_LOCAL` 判斷）。用 `StaticFiles` 直接讀檔，**不用重啟**，重新整理就生效。

### 交易紀錄三處落位（Leo 問「現在放哪」）

| 用途 | 位置 |
|---|---|
| 看（手機開得了） | obis `04_AI Report\Investment\交易紀錄.html` |
| 原始資料 | `state/trade_journal.jsonl`（gitignore 保護，公開 repo 進不去） |
| 備份 | Drive `_local_only\`，輪替 8 份 |

⚠️ 發現備份落後：備份跟著 08:45 排程跑，當天新增的交易要隔天才進備份。已手動補跑一次。

聯發科 5 股 @4275 已回填成成交，**券商由統一更正為元大**（Leo：統一沒辦法下單，後來在元大做的），並在 note 註明原因。

## 2026-09-03（續十七）查股頁收盤價跟看盤軟體對不上：兩層快取疊加

Leo 拿 XQ 對照 00981A：我們顯示 30.37（資料日 9/1），XQ 顯示 29.13（9/3）。

### 查證：數字沒錯，是「舊」不是「錯」

直接打 yfinance：`08-27 29.52｜08-28 30.09｜08-31 29.40｜09-01 30.37｜09-03 29.13`
——我們的 30.37 對 9/1 是正確的，XQ 的 29.13 對 9/3 也是正確的。**這檔 9/2 整天缺資料**。

### 為什麼會慢：兩層快取疊加

1. `combo_result.json`（燈號掃描）是**早上 07:00** 跑的，那時台股還沒收盤 → 內容必然是前一交易日收盤。這是設計不是 bug，但頁面只寫「資料日」不夠顯眼。
2. 更隱蔽的一層：`price_store` 的 `STALE_HOURS=12`，而它 **07:45 被掃描更新過** → 到 19:45 前都判定「新鮮」不重抓。所以我第一版做的「即時重算」**只重算指標、沒重抓價格**，拿到的還是 9/1 —— 那個標示是騙人的。

### 修法

- `price_store.get_ohlc/get_closes` 加 `force` 參數，無視 STALE_HOURS 直接重抓
- `lamp_lookup.lookup(ticker, live=True)` 跳過燈號快取**並** `force=True` 重抓價格
- 查股頁：`?live=1`；快取版顯示「今日掃描快取（前一交易日收盤）　🔄 即時重算」，資料日落後 ≥2 天再加黃字「⚠️ 落後 N 天」
- 實測驗證（對外網址）：快取版顯示 9/1＋落後 2 天＋重算連結；`&live=1` 顯示 **9/3 29.13**，與 XQ 一致 ✅

⭐ 通則：**「即時」這種字眼要先確認整條資料鏈都真的即時**。這次是第二層快取讓標示變成謊話——只驗最外層（有沒有跳過燈號快取）會以為修好了。

## 2026-09-03（續十八）Leo 抓到我解釋錯：9/2 有交易，是 yfinance 漏 K 棒

續十七我說「00981A 這檔 9/2 整天缺資料」，Leo 反問「理論上也應該是 9/2 的資料不是 9/3」——**他是對的**，前一個交易日是 9/2 不是 9/1，我的解釋只有在 9/2 真的不存在時才成立。

### 用 FinMind 獨立驗證：9/2 有交易

`TaiwanStockPrice` 查 00981A：`08-28 30.09｜08-31 29.40｜09-01 30.37｜**09-02 29.73**｜09-03 29.13`。
**9/2 有成交（29.73、量 1.8 億），是 yfinance 少了那一根。**

### 抽樣量範圍：ETF 中招、個股沒事

| 代號 | FinMind | yfinance | |
|---|---|---|---|
| 00981A | 24 根 | 23 根 | 🔴 漏 2026-09-02 |
| 0050 | 24 根 | 23 根 | 🔴 漏 2026-09-02 |
| 2330 / 2454 / 3037 / 1580 / 6488 / 2618 | 24 根 | 24 根 | 正常 |

→ 9/2 那天 **yfinance 對台股 ETF 整批漏 K 棒**，個股不受影響。影響面：ETF 的 SuperTrend／RS／動能都算在少一根的序列上，以及顯示的「資料日/現價」會退回 9/1。

⭐ 教訓：**單一資料源查不到，不等於那件事沒發生。** 我拿 yfinance 沒有就下結論「那天沒資料」，等於把「我的來源沒有」講成「事實上沒有」。要下這種結論至少要第二個來源。（同 verify_final_state_not_observed）

### 順手擋掉另一個假結論：電金比回測的連續性檢查

放慢重補後資料變成「範圍 2023-05~2026-09 但只有 312 筆」（涵蓋率 38%）——**中間全是限流造成的空洞**。100 日均線是連續 100 筆算出來的，餵一段跨月空洞的序列給它，它照樣算得出數字但完全沒有意義。

`ef_backtest.py` 加連續性檢查：涵蓋率 < 90% 直接拒絕回測並印出補資料指令。續十七那次的初步結果是在 2025-06~2026-09 連續 310 天上跑的（涵蓋率 97%），那份仍然有效，只是樣本短。

好消息：`market_thermometer.run()` 每天會重補 `[最早日, 今天]` 的所有缺口，所以空洞會**每天自己慢慢補**，不用一直手動跑。

## 2026-09-03（續十九）台股缺 K 棒：用 FinMind 補洞，不換資料源

Leo：「做3吧 不然一直缺資料也不是辨法」（＝要整個換成 FinMind）。動手前先實測三件事，結果有兩件會改變投資訊號：

| 查證 | 結果 |
|---|---|
| **還原權值** | FinMind 是**原始價**、yfinance 是**還原價**。實測一年前差 1.3~4.7%（2618 差 4.72%），最近日期完全相同 |
| **成交量** | **兩邊對不上**：2330 在 9/1 是 3,185 萬 vs 1,721 萬（1.85 倍），且比例每天不同（9/3 只差 6%） |
| **速率** | 0.63 秒/檔、195 檔約 2.1 分鐘、10 檔測試零失敗、無限制標頭 → 速度不是問題 |

**全換的代價**：所有台股歷史價格從還原價變原始價 → SuperTrend/RS 全部位移、**燈號可能翻**；成交量不同 → 雙重颱風那盞燈也會變。**在沒有回測依據下改變策略行為**，就是同一天早上換匯那個坑的翻版（`_check_basis` 就是為它加的）。

**改成：yfinance 當主（維持還原價體系），FinMind 只補它缺的那幾根。** Leo 的目標「不要一直缺資料」100% 達成，而且不動策略行為。近期缺口（最後一次除權息之後）兩邊價格完全相同，補進去零誤差。

實作在 `price_store._fill_tw_gaps()`，掛在 `_download()` 之後：
- 只處理 `.TW`/`.TWO`，只回頭檢查 **120 天**（每天 130 檔各拉 3 年 JSON 是 ~13MB/天的無謂流量；實際失效模式是近期 K 棒漏掉）
- 合併時**既有的優先**（`duplicated(keep="first")`），不覆蓋 yfinance 的還原價
- 有補就印出來（補幾根、哪幾檔），不靜默

驗證：`00981A.TW+1、0050.TW+1`，9/2 補回 29.73（與 FinMind 官方一致），2330/1580.TWO 本來就完整沒被動到。**指標確實改變**：00981A 的 RS60 從 -0.66% → **-2.27%**，這就是少一根的實際影響。

---

## 2026-09-03（續二十）補監控收尾：查出 `holdings.json` 躺在公開 repo，以及 BRK.B 一年抓不到價

補監控批次跑完（60 檔，Max plan 訂閱額度不是現金），覆蓋率 18/78 → 75/78。剩下 3 檔全部失敗，追下去挖到三件事：

### 1. 🔴 `holdings.json`（Leo 60 檔美股持股清單）被追蹤在**公開** repo

追 BK/CUK 為什麼抓不到時順手 `git ls-files` 才看到。跟今天早上 `state/thesis_conditions.json` **完全同一類的洩漏**——早上修了那一個，**沒有回頭掃還有沒有別的**，所以這個又躺了一天。

- 已處理：`git rm --cached` + 寫進 `.gitignore`，本機檔案完好
- ⚠️ **git 歷史裡還在**——要跟 thesis_conditions.json 一起清（Leo 手動跑 filter-branch 那條指令要加上這個檔）
- 同時掃過其他被追蹤的個人資料：`portfolios.json` 是**模擬倉**（base $10,000 虛擬）不是真持股，可以公開；`state/` 其餘是市場資料

**⭐ 教訓**：發現一個檔案洩漏時，當下就要掃「同一類的還有沒有」，不能只修被指出來的那一個。

### 2. 🔴 `BRK.B` 在 yfinance **一年來從來沒抓到過價格**

yfinance 的 B 股要寫 `BRK-B`，寫 `BRK.B` 一律回「possibly delisted, no price data」。而持股來源（trade_plan 讀 Firstrade 報表）寫的就是 `BRK.B`。

下游只看到「抓不到也沒有快取」一行——**長得跟「這檔真的下市了」一模一樣**，所以從來沒人追。

修在 `price_store._yf_symbol()`：下載用 yfinance 寫法，回存用**呼叫端給的原始寫法**當 key（否則要 `BRK.B` 的人拿到 `BRK-B` 這個 key，等於還是查不到）。台股 `.TW/.TWO` 與 `^` 開頭的指數不碰。

驗證：`BRK.B` 22 根 K 棒、505.24（與 `BRK-B` 一致），2330.TW/NVDA 不受影響。

### 3. `BK` / `CUK` 是**過期代號**，不是抓不到

- `BK` → **BNY**（紐約梅隆 2025 年更名換代號）
- `CUK` → **CCL**（嘉年華結束雙重上市，CUK 併入 CCL）

Firstrade 實際持股報表寫的就是 BNY 和 CCL（而且兩檔早就有失效條件在監控），是 `holdings.json` 這份**手動維護**的清單沒跟上。已改名不是刪除——刪掉的話 `researcher_holdings.py`／`st_alert.py` 這兩個只讀 holdings.json 的消費端會漏掉這兩檔。

**⭐ 同一個指紋**：手動清單與自動來源並存 → 手動那份會默默過期，而且過期的樣子跟「資料源壞了」一樣。

### 結果

失效條件監控覆蓋率 **76/76 檔（缺口 0）**。

---

## 2026-09-03（續二十一）查股頁改成跟投資站同一套設計系統

Leo：「查詢個股設計風格不一致，請改成跟燈號風格一致」「查任意股也是風格不一致，**請之後設計注意**」。

### 不一致在哪（三層，越下面越隱蔽）

| 層 | 問題 |
|---|---|
| **缺頁首** | 查股頁沒有 `board_theme.header()`，沒有標題列也沒有導覽——點進去等於離開網站，回不去 |
| **自己寫一組色** | `lookup_page.PAGE_CSS` 用 `#1a1d23` / `#2a2e35` / `#e8eaed`（灰褐），投資站是 `--surface #0F172A` / `--line #1E293B`（深藍）。並排就是兩個網站 |
| 🔴 **共用元件本身就偏離色票** | `board_theme.LOOKUP_CSS`（搜尋框）和 `technical_indicators.CSS`（技術面卡片）**也都是寫死的灰褐色**。查股頁沿用它們，等於把偏差一起帶過去——而 `technical_indicators` 同時出現在**產業鏈看板／進出燈號／財報分析／查股四個頁面**，四頁都帶著同一個偏差 |

**第三層才是真正的原因**：我以為「沿用共用元件＝風格一致」，但共用元件自己就沒有遵守色票。**一致性不是靠複用，是靠所有東西都指向同一個色票來源。**

### 做了什麼

1. `lookup_page`：接上 `board_theme.header()` ＋ 新增 `nav_abs()`（**絕對網址**——這頁跑在 `stock.talentxtrend.com`，用 NAV 的相對路徑 `combo.html` 會連到自己那台的 404）
2. 搜尋框改用站上同一顆 `.lkbox`（連 class 都沿用，不再各寫一份）
3. `PAGE_CSS` 全部改走變數，**這頁不准再出現寫死色碼**
4. `LOOKUP_CSS`、`technical_indicators.CSS` 一併改走變數 → 首頁／燈號／看板／財報／查股五處同時修正
5. `board_theme` 新增 `PAGES_URL` 常數（全專案原本有 **8 個檔案**各寫死一份投資站網址）
6. 新增 `search` 圖示；中文值的卡片加 `.zh` 不套等寬數字字體

### ⚠️ 差點做壞的地方

`earnings_infographic.py` **用了 `ti.CSS` 但沒有載入 `BASE_CSS`**，也沒自己定義那些變數。直接換成 `var(--surface)` 的話，財報卡的卡片背景會**整個變透明而且不會報錯**（CSS 找不到變數就當沒設定）。

→ 每個變數都帶 fallback：`var(--line,#1E293B)`。**實測驗證**：做一頁只載 `ti.CSS`、完全沒有 `:root` 的測試頁，卡片仍正確顯示深藍底＋邊框。

⭐ **抽共用 CSS 變數前，要先查每個消費端是不是都載了定義那些變數的那份 CSS。**

### 設計規則（往後一律遵守）

1. **新頁面一律 `board_theme.header()` ＋ `BASE_CSS`**，不自己寫頁首與色系
2. **任何頁面／共用元件都不准出現寫死的中性色碼**（背景/邊框/文字灰階）——一律 `var(--*)`。有顏色語意的訊號色（綠買紅賣藍持有）例外
3. **共用元件的 CSS 變數要帶 fallback**，因為不是每個消費端都載了 `BASE_CSS`
4. 跑在別的網域的頁面用 `nav_abs()` 不是 `NAV`

---

## 2026-09-03（續二十二）老墨驗證日程接上法說會 + 🔴 RS 訊號整段位移一根

### 一、老墨三段時程「到時候怎麼驗證」（Leo 選了三項零成本作法）

**① 先訂判定標準**（最該先做的，不是機制）
`verify` 欄原本只寫「去哪裡看」，沒寫「什麼算過」。例：「N2 月產能年底達 10 萬片」，
台積電講 9 萬算 confirmed 還是 slipped？**沒定義的話到期一定往自己看好的方向合理化。**
11 條全部寫入 `criteria`（confirmed/slipped/broken），標準從宣稱本身機械推出；
少數要自己抓中間線的（n2-100k 的 7 萬＝宣稱值 70%）在 `criteria_note` 標明
「這是我訂的、非外部來源」。`a16-ready` 訂得最兇：**再滑一次就是 broken**
（2026→2027 已滑過一次）。

**② 接法說會**（Leo：「法說會我們財報分析不是會抓資料？」——對，而且比代理指標直接）
`earnings_call.py` 本來就走本機 claude（Max plan、不花現金）抓逐字稿，
`guidance` 型項目的定義裡明寫「新產能/新廠爬坡時程」。兩個缺口都補了：

| 缺口 | 修法 |
|---|---|
| 抓到的東西**產完就丟** | `build()` 只回 HTML 嵌進財報卡。→ 加 `_persist()` 存 `state/earnings_call_notes.json` + `guidance()` 查詢 |
| 要看的公司**不在母體裡** | 每日財報卡母體是 `holdings`（31 檔），TSM/COHR/2330/2308/2301 全不在。→ `--fetch` 自己對 watch 標的呼叫，不產整張卡（那要 3-8 分鐘）|

⭐ **首跑就有結果，而且離到期還有 27 天**：光寶科法說原話「800V HVDC 電源機架
**8 月開始送樣、13 週認證後預計 11 月小量產、2027Q1 擴大量產**」；台達電只說
PowerRacks「2026 年開始初期出貨、能見度有限」。對照先訂好的標準 → **指向 slipped**。
但 3Q26 還沒結束、Q3 法說（10 月底）才是定論，所以**只記 note 不改狀態**。

⚠️ spacex-1gw / orbit-scale 標的非上市，沒有法說會可讀，到期只能人工判斷（已標明）。

**③ 驗證結果接回決策**（原本驗完什麼都不會發生＝只是記帳）
- `investment_chief.today_tickers()`：slipped/broken 後 3 天內，該鏈**持股**進重評名單
- `board_html_legacy.chain_phase()`：看板 ⏱ 標籤標「⏳ 已延後 / ❌ 前提已被打破」

實測（照「沒執行過的路徑不算能用」）：a16-ready 標 broken → AI 伺服器鏈觸發
TSM/AMD/MU/NVDA/MSFT **5 檔**；hvdc-3q26 標 slipped → **0 檔**，查下去該鏈確實
無持股，**是正確行為不是 bug**。測完已還原。

⚠️ `today_tickers()` 裡的 `date` 是 `time.strftime` **字串**不是 `datetime.date`，
新程式直接寫 `date.fromisoformat` 會 AttributeError，改用 `_date` 別名。

### 二、🔴 RS 訊號整段位移一根（全站每一檔都在發生）

Leo 對照 XQ 發現奇鋐（3017）的 🩷「資金比股價先動」沒標出來。

**不是沒畫，是算錯。** `technical_indicators.build()` **分別**用
`yf.Ticker().history()` 抓個股和基準，然後直接 `.tolist()`——**完全沒對齊日期**。

實測：3017.TW **486 根 vs ^TWII 485 根**，而且 **NVDA / 2330.TW / 1580.TWO 全都差 1**。
`mansfield_rs_series()` 右對齊回傳 `min(len)`，接著 `rs_signal_series()` 拿
`rs[i]` 去比 `closes[i]`——**兩個 i 不是同一天**。而 🩷 的判定正是
「RS 創新高 **但股價未創同期新高**」，兩邊差一天就會標錯或漏標。

3017 修前後：🟡翻正 **3 點 → 1 點**、🩷資金領先 **0 點 → 08/06 有了**。

修法：`bs.reindex(hist.index).ffill().bfill()` 對齊到個股的交易日——長度必然相同、
而且 `i` 保證是同一天。實測四檔全部 486/486、NaN 0。

⭐ 這是 `board_html_legacy._align_rs` 和 `backtest_position_sim` **早就修過的同一個坑**
（2026-08-11 的註解就寫著「mansfield_rs_series 內部右對齊、長度可能比 closes 短」），
這支漏掉了。**又一次「修一個模式沒掃全部同類」。**

⚠️ **不能拿 XQ 的數字當對錯依據**：他那張圖的基準是 **S&P 500**（面板下拉選單），
我們台股用 ^TWII，短線 RS 7.42 vs 6.00 本來就不會一樣。實測換 ^GSPC 算，
訊號點位置也不同——**先確認驗的是不是同一個東西**。

### 三、查股頁兩項 UI（Leo）

- **圖表預設展開、拿掉收放鈕**：`build(expanded=True)`。只給單一個股頁用；
  看板/燈號/財報卡一頁很多檔，全展開會爆，那邊維持收合。
- **電腦版 7 張小卡排成一排**：≥1180px 時 `.wrap` 放寬到 1440px、`.lk-grid` 固定 7 欄。
  原本 `auto-fit` 在 1100px 下差幾像素塞不下，變成 6+1。

### 四、另修：`earnings_call` 存檔 key

`--fetch` 不帶 `quarter_label` 時原本用「今天日期」當 key，每跑一次就多一筆同內容
紀錄，`guidance()` 會把同一句話回好幾遍。改用年+季（`2026Q3`）。

### ⚠️ 兩個給錯的指令（記錄下來免得再犯）

1. **`if ($?) { git push }` 在 PowerShell 5.1 會靜默跳過**——`git filter-branch`
   把 WARNING 寫到 stderr，PS 就把 `$?` 判成 false，即使指令其實成功。
   結果歷史改寫成功但沒推上去。
2. **`git log --all` 會掃到 `refs/original/*`**（filter-branch 自己留的備份 ref，
   只在本機、永遠不會被推），所以驗證要指定 ref：`git log board/main -- <path>`。

---

## 2026-09-03（續二十三）產業鏈看板三件事：深度解讀缺三鏈、跨鏈重複、AI 優先排序

### a. 「AI 電源/散熱 圖表打不開」——不是圖表壞掉，是按鈕根本沒生出來

線上逐檔實測那條鏈 8 檔（VRT/NVTS/3324/2301/3017/3653/3665/2308），**圖表全部畫得出來**
（Chart 實例都在、canvas 有畫上像素、零 JS 錯誤）。真正的差別：

| 鏈 | 按鈕 |
|---|---|
| 原本八鏈 | 展開風險與觀察重點 ＋ **產業深度解讀** |
| AI 材料/被動元件、AI 電源/散熱 | 只有前者，**沒有深度解讀** |

`chain_reports.json` 只有八鏈，9/3 新增的兩條沒有；順帶查出**關鍵金屬/原物料一直也沒有**
（舊缺口）。⭐ **「功能沒出現」跟「功能壞了」外觀一樣**——使用者只能描述成「打不開」。

修法：`refresh-chain-reports` skill 從八鏈擴到 11 鏈，加鏈名↔slug 對照表，
並支援 `$ARGUMENTS` 只跑指定幾條（`materials,power_thermal,metals`）。
驗收那行原本寫死 `grep -c 'rptbtn'` **應為 8**——改成「應等於有報告的鏈數」並註明
不要寫死數字，就是這次踩到的。

⚠️ **零成本路徑本來就已經在用**：`refresh_reports_quarterly.cmd` 跑的是
`claude.cmd -p "/refresh-chain-reports"`＝本機 CLI／Max plan 訂閱額度，不是付費 API。
（另查：`RefreshChainReports` 排程從沒執行過——Last=1999、rc=267011，季度觸發要 10/1 才第一次跑，
所以現有八鏈那份是手動產的。）

🔴 **排程沒設成功**：寫 `.cmd` 與註冊排程兩個動作都被權限守衛擋下，
理由是「無人看管 + `--dangerously-skip-permissions`」不在 Leo 的授權範圍內。
沒有繞過，改成把指令交給 Leo 自己執行。

### b. 同一檔出現在不同鏈——是對的，且不會重複計算

台股 56 檔裡 5 檔跨兩鏈：3017 奇鋐／3324 雙鴻（AI 伺服器＋AI 電源/散熱）、
3037 欣興（AI 伺服器＋玻璃基板）、3105 穩懋（矽光子＋低軌衛星）、1504 東元（機器人＋AI 電力）。
公司本來就同時服務兩條鏈。

**查程式碼確認不會重複算**：`paper_portfolio` 的「產業鏈全」是**聯集**
（`sorted(set(us + tw))`），等權重按去重後的代號分配；模擬倉裡 3017 只有一筆 `sh=0.94`。
研究員/投資長觸發也是以代號為 key 去重。

### c. 看板鏈排序改成 AI 優先（Leo 指定）

`AI 伺服器 → AI 材料/被動元件 → AI 電源/散熱 → AI 電力/核能 → 矽光子/光通訊 →
玻璃基板/TGV → 關鍵金屬/原物料 → 太陽能 → 機器人 → 低軌衛星 → Bitcoin→AI 機房`

⚠️ `CHAIN_ORDER` **同時決定三處**：看板區塊順序、頂端錨點導覽、`alert_telegram` 推播分組。
三處都 import 同一份，已在註解標明別各自再排一次。
驗證：對照 screen_result 無漏無多、圖示齊全、重產 board.html 實測 c0~c10 順序正確。

---

## 2026-09-03～04（續二十四）投顧報告失效線 + 券商異動提醒上線；一晚抓到 4 個假訊號

路線圖第 5 項落地，順帶把「新持股沒進監控」的漏洞補掉。

### 一、`advisor_reports.py`：投顧報告失效線監控器

13→16 份券商 PDF → 本機 claude 結構化（Max plan，**不扣款**）→ 每份 4-8 條失效條件。

**價值不在「多一個監控」**（先查涵蓋率才決定做）：13 份涵蓋 9 檔，只有 2882、4979
在我們系統裡，其餘 **7 檔完全在視線外**。而且我們原本只有 yfinance 的「共識目標價」
（一個平均數），**沒有「憑什麼是這個數字」那一層**。

🔴 **Leo 問「這是要幹嘛？投資長看得到嗎？個股資訊看得到嗎？」——原本兩個都看不到**，
那份資料會是沒人經過的死路。所以接三個真正會被走到的出口，而不是另做一頁：
`investment_chief.gather_material()`（價值角度材料）／查股頁券商卡／日報第④段。

判定線在解析當下就訂好、不事後調整。⚠️ 跌破線 **-15% 是我訂的**（這批報告潛在漲幅
10.9~36.3%，跌 15% 等於上檔空間被反向吃掉一半以上），已在檔頭標明。

### 二、`target_changes.py`：每日目標價/評等異動提醒

資料＝永豐金證券法人部的每日異動表**截圖**（.webp）。本機 claude 直接讀圖
（`_ask_claude` 沒停用工具，subprocess 裡的 Read 讀得到圖片檔）。24 筆異動。

⚠️ **刻意不拿這份表的數字建失效條件**：TP Old 欄有明顯錯位
（PANW `0→390`、奇鋐 `78→4500`、南電 `2444→2460` 卻寫潛在幅度 105%）。
只做「誰調了、往哪個方向」。

🔴 **設計搞錯一件事（Leo 追問才發現）**：照「只信高盛」做成只推高盛，結果今天推播
**0 筆**（高盛動的 PANW/緯穎6669/台新新光金2887 我們都沒追蹤）；但**持股裡真的有一筆
被降評（AAPL → Sell）卻不會通知**，只因為降的是 Morningstar。
⭐ **券商可不可信是「判斷內容」的事，不是「要不要讓你知道」的事**。
建議改成：持股被任何人調降一律推 / 高盛任何異動推 / 其餘只記不推。**待 Leo 拍板。**

### 三、🔴 一晚抓到 4 個會產生假訊號的錯（每一個都「看起來像正常數字」）

| # | 錯 | 後果 |
|---|---|---|
| 1 | `implied_multiple` 拿 EPS 清單**第一筆**當分母 | 4 筆假的「估值前提已用完」：聯發科 65.6 倍、萬潤 217 倍、雙鴻 94.5 倍、寶雅 35 倍。正確是聯發科 15.5 倍（25 倍 × 2H27E-1H28E EPS 280）|
| 2 | `is_trusted` 裸子字串比對 | `.strip()` 把 `"gs "` 變成 `"gs"` → **"mornin(gs)tar" 命中** → 「AAPL 被 Morningstar 降評」被當成高盛異動 |
| 3 | 外資報告只抽前 2 頁 ＋ `txt[:9000]` | 高盛目標價在**第 5 頁**→ 抽不到 → **跟「這份沒給目標價」長得一樣**。第一次修完仍失敗，因為關鍵頁被截斷——**「抓到了」跟「餵進去了」是兩件事** |
| 4 | **`nan is not None` 是 True** | 「現價 nan，距離 nan%」印進投資長材料 → 趨勢角度說算不出乖離、價值角度判「資料不足」|

修法：
1. 新增 `valuation_eps`（報告說倍數乘在哪個數字上）＋**恆等式自我檢查**
   `倍數 × 基數 ≈ 目標價`（誤差 12% 內）過不了就不出訊號。修後 16 份逐份重算**零誤報**。
   ⭐ 這是唯一能在「數字看起來很正常」時攔住它的辦法。
2. 改整詞比對（英數字段＋中文字切詞）。
3. 前 N 頁 ＋ 掃全份找關鍵字頁，且**關鍵頁排最前面**；prompt 上限 9000→16000。
4. `math.isfinite` 擋 nan（不是只擋 None）＋ 現價改用獨立來源。

### 四、`held_universe()` 納入交易紀錄（Leo 拍板治本）

Leo 9/3 在元大買聯發科 5 股，`trade_journal` 有紀錄但 `holdings.json` 與券商對帳來源
都沒有 → 2454 **沒有任何失效條件在監控**，而我當天還回報「覆蓋 76/76 缺口 0」。
加第三個來源（`status=filled` 的 buy），只收 buy 不扣 sell（這是決策紀錄不是庫存表）。
修後 **78/78 缺口 0**，另抓出 CEG 同樣漏掉。

### 五、`stock_brief.py`：個股整合報告

六層擺同一頁（燈號／券商報告與推導／估值前提／預估前提檢查／毛利率位階／
投資長兩角度／近期異動），**全部讀既有 state 檔，不重算不呼叫 AI 不花錢**。
有就顯示、沒有就整段不出現——實測 3017／NVDA／2330 都是 0 份報告照樣產得出來。

2454 首份：投資長**兩角度給出相反判斷**（設計如此）——趨勢「續抱/可買」
（防守線 3821.5、緩衝 +13.57%，但族群轉弱）、價值「考慮出場」
（現價是洪瑞泰貴價 1944 的 **2.23 倍**；高盛的 7000 需要本益比從 15.5 倍擴張到 25 倍、+61%）。

### 六、決策：風報比維持用共識目標價（Leo 2026-09-04 拍板）

券商報告的目標價、異動表的目標價**都不進風報比公式**。`add_rr` 只吃 yfinance
`analyst_price_targets` 的 mean。理由：混進去會做出「看起來像風報比但意義不同」的數字，
而且不同檔用不同來源（有報告的用報告、沒報告的用共識）就失去可比性。
實測差距（2454）：共識 5,502.9 → rr 2.71／高盛 7,000 → 約 5.2／JPM 5,600 → 約 2.9。
**翻倍不是因為風險變小，只是換一個更樂觀的人來估。**

### 七、其他

- 台股中文名改自動來源（`tw_names_cache.json` 1,986 筆），12 檔英文全名修好
- 未分鏈股票不再靜默消失（加「其他（未分鏈）」桶）
- 產業鏈看板 AI 優先排序（Leo 指定）
- 解析結果是**第三方付費研究內容** → 兩個 state 檔 gitignore ＋ 進 `backup_local_only`

### ⚠️ 工具面的坑

- 背景指令接 `| tail -22` → **log 前面全被截掉**，害我誤判「只處理 10 份」。背景任務不要接 tail。
- 跨工具傳輸會吃掉反斜線：寫 `r"\bgs\b"` 進檔案，實際存進去的是**真的退格字元**
  （`cat -A` 顯示 `^Hgs^H`）。要寫正則進檔案時用 `cat -A` 驗，或避開反斜線改用別的做法。

---

## 2026-09-04（續二十五）報告摘要層＋查核層＋同一檔多份整合

Leo：「ab 一起做，我希望可以等於是一份摘要，也有像老墨的 html 檢查報告商寫的是不是事實」。

### 起點：Leo 問「報告還有寫什麼內容？只有這麼一些嗎？」

實查後承認：高盛那份 PDF **11 頁、49,221 字，我們只抽了大約 8 個欄位**。
根因是 schema 只設計了「做成失效條件用得到的欄位」，**沒有設計「這份報告在講什麼故事」那一層**。
所以看到的是骨架不是內容。沒抽的東西裡有幾個明顯有價值：完整財務預估表、
目標價調整史、季度 EPS、以及高盛自己說「被低估」的 custom HBM base-die。

### 一、schema 擴充（A）：把表格也抄下來

新增 `forecast`／`target_history`／`eps_quarterly`／`summary`／`key_points`。
`key_points` 分三型：`nonconsensus`（報告自稱被市場低估的觀點）／`claim`（可查核的
具體宣稱）／**`caveat`（報告自己講的保留——最容易被讀者略過）**。

抽取成果：16/16 有 summary 與 key_points、14/16 有 forecast、10/16 有季度 EPS。
高盛 2454 的兩條 caveat 特別有價值：
- 「NVLink 整合才剛起步、**未揭露營收時程**，近期對獲利貢獻有限」
- 「輝達下一代 NVHBM 的基礎層**本身由輝達設計**，聯發科僅延伸參與記憶體架構、
  互連與先進封裝整合，**並非取得核心設計主導權**」

🔴 **`target_history` 一開始是空的**，而那是「目標價領先股價還是追著跑」唯一的來源。
原因：那張表在**第 8 頁**，而 `pdf_text` 用「命中哪個關鍵字」排優先序，
`Target price history` 排第 5 → 前面五頁就把 `extra_pages=3` 的額度佔滿。
修法：提到第一順位（中文報告不會有這字串，不排擠中文券商首頁）＋ extra_pages 3→5。
修後抓到 **17 筆調整史（2023-10 至今）**。
⭐ **同一晚第二次踩「抓到了 ≠ 餵進去了」**（前一次是 `txt[:9000]` 切掉第 5 頁）。

### 二、`report_factcheck.py`（B）：報告的假設 vs 我們算的

六個查核項、三態判定（✅ 對得上／⚠️ 有落差／⏳ 還不能驗）：

| 查核項 | 拿什麼比 |
|---|---|
| 估值倍數 | 現價 ÷ 報告自己的基數＝市場現在給幾倍 |
| 成長率假設 | 這檔**自己**的歷史月營收 YoY 中位與最大（base_rate）|
| 今年營收 | base_rate 的「剩下幾個月要跑多快」＋分析師準頭 |
| 毛利率風險 | 報告自列風險含毛利時，對照 margin_profile 的位階 |
| **目標價軌跡** | 報告自己的 target_history vs 同期實際股價漲幅 |
| 季度 EPS | ⏳ 等財報自動比對 |

⭐ **設計原則寫進檔頭：重點不是抓券商說謊**——他們寫的多半是預估不是事實。
是把「他假設什麼」跟「這檔自己的歷史做得到什麼」擺在一起，讓落差自己現形。
⚠️ **刻意不查事件真偽**（「NVIDIA 是不是真的認購 35 億」）——那要花錢，
而且報告引用的是公開重訊，造假機率極低。我們查的是**推論與假設**。

2454 首跑 **2 對得上／3 有落差／1 不能驗**，三條落差都有實質內容：
- 2027 營收要 **+96.8%**、2028 再 +84.9%，而歷史最大 **+106.2%**
  → 「要求連續貼在或超過自己的歷史上限——不是說做不到，是**沒有容錯空間**」
- 毛利率 46.2%、**24 季第 25 百分位** → 「報告點的風險**不是未來可能發生，
  是現在就在偏低的位置**，要往上走才撐得起 EPS 預估」
- 目標價 915→7,000（**+665%**）vs 同期股價 801→3,555（**+344%**）
  → 「**目標價漲得比股價還多**，上檔空間有多少是重新評價倍數給的、
  有多少是盈餘真的成長，值得分開看」

「估值倍數」判 ✅（市場 15.3 倍 vs 報告 25 倍，還差 64%），但註記寫明
**這 64% 要靠本益比擴張，不是靠盈餘成長，兩者是不同的事**。

### 三、同一檔多份報告整合（Leo：「請整合在一起」）

多份時先給對照表（券商／日期／評等／目標價／倍數／**乘在哪一期**），再逐份細節。
7750 新代五家：

| 券商 | 評等 | 目標價 | 倍數 | 乘在哪一期 |
|---|---|---|---|---|
| 康和 | 強力買進 | 3,570 | 35 倍 | 2027F |
| 中信 | 買進 | 3,360 | 31 倍 | 2027F |
| **宏遠** | 買進 | **2,650** | 30 倍 | **2026F** |
| 第一金 | 買進 | 3,400 | 33 倍 | 26H2-27H1 |
| 元大 | 買進 | 3,210 | 30 倍 | 2026-27平均 |

⭐ 目標價差 35%，而**宏遠最保守不是因為看空**（一樣「買進」），是它用 **2026 年**
EPS、其他家用 2027。**同一批事實、不同的假設，答案差三成**——這正是「整合在一起」
才看得出來的東西，逐份讀反而會被每一家的敘事帶走。

### 四、其他 UI 修正（都是 Leo 指出的）

- **投資長 reasoning 分段**：原本一整片文字牆。它寫的時候本來就有「事實／推論／
  資料缺口」標記，改成拆成有標籤的分段；找不到標記就退回照句號斷行，不硬套格式。
- **字卡上色**：用**跟查股頁/燈號頁完全同一套符號**（🟢/⚫ 四燈、🔴多方/🟢空方、
  ⭐打點成立、RS 正負綠紅），不另外發明。⚠️ 現價與風報比**刻意不上色**——
  board_theme 的規則是「顏色只給訊號用、數字類不上色」。
- **查股頁補券商異動**：⚠️ **「不主動吵你」不等於「看不到」**。日報推播維持只推
  可信名單（Leo 指定只信高盛），但主動查一檔時全部列出、可信名單標 ★。

---

## 2026-09-04（續二十六）目標價假訊息／軍議上線／電金比回測跑出來了

### 一、🔴「查無目標價」是假的——101 檔裡有 75 檔其實有

Leo 問「我們現在查股票可以看到目標價嗎」查出來的。

`combo_scan.add_rr()` 在 SuperTrend 空方時**直接 return，連 `target` 都不填**，
下游看到 None 就顯示「查無分析師共識目標價」。實測 194 檔裡 101 檔沒目標價，
拆解後 **75 檔是快取裡明明有**（2330 台積電 3229.33／MU 1513.41／CCJ 129.76／
ACGL／CI／KR…），只有 26 檔真的查無（ETF 與 ADR）。

⭐ 那是把「**我們刻意不算**」講成「**外面沒有資料**」——兩件完全不同的事，
而且錯的那個看起來完全正常。

修法：目標價一律填（跟多空無關）；**風報比維持空方不算**（沒有持有中的停損，
談賠率無意義，原設計是對的不動）。101 → **26 檔**。

順帶：目標價快取 key 原本「呼叫端傳什麼就存什麼」——掃描端 `2330`、查股頁
`2330.TW`，同一檔存兩份互相不認。統一走 `tw_symbol.resolve()`。

### 二、#軍議上線：仲達與陳壽可以問話了（路線圖第 1 項階段 3）

兩位的底層早就在跑（仲達 P1＝`thesis_check.py`、陳壽 A 型＝`verdict_review.py`），
但輸出只在日報和 state 檔。新增 `war_room.py`＋Discord `/仲達` `/陳壽`。

**兩個誠實邊界寫進 prompt，不靠自覺**：仲達不下行動指令（電金比只量測）；
陳壽樣本 < 20 筆不得談紀律。

首測兩位都守住了：仲達主動標「券商異動數字未經覆核」「電金比不當出場依據」，
還自己抓到 UHS 是失效條件未覆蓋的那一檔；陳壽拒絕用 4 筆下紀律結論，
**連「偏保守/偏積極」這種軟性判斷都擋掉**。

⭐ **仲達還點出我的疏漏**：「材料未標注 WM 是否為持股」「券商異動沒標注是否為
你的持股」。他答得誠實，但那是**我沒給資料**。已補：每一列都標（持股）/（非持股）。

實作坑：`thesis_check_today` 的結構是 triggered/near/pending_metric 三個**陣列**，
每筆是 `[代號, 說明, 是否持股, 角度]` 的 list 不是 dict——照 dict 假設寫的第一版
材料印「共檢查 0 檔」，**跟「今天真的沒東西要檢查」長得一樣**。

### 三、異動提醒改三層（Leo 拍板）

原本只推「可信券商 ∩ 母體內」＝只推高盛 → 首跑 0 筆；**但持股裡真的有一筆被降評
（AAPL → Downgrade to Sell）卻不會通知**，只因為降的是 Morningstar。

⭐ **券商可不可信是「判斷內容」的事，不是「要不要讓你知道」的事。**
改成：持股被**任何人**調降一律推／可信名單任何異動推／其餘只記不推。

### 四、🔴 電金比回測終於跑出來——而且卡了兩天的原因是我的量測工具錯了

**先講錯在哪**：連續性檢查用「期間天數 × 5/7 × 0.96」推估應有交易日數，
再看實際筆數的比例。這個估法**把國定假日當成資料缺口**——2026 農曆年假連休
7 個工作日，直接把連續段切斷。

- 我算的：最長連續段 **215 天**、涵蓋率 75% → **判定資料不足、拒絕回測**
- 實際：扣掉週末與 **43 個已知休市日**後，有 **533 個連續交易日**（2024-06-27 ~ 今天）

⭐ **量測工具本身有 bug，而它的輸出看起來完全正常。** 今天第二次踩
（早上是 `grep -c` 數行不數次，害我以為深度解讀只補了 2 鏈）。

修法：讀 `ef_ratio_closed.json`（抓取時就記下來的非交易日），週末與已知休市
**跳過不算斷**，真的缺一個交易日才算斷；門檻改成 `MA_DAYS + 250` 講清楚為什麼。

**回測結果（433 個交易日、N=1~40 全掃）**：

| N | 策略總報酬 | 買抱 | 策略 MDD | 買抱 MDD |
|---|---|---|---|---|
| **7** | **+103.61%** | +103.24% | **-10.42%** | -26.71% |
| 4 | +101.34% | +103.24% | -10.42% | -26.71% |
| **32（老墨用的）** | **+75.88%** | +103.24% | **-26.71%** | -26.71% |
| 30 | +57.94% | +103.24% | **-29.34%** | -26.71% |

🔴 **40 個 N 只有 1 個贏過買進持有。老墨的 N=32 輸 27 個百分點，而且最大回檔
跟買抱完全一樣（-26.71%）——承擔同樣的痛、少賺三成。**

但有一個結構性的東西：**N ≤ 7 把最大回檔砍掉 61%**（-26.71% → -10.42%），
報酬幾乎不變。大 N 反應慢，等訊號確認時跌完了才出場，**保護等於零**。

⚠️ **不足以當採用證據**：只有 N=7 一個點贏，旁邊 N=8 就掉到 +88%——那是雜訊的
形狀不是結構的形狀。樣本 433 天不含完整空頭循環，也沒扣交易成本。
能說的只有：**老墨的 N=32 在我們這段樣本沒有證據支持；若要用這指標，方向是往
小 N 走，而且它的價值在控制回檔不在提高報酬。**

### 五、另修一個假訊息

`backfill` 印「⚠️ N 天被證交所限流擋掉，**下次執行會再試**」——**對舊缺口是騙人的**：
每日 `run()` 只掃最近 14 天（window=14），2026-02~07 的缺口永遠不在窗口內。
改成明講，並加 `--fill-gaps` 專補舊洞（一次幾天、間隔拉長，避開限流）。

### 六、收尾格式寫進全域 CLAUDE.md

Leo：「可以記下來每次跑完都幫我整理做了什麼？有什麼下一步到全域，不然每次都看不懂」
→ 全域新增硬性規則：**做了什麼（原本怎樣→現在怎樣）／為什麼（只寫非顯而易見的，
尤其取捨與刻意不做的）／下一步（我能做的 vs 卡住的）／等你決定的（每項要能用
「好」或「不要」回答）**，並要求同一輪把開發日誌、記憶、commit 一起做完。

---

## 2026-09-04（續二十七）電金比警示定案 N=32／fill-gaps 上排程／6669 是我講錯

### 一、回檔警示定案：N=32（Leo 拍板沿用老墨的版本）

我先按 Leo「改用小 N」實作 N=7，他看完 N=7 vs N=32 的進出明細後改回 32，
理由：「**7 天很可能是在震盪**」。

⭐ **回測反駁不了這個理由**。我們的樣本只有 433 個交易日、不含完整空頭循環、
也不含長期橫盤。N=7 在這段只進出 5 次沒被巴到，但那是**這一段剛好沒有震盪盤**，
不是 N=7 抗震盪。他擔心的是樣本裡沒有的情境——這不是「數據 vs 直覺」的對立，
是「數據沒涵蓋到的地方」。

🔴 **代價照實寫進警示文案，不隱藏**：N=32 在這段樣本總報酬 +75.9% vs 買抱 +103.2%、
最大回檔 **-26.7% 跟什麼都不做完全一樣＝沒有保護效果**。

**兩個 N 的差別全在 2025-04 那一次**（進出明細對照）：

| | N=7 | N=32 |
|---|---|---|
| 清倉 | 2025-03-05　加權 **22,872** | 2025-04-11　加權 **19,529** |
| 回補 | 2025-05-14　21,783 | 2025-05-14　21,783 |
| 淨值 | 0.9986（幾乎無傷）| **0.8526（-15%）** |

N=32 **賣在低點、買在高點，一來一回虧 15% 且永久**（後面 2,900 點的漲幅用少 15%
的本金去賺，所以最後差 27 個百分點）。N=7 在跌 3% 時就出場、同日回補。
⭐ **這就是「大 N 保護等於零」的實體**：等 32 天確認完，跌已經跌完，賣在最痛的地方。
老墨的規則不是方向錯，是**反應太慢，慢到把保護變成傷害**。

⚠️ **警示文案改成隨 WARN_N 變**：N≤7 才引用「-10.4% vs -26.7%」那組數字；
N≥8 改寫成「這個門檻在我們的回測裡沒有減少回檔的證據」。
⭐ **不能拿 N=7 的回測結果去背書 N=32 的門檻**——那會變成用別人的成績單當自己的。

Leo 另外要求拿掉文案裡的決策歸屬（「這個門檻是 Leo 指定沿用老墨的版本」）——
日報是給他看的，不需要覆述他自己做過的決定。只留可查證的：數字＋樣本限制。
留下「兩個方向都還沒驗證到」，因為限制對兩邊對稱：N=32 沒被證明有用，
**小 N 也沒被證明在震盪盤安全**。

### 二、`--fill-gaps` 掛進每日排程

`researcher_stock_sync.cmd`（08:45）新增，每次補 6 天、間隔 3 秒——遠低於證交所
限流門檻（實測約 45 次密集請求才觸發）。剩 199 天舊缺口，約一個月磨完。

### 三、🔴 6669 緯穎「不在守備清單」是我講錯

它**一直都在 `screen.py` 的 AI 伺服器鏈池子裡**（第 49 行）。池子 14 檔、每週取
前 8 名，這週落選（門檻 score 1.54）：

- 選上：2382 廣達 2.23／3324 雙鴻 1.92／3017 奇鋐 1.92／3037 欣興 1.85／
  2317 鴻海 1.77／2356 英業達 1.69／2330 台積電 1.62／2376 技嘉 1.54
- 落選：**6669 緯穎**／3231 緯創／2308 台達電／2368 金像電／3533 嘉澤／8046 南電

⭐ **這正是記憶裡記過的那條**：「在不在守備清單」要看 `screen.py` 的**池**，
`screen_result.json` 是**篩後子集**。我昨天照 screen_result 判斷就講錯了。
**不用加它——它每週都在被評估，分數上去自然就進來。**

### 四、收尾格式寫進全域 CLAUDE.md

Leo：「可以記下來每次跑完都幫我整理做了什麼？有什麼下一步到全域，不然每次都看不懂」

固定四段：**做了什麼**（原本怎樣→現在怎樣，有數字給數字）／**為什麼**（只寫非
顯而易見的，尤其我做了跟他要求不同的事、取捨、刻意不做的）／**下一步**（我能做的
vs 卡住的）／**等你決定的**（每項要能用「好」或「不要」回答，沒有就寫沒有）。
並要求同一輪把開發日誌、記憶、commit 一起做完，不等他再問。

---

## 2026-09-04（續二十八）🔴 軍議答錯股票——最危險的那種錯

### 事件

Leo 問仲達「**高力怎麼看？**」，回答的是 **HIG（The Hartford Insurance Group）**。
高力是 **8996**，跟 HIG 完全是兩家公司。

8996 不在材料裡（非持股、不在燈號母體、無券商報告），他就在材料中找了個
「看起來最像」的代號套上去，**而且整段講得很有條理**——格式正確、語氣謹慎、
甚至誠實標了「材料沒給的部分」。

⭐ **這是最危險的一種錯**：所有可見的品質訊號都是對的，只有主體是錯的。
靠 prompt 叫他「不要猜」擋不住——**他不覺得自己在猜**。

### 修法三層（不是加一句叮嚀）

1. **送進 AI 之前先硬解析**（`war_room.resolve_in_question`）：
   台股中文名 → 直寫代號 → 中英文公司名（`lookup_page.resolve`），
   把那一檔的**真實資料**擺在材料最前面（放最前面是因為材料會被截斷）。
2. **系統外的股票即時算一次**（`lamp_lookup.lookup`，跟 `/查` 與查股頁同一支），
   並明確標示「不在每日掃描母體、為了回答這個問題臨時算的」
   「**沒有失效條件在監控**——沒有人在盯它的判斷基礎」。
3. **prompt 加代號鐵律**，並把這次的實例寫進去當反面教材。

### 美股中文名對照表（Leo：「建吧」）

台股中文名有官方來源（證交所＋櫃買，自動抓 2,000 檔），**美股沒有**——
Yahoo 搜尋不認中文。手動建 41 組進 `lookup_page.US_ZH_NAMES`。

⚠️ **只收台灣財經媒體確實通用的譯名**，不確定的寧可留空——回「沒解析到」是誠實的，
對錯股票就是剛修掉的那個 bug。
⚠️ **不收會跟台股撞名的**：「台積電」是 2330（台股掛牌）不是 TSM，
所以收「台積電ADR」→ TSM。解析順序台股名冊優先，實測 台積電→2330、台積電ADR→TSM。

驗證：輝達→NVDA、超微→AMD、波克夏→BRK-B、嘉年華→CCL、
高力→8996（即時算出燈號 3/4、風報比 3.92）、Palo Alto Networks→PANW、
Costco→COST、COST／2454 直寫代號都可以。

### ⚠️ 途中踩的四個實作坑（都是同一類：跨工具傳輸吃掉跳脫字元）

1. **regex 的 `\b` 變成真的退格字元 0x08** → regex 成了「找退格字元包住的代號」，
   永遠匹配不到。**grep/sed 顯示不出來，只有 `repr()` 看得見。**
   改用字元類自己界定，不依賴反斜線。
2. **`\x00-\x7F` 的 `\x00` 變成真的空位元組** → `SyntaxError: source code string
   cannot contain null bytes`。改用 `"".join(c if ord(c) < 128 else " " for c in q)`。
3. `\n` 在寫檔時變成真的換行 → f-string 未閉合（同一晚第 N 次）。
4. **先 `.upper()` 再抓代號**會把「Palo Alto Networks」切成 PALO/ALTO，
   而且因為 cands 有東西了、公司名解析永遠不會執行。改在**原文**找——代號本來就大寫。
5. **整句丟給 `lookup_page.resolve`** 會因為結尾的中文字（「如何」）被判定成查台股。
   英文部分要另外抽出來再試一次。

⭐ **通用做法**：要寫含反斜線的 regex/跳脫序列進檔案時，
用 `Edit` 工具（不解析跳脫）或改寫成不需要反斜線的等價式；
寫完用 `repr()` 或 `cat -A` 驗，不要只看 `grep`。

---

## 2026-09-04（續二十九）四位軍師到齊 + `/軍議` + 兩次「材料缺一塊就自己填」

### 一、補上孔明與龐統，Discord 現在 6 個指令

原本只有仲達/陳壽能對話，龐統/孔明只會自己跑。

- **孔明（投資長）**：材料**直接呼叫 `investment_chief.gather_material()`**，
  不重寫一份——所以 `/孔明` 跟每日排程產出的判斷吃同一包東西，
  不可能「兩邊講法不一樣」。沒點名股票時明講「要指定一檔」。
- **龐統（研究員）**：重用 `researcher_industry._search_one()`（鉅亨網關鍵字搜尋，
  公開免 key、零成本），跟每日研究員同一個來源。只給材料不下判斷。

首測都守住本分：孔明對 2454 給了**兩個不同調的判斷**（價值「資料不足」、
趨勢「續抱可買」），並拒絕拿趨勢材料裡夾帶的洪瑞泰貴價去替價值角度背書；
龐統主動指出「其餘幾則是大盤新聞，**2454 只是被關鍵字比對到，跟它本身無關**」。

### 二、`/軍議`：一次問三位，**程式分派不用 AI**

Leo 問「不能四位一起回答嗎？由孔明統一分派？還是加一位角色？」——
只做第一個，後兩個不做，理由寫進 `council_roles()` 的註解：

- **不用孔明分派**：分派是規則問題（有沒有指定個股），不是判斷問題；
  他的職責是「對一檔給兩個獨立角度」，兼任調度會混淆角色。
- **不加第五位**：四位已涵蓋找材料/下判斷/看風險/看紀錄。
  第五位只會是「轉述前四位講的話」的人——**正是 Leo 剛嫌過的囉嗦**。
- **不用 AI 分派**：多一層 AI 就多一次會錯的機會（同日才踩過答錯股票）；
  分派者要猜使用者想問什麼，猜錯的成本比省下的時間高。

規則：有指定個股 → 龐統/孔明/仲達；沒指定 → 龐統/仲達/陳壽。
⚠️ 問個股時不叫陳壽（他的材料是全局的，叫了只會重複統計）。
⚠️ Discord **一位答完就先送一則**——三位要 2-3 分鐘，全跑完才送等於中間沒回饋。

### 三、🔴 兩次「材料缺一塊，他就填一塊」

**(a) 答錯股票**（上一則已記）：問「高力」答成「HIG」。

**(b) 編造公司業務**：首次軍議時**仲達說「高力是連接器/線纜廠」**——
實際是**熱交換器**廠。龐統答對，只是因為我當時只給了龐統公司資料。

⭐ 兩次是**同一個病**：材料少一塊，AI 就用自己的知識補上，而且**補得很自然**——
格式正確、語氣謹慎，只有那一塊是錯的。
⭐ **修法不是叫他「不要猜」，是把那塊補進材料。**
`_profile()`（yfinance longName/sector/industry/marketCap/businessSummary，免費、
台美股都有）接進 `_one_stock_block()` 與 `material_kongming()`——
**只要有軍師會提到某一檔，就一定拿得到那檔的真實業務描述。**

順帶：Leo 問「高力在做什麼的」龐統原本答不出來，也是同一個缺口造成的
（材料只有新聞快訊）。補上之後第一句就答到「板式熱交換器、甲醇燃料電池、
氫氣產生器、**液冷解決方案**」——最後那項跟 AI 資料中心有關，原本完全給不出來。

### 四、精簡與排版（Leo 兩次反饋）

**「有一段囉嗦不太像軍師」** → 加精簡規則：第一句就回答問的那件事、
同類雜訊合併成一句（不要每則後面掛「（這是大盤新聞）」）、
不要固定段落（「沒有的部分」有話講才寫）、同一件事只講一次。

**「可以多一些圖示或段落嗎，現在有點難閱讀」** → 加排版規則：
判斷用燈號開頭（🟢🟡🔴⚪，沿用投資站語彙不自己發明）、
**事實用 `・`／推論用 `→`**（符號就夠，不用再寫標籤）、
段落之間空一行、一行不超過 40 字、數字加千分位、
標題用粗體不用 `#`、免責用 ⚠️ 單獨一行。

⚠️ 順帶修一個算數錯：仲達同一段裡寫「共 7 檔（另 2 檔非持股）」又寫
「這 6 檔（…共7檔）」。改成材料直接給「觸發 9 條（持股 7 條、非持股 2 條）」。
⭐ **算數不是 AI 的強項，程式能算好的就不要留給它算。**

### 五、#軍議 貼了說明文章（Leo 要求，已由他釘選）

四位各自的職責、指令範例、`/軍議` 分派規則、怎麼讀回答（燈號語彙、`・`/`→`、
兩種要特別注意的免責），以及五條共同規矩。
⚠️ 程式置頂失敗 403——bot 沒有「管理訊息」權限。

### ⚠️ 過程中把檔案寫壞一次

用腳本寫多行字串時 `\n` 被轉成真換行，我寫的自動修復迴圈又一直往同一行黏，
**200 次還沒修好、越修越壞**。正解是 `git checkout` 還原到最後一次提交，
改用 Edit 工具重做（它不解析跳脫）。
⭐ **修復腳本沒有在每次迭代驗證「有沒有變好」，就會把小問題放大成大問題。**

---

## 2026-09-04（續三十）整合報告接上排程／9/4 目標價異動三張表／重疊截圖去重

### 一、`stock_brief.py` 從頭到尾沒有任何呼叫者

答 Leo「下一步還有什麼」時逐行讀了 `researcher_stock_sync.cmd`，才發現
**整合報告那支根本沒接上任何排程，也沒有其他程式 import 它**。

實際後果：Leo 丟一份新的券商 PDF 進資料夾 →
`advisor_reports.py parse` 會解析、`check` 會做失效檢查、日報會提到，
**但他真正會打開來看的那份 HTML 停留在我上次手動跑的版本**。

⭐ **「資料有更新」跟「交付物有更新」是兩件事**——這正是
`derived_files_stale_after_correction` 那條的同一個形狀，
只是這次不是我改了來源忘了重跑，而是**從一開始就沒有人會重跑**。

修法：
- `stock_brief.py` 加 `--all`：重產所有「有券商報告」的個股（現在 12 檔）
- 母體刻意只取有券商報告的，不取全部持股——沒報告的話這一頁上半部整段是空的，
  產出來只是把燈號頁的數字換個地方放
- 一檔失敗不影響其餘 N−1 檔（排程裡尤其重要），失敗才回非零
- 接進 `researcher_stock_sync.cmd`，排在 `advisor_reports` / `target_changes` /
  `investment_chief` **之後**——它只讀那幾步寫出的 state 檔

首跑 12 檔全成功，落在 obis `04_AI Report\Investment\`。

### 二、9/4 三張目標價異動表（日股／歐美股／台股）

解析 85 筆（日股 8、歐美 43、台股 10，加上 9/3 那兩張）。逐張核對過筆數與
可信券商筆數都對得上圖。

推播 3 筆：

| | 標的 | 券商 | 內容 | 為什麼推 |
|---|---|---|---|---|
| 🔻 | ON SEMI | Wells Fargo | 110 → 95 | 持股被調降 |
| ★ | BROADCOM | Goldman Sachs | 525 → 540 調升 | 可信券商 ∩ 母體 |
| 🔻 | APPLE | Morningstar | Downgrade to Sell | 持股被降評 |

母體內但券商不在可信名單 14 筆（存檔可查、不推），母體外 57 筆。

⚠️ **來源表本身的欄位問題這次又出現**，而且是新的變形：
`AVGO HSBC 600 → 560` 圖上標的是 **▲**（數字明明是下調）、
`AAPL 1250 → 285`、`緯穎 10000 → 3352.6 ↘ 但潛在幅度 +35.5%`。
維持既有設計——**方向以圖上的箭頭為準、不用兩欄相減自己推**，
因為 `TP Old` 欄的錯誤率比箭頭高。數字照樣標「未經覆核」。

### 三、🔴 重疊截圖造成重複提醒

首次跑出來的提醒裡，**「AAPL 被 Morningstar 降評為賣出」印了兩次一模一樣的行**。

原因：**每天那張表都會帶到前一天的資料**（9/4 的表裡同時有 9/4 和 9/3 的列），
9/3 丟過一次、9/4 又丟一次，重疊的列就被算成兩筆。
`all_rows()` 原本只是把每張圖的列攤平，沒有去重。

修法：以「日期＋代號＋券商＋評等＋新目標價」當身分去重，留第一次讀到的那份，
後來的來源檔名記進 `_dup_src` 保留可回溯性。
去重後 4 → 3 筆提醒、母體內 16 → 14、母體外 65 → 57。

⭐ **只要來源是「每天一份、內容有重疊」的東西，攤平就一定要去重**——
這個坑會隨著丟圖天數線性放大，第 N 天的重複量是 N−1 倍。

### 六、整合報告索引頁（Leo：「補吧」）

`stock_brief.py --index`（`--all` 跑完也會自動產一份）→
obis `04_AI Report\Investment\整合報告索引.html`。

一列一檔，六欄：**代號/名稱｜券商報告份數＋是哪幾家＋最新日期｜報告目標價＋距現價％
｜查核幾條對得上/有落差/等財報｜失效線有沒有被觸發｜我們自己的資料（燈號、投資長、異動）**。

排序＝**失效線被觸發的排最前**，其次查核落差多的，再來報告份數多的。
觸發的那列整列底色會轉紅。首跑 12 檔、0 檔觸發，2454 排第一（3 條落差）。

設計上的三個取捨：

1. **母體只取「有券商報告」的 12 檔，不是全部 88 檔持股。**
   沒報告的話這一頁上半部整段是空的，多 76 個空殼只會讓有內容的 12 份更難找。
2. **目標價取「最新一份報告」，不取最高的也不平均。**
   頁首寫明它跟風報比用的共識目標價**不同源、不要互相對照**——
   7750 五家差 35% 就是因為用不同年份的 EPS，取平均會把這個資訊抹掉。
3. **索引刻意重跑一次 `gather()`／`factcheck`**，跟 `--all` 有重複計算。
   換來的是索引不依賴 `--all` 的內部狀態、`--index` 單獨跑得起來、
   改 `render()` 不會連帶弄壞索引。12 檔幾秒鐘，不值得為此耦合。

手機版把表格轉成卡片（每欄自己一行、欄名用 `::before` 帶出來），
用 375px 實際看過。桌機／手機兩種寬度都截圖確認過才收工。

⚠️ **連結能不能點還沒驗證**——索引用相對路徑連到同資料夾的 12 份 HTML，
在檔案系統裡一定可以，但**手機用 Google Drive App 預覽 HTML 時相對連結會不會解析，
我沒有 Leo 的手機沒辦法測**。所以索引本身是設計成「不點進去也看得完」的儀表板，
連結只是加分。

---

## 2026-09-05（續三十一）補失效條件（涵蓋率 78/78）／obis 資料夾分層／三個順手抓到的靜默失敗

### 一、⚠️ 先更正：我上次講的「8 檔沒有失效條件、只有華通是真的漏掉」是錯的

華通 2313 **本來就有**條件在監控（9/3 登錄的三條），BRK.B 也有。

我數錯的原因：比對持股與登錄簿時**兩邊都用原字串**，而持股母體同時吐出
`2313` 和 `2313.TW` 兩種寫法、登錄簿只有 `2313.TW`，於是裸的那個看起來沒被涵蓋。

⭐ **`thesis_check` 每天都會印「持股涵蓋率 76/78」這一行，答案本來就在那裡**，
我卻自己重寫一段比對邏輯去算，還算錯。
**系統已經算好的東西不要自己重算一遍**——這是 8/31 那條規則的第五次變形。

真正缺的是 **2303 台光電**和 **UHS**：它們的趨勢條件當天剛被觸發，剩下的只有
「品質關轉通過」這種要等財報的條件，程式每天檢查不到 → 等於沒在監控。
補建後**涵蓋率 78/78**。

### 二、🔴 舊論點沒被新判斷蓋掉（找上面那個錯的時候發現的）

`_register_conditions` 直接拿 `v["ticker"]` 當 key，而同一檔在不同天可能被寫成
`2454` 或 `2454.TW`（看那天材料從哪個來源進來）→ **新判斷是「新增」不是「覆蓋」**。

2303／2454／2731 各留著一份 9/3 的舊論點且狀態仍為 active，而日檢是逐筆走
`reg.items()`，等於**拿投資長已經換掉的舊條件去報「你的判斷基礎不成立」**。

⭐ **覆蓋寫入只在 key 完全相同時才叫覆蓋——key 沒正規化，覆蓋就是新增。**

修法：`normalize_registry()` 收斂 key 並保留 `source_date` 較新的那份，
**寫入端與讀取端都呼叫**，跑一次就自己收斂（不用一次性搬遷腳本）。205 → 202 檔。

### 三、obis `04_AI Report/Investment` 分四層（Leo：「幫我整理一下」）

最上層 31 個散檔 → 4 個資料夾：

```
Investment/
├── 每日看板/        7 份，程式每天／每週覆寫
├── 個股整合報告/     索引 + 12 檔
├── 產業鏈深度報告/   原本就有
└── 存檔/           一次性報告（含 老墨三段時程/ 6 份）
```

**分法是「誰會再寫它一次」，不是主題**：會被覆寫的進每日看板（看到舊日期就代表
那支壞了），一次性的進存檔（舊日期是正常的）。兩者混在一起時，
**「這個檔案過期了」跟「這個檔案本來就是那天的」分不出來**——這正是下面第四點
那個一個月沒人發現的 bug 能藏這麼久的原因。

🔴 **只搬檔案不改程式的話，隔天 08:45 排程會把它們重新寫回最上層**——整理當天有效、
隔天自動失效，而且看起來會像 Leo 自己又把檔案丟出來。
這個路徑原本被 **11 支程式各自寫死一次**，全部改成 `import obis_paths`。
⭐ **同一個常數散在 N 個檔案裡，就代表之後一定會漏改其中幾個**（這個形狀在本專案
踩過四次了：財報卡舊公式、RRG 寫死的 52/26、失效條件的 key、這次）。

### 四、順手抓到的三個靜默失敗

**① 財報深度分析停在 8/4，整整一個月**
每日排程跑的是 `earnings_index.py -o docs/earnings.html`，**沒帶 `--obis`**，
所以只重建網站那份，Leo 手機上開的那份沒動。而這一步**每天都回報成功**。
已補 `--obis`，重產後 28 份。

**② 美台股看板.html 沒有任何東西會再產生它**
`board_html_legacy` 只被其他程式 `import` 拿 `supertrend()` 用，它的 HTML 產出
沒有任何排程。搬進存檔並在檔名標「已停產」。
⚠️ 有先查兩層（排程 + import）才判定，因為 8/31 差點誤刪 `st_alert.py` 就是只查了一層。

**③ `investment_chief` 取現價時台股判斷只認裸代號**
判斷式是 `^\d{4,6}[A-Z]?$`，`fill_missing()` 刻意傳原始寫法 `2303.TW`（它的註解裡
就寫「用原始寫法」）→ 落到美股分支被當成 `BRK.B` 那種寫法 → `2303-TW` →
yfinance 404 → 現價靜默變 None → 材料印「查無現價」→ 兩個角度一起被拖成資料不足。
⭐ **同一晚踩第二次同一件事：帶不帶 `.TW` 是同一檔**，任何分辨台股/美股的判斷
都要兩種寫法都認。

### ⚠️ obis 不在的機器上不可以先建資料夾

`portfolio_html` / `buffett_html` / `industry_rotation` **也會在 GitHub Actions 的
Linux runner 上跑**，那裡 `C:\Users\...` 只是一個普通字串。
原本的寫法是開檔直接失敗、被呼叫端 try/except 吞掉，沒事；
但只要在寫檔前先 `makedirs`，**Linux 會真的建出一個名字裡有反斜線的資料夾**，
把 6MB 的 HTML 寫進去，然後可能被 `git add` 掃進公開 repo。

`obis_paths.available()` 判斷 obis 在不在，不在就什麼都不建。實測模擬過。
⭐ **加「先建資料夾」這種看起來無害的一行，會把原本安全的失敗變成成功的錯誤。**

---

## 2026-09-05（續三十二）持股出場訊號標紅＋補「存量」視角

### 一、Leo 要的：SuperTrend 翻空／RS 跌破 60MA 針對持股特別標示

**為什麼需要**：9/5 日檢一次觸發 40 條，但其中 **32 條意思是相反的**
（RS 站回均線＝之前的出場疑慮解除），真正該看的只有 TSLA、GOOG 兩條，
而版面上它們長得一模一樣。

**做法**：`thesis_check` 的 triggered 多帶一欄「條件型別」，日報與仲達把
持股的 `EXIT_TYPES`（`supertrend_bear` / `supertrend_flip` / `rs_below`）
抽出來，用 🔴 排在最前面、獨立成一組。

⚠️ **`price_above` 刻意不收。** 同一個型別兩種意思——BEN 身上一條是
「漲破貴價 45.30 → 出場」，另一條是「站回翻空點 35.55 → 減碼訊號解除」。
⭐ **只把「型別本身就等於方向」的兩型標紅，寧可少標不要標錯。**
（我原本想提「幫每條加一個方向欄位」，但那要動投資長 prompt 且舊的 588 條沒有
方向可補；先做這兩型是零風險的一半。）

### 二、順著 Leo 追問補的「存量」視角

Leo 接著問「**現在美股持股有哪些 SuperTrend 翻空？**」——查完才發現
**系統沒有任何地方看得到這個**，只能一天一天從觸發事件裡拼回來。

根因：`triggered` 是「今天剛發生的事件」。多數翻空的持股是更早翻的，
甚至**登錄條件時就已經是空方**（那種會被歸到 `pending_metric`，不算新觸發），
所以它們**永遠不會再出現在事件流裡**。只看事件流會回答成「今天沒事」。

⭐ **事件（今天變了什麼）跟存量（現在是什麼）是兩個問題，只做事件會漏掉存量。**

新增 `exit_state`：每檔持股現在的 ST／RS 狀態。首跑 78 檔持股——
**兩條都成立 24、只有 ST 翻空 5、只有 RS 跌破 14、兩條都沒事 34、算不出來 1**
（SPCX，明寫「不等於沒事」）。日報給一行計數＋最嚴重那格的代號，
仲達拿到完整四組清單。

### 三、標紅那條路徑今天沒有持股觸發 → 用假資料逐案驗

硬規則「沒執行過的路徑不算能用」。今天實際觸發的三條都不是持股，
正常跑一次根本不會經過那段程式。用假資料驗四種情況：

| 情況 | 期望 | 結果 |
|---|---|---|
| 持股 + `rs_below` | 🔴 標紅置頂 | ✅ |
| 持股 + `rs_above`（解除） | 維持 🚫 不標紅 | ✅ |
| 持股 + `price_above`（有歧義） | 維持 🚫 不標紅 | ✅ |
| 非持股 | 只進公開版 | ✅ |

### ⚠️ 我在回答裡把 2303 講成台光電，實際是聯電

台光電是 2383。這個錯誤沒有進到程式或資料——日報自己查證交所簡稱印的是
「2303 聯電」，是我口頭講錯。

### 順帶：2303 的 9/5 判斷是在現價抓不到的情況下做的

`fill_missing` 補建 2303 那次，正好踩到當晚才修的 `2303.TW → 2303-TW` 符號 bug，
所以投資長是在「查無現價」的材料下寫的判斷（它的趨勢角度寫「RS 未跌破」，
但同一天 `thesis_check` 算出來是**已跌破**）。修好符號後重跑該檔。
⭐ **材料缺一塊，AI 就自己填一塊**——又一個實例，而且這次是缺現價。

---

## 2026-09-05（續三十三）把「手寫的判讀」變成規則：查核層 6 → 9 項 + 通用中文重點頁

### 起點：我自己講錯了一次

做完高盛 2454 的中文重點頁之後，我跟 Leo 說「這份的判讀是手寫的，做成自動版
會比較差」。Leo 追問「這個的意思是什麼」，我回頭把那三條發現攤開，才發現
**它們全部都是算術**：

| 我以為是「判斷」的 | 實際上是 |
|---|---|
| 報告的 CAGR 對不上它自己的表 | 開 n 次方，跟內文寫的比 |
| 2026 是空白年 | 逐年拆 EPS YoY |
| 上檔全來自倍數 | 把 目標價/現價 拆成兩項相乘 |

⭐ **能算的就不要讓 AI 用講的。** 模型寫的判讀每次都不一樣、而且不會自己去驗算；
規則寫一次，16 份既有報告和之後每一份都適用，還能被回頭檢查。

⭐ 更該記的是**我一開始的直覺是錯的**——我把「我當時是用想的」當成「這件事只能用想的」。
**做過一次跟只能手做，是兩回事。**

### A. `report_factcheck` 六項 → 九項

三條新規則的共同點：**完全不需要外部資料，拿報告的兩個地方互相對帳**
（跟既有的「倍數 × 基數 ≈ 目標價」同一招）。

**① 複合成長率**：內文寫的 CAGR vs 拿它自己的預估表重算。
2454 實例：內文「2025-28E 營收/獲利 CAGR 43%/59%」，但表格 595,966 → 2,483,426
開三次方是 **60.9%**、EPS 是 **86.2%**。對得起來的算法是把三年當成**四期**複利。
→ 券商常見的期數寫法差異，不是造假，但**只讀那句話沒看表的人會低估這份報告押了多大**。

**② 有沒有空白年**：逐年拆 EPS YoY。2454 的 2026E 營收 +14.5% 但 EPS 只有 **+0.8%**，
爆發全押在 2027（+177.6%）。⭐ **這件事看 CAGR 完全看不到——平均會把空白年抹平。**

**③ 上檔空間的來源**：拆成「盈餘成長 × 倍數變化」。
2454：盈餘要 +320%、倍數要壓縮 −62%，相乘 = 上檔 +58.6%。

解析 schema 加 `cagr_claim`；舊報告沒有這欄時退回從摘要文字比對，
**抓不到就顯示「還不能驗」，不從不可靠來源湊數字**。16 份全跑過，0 失敗。

#### 🔴 首測抓到自己的 bug：一組數字配到多個欄位，會互相亂配

宣稱值是一組 `[43, 59]`（營收/獲利），原本**每個欄位都拿整組去比**，於是
「營收三年複利 60.9%」被組裡的 **59**（那是 EPS 的）在誤差內比中 →
產生一個**假的 ✅對得上**。
⭐ **「有一個對得上」跟「對的那個對得上」是兩回事。** 數量吻合時照順序配。

#### 另外兩個修
・「現在的 EPS 基數」原本取表上第一列，但第一列常常是**去年的實際數**（2454 取到 2025）。
　改成優先取今年那一列。
・新的「上檔來源」跟既有的「估值倍數」看起來會互相矛盾（一個說倍數要壓縮 62%、
　一個說要擴張 59%）——**兩者用的 EPS 基數不同，都對**。在註解裡明說，
　不然同一張表自相矛盾。

### B. `report_zh.py`：一份報告一頁中文重點

`--all` / `<代號>` / `--broker` / `--list`，已接進 08:45 排程（排在 stock_brief 之後）。
跟 `stock_brief` 的差別：那頁答「這檔現在怎麼看」，這頁答
「**這份報告在說什麼、哪裡站不住腳**」。首跑 12 檔全成功。

#### 開頭那段，我改了兩次
第一版只列查核抓到的**第一條**落差。結果 2454 那份剛好把 CAGR 那兩條擠掉——
**而那正是 Leo 覺得有價值的一條**。改成前三條，還是擠掉。
最後改成**全部各列一行**。
⭐ 我沒有一套有依據的嚴重度排序，**所以就不排**——寧可多幾行，
不要替他決定哪一條不用看。

### 順帶修掉的
`**粗體**` 星號直接印在畫面上（`board_theme.esc_b()`），以及三處我自己在
字串裡寫死的 markdown。25 頁全掃過，殘留 0 處。

---

## 2026-09-05（續三十四）老墨上課三條規則的落地：一條已在跑、一條回測沒優勢、一條有撞名陷阱

Leo 帶回老墨的課堂重點（三條進出場 + 三條報告可信度），要求「看怎麼應用到現有系統」。
逐條對照後只有兩件事需要動手，而動手的過程抓到我自己一個嚴重的方法論錯誤。

### 一、先查現況，三條進出場只有一條要做

| 老墨說的 | 現況 |
|---|---|
| ② 跌破 RS60 = 賣點 | **已完全實作**：投資長失效條件 `rs_below`／模擬倉「RS 跌破全出」／9/5 剛加的紅色標示與存量表。78 檔持股裡 38 檔已跌破 |
| ① 閃電 + 10 日內 RS 粉紅/藍點 | 零件全有（`squeeze_momentum`／`rs_signal_series`／「剛噴出」的定義早就寫在回測檔裡），**但沒有任何地方每天掃這個組合** |
| ③ 報告 + 四燈 + 風報比>1 | 未做。⚠️ 現有 `COMBO_MIN=3` 不是 4，**沒有去動它**——那會改變模擬倉母體與所有既有排序 |

### 二、閃電的定義：回去查老墨自己的逐字稿，發現我只對一半

原本要問 Leo，他說「可以讀一下老墨的 YouTube 逐字稿」。查出來我們 **9/3 就抓過**
（obis `youtube_learning/2026-09-03_..._老墨XQ看盤_學習筆記.md`），零成本。他的原話：

> 充能爆發指標：市場沒方向時進入「擠壓」狀態，擠壓到最後會「**釋放（閃電符號）**」，
> **看釋放後變紅柱還綠柱決定方向**。搭配 SuperTrend 使用。

⭐ **閃電 = 擠壓釋放**（我猜對了），但**釋放本身不是方向**——釋放後動能柱紅(>0)
才是偏多。只掃「釋放」會把往下噴的那一半也當成買訊。**我原本要做的版本會錯一半。**

⭐ 元教訓：**要問使用者之前，先確認自己手上有沒有答案。** 那份逐字稿躺在 obis
兩天了，是我自己叫程式抓的。

### 三、🔴🔴 我的回測有前視偏差，第一版數字全部作廢

第一版跑出來非常漂亮，而且**完全符合預期**：

| | 10 日 | 20 日 | 勝率 |
|---|---|---|---|
| ③ 老墨全套（486 訊號） | **+5.01pp** | **+5.03pp** | **75.9%** |
| ① 只有閃電（5,662） | −0.39pp | −0.44pp | 53.1% |
| ② 閃電＋紅柱（2,853） | −0.37pp | −0.68pp | 53.3% |

結論看起來很有說服力：「閃電本身沒用，做工的是 RS 那個點」。**我差點就這樣報出去。**

**錯在哪**：條件是「閃電**之後** 10 日內出現 RS 點」，但我從**閃電那天**開始算報酬。
而 🔵 的定義是「RS 創 120 日新高」——**RS 要創新高股價通常已經先漲**，
那 10 天的漲幅就被算進「訊號後報酬」裡。而且閃電當天根本不知道後面會不會出現那個點。

⭐⭐ **用未來才知道的條件挑進場點，再從條件成立之前開始算報酬＝前視偏差。**

修法：進場日改成 **RS 點出現那一天**（那天才知道），報酬從那天算。修完：

| | 前視版（錯） | 修正版（對） |
|---|---|---|
| ③ 10 日 | +5.01pp／勝率 75.9% | **−0.24pp／勝率 53.4%** |

**整整 5 個百分點全部來自那 10 天的價差。**
⭐ 反過來這說明一件有用的事：**行情發生在 RS 點出現之前**。等 🔵/🩷 畫出來那段已經走完
——RS 點是**確認**不是**預告**。

### 四、修正後的完整結果：四個版本都沒有優勢

5 年、193 檔守備清單、基準＝同池同期「任意一天進場」（18.6 萬筆）：

| 版本 | 訊號 | 10 日 | 20 日 | 60 日 |
|---|---|---|---|---|
| 基準 | 186,464 | +0.87% | +1.67% | +4.97% |
| ① 只有閃電 | 5,662 | −0.39pp | −0.44pp | −1.24pp |
| ② 閃電＋紅柱 | 2,853 | −0.37pp | −0.68pp | −2.15pp |
| ③ 閃電→RS 點（點那天進） | 486 | −0.24pp | −0.80pp | −3.52pp |
| ④ **RS 點先→再閃電**（閃電日進） | 274 | **+0.09pp** | **+0.03pp** | −2.90pp |

④ 是我加的變體：**兩個條件在進場當下都已成立**，完全知道得到，邏輯上更貼近
「資金先動、然後爆發」。它是四個裡最好的，但 10/20 日只是**打平**，60 日還輸 2.9pp。

#### ⚠️ 這個結果證明了什麼、沒證明什麼

**沒有證明老墨的方法沒用**，四個理由都是真的限制：
1. **我測的不是他的完整流程**——他是「先由上而下挑出當下轉強的產業 → 再找領頭羊 →
   才看指標」，我在**固定的 194 檔守備清單**上掃，少了最前面那層產業篩選。
2. **指標是我們重建的**，不是 XQ 原始碼；布林 20/2.0、凱特納 20/1.5、RS 60/240、
   新高回看 120 都是我們的預設值。參數不同就是另一個訊號。
3. **訊號診斷不是策略回測**：沒停損、沒部位管理、沒算成本。
4. **基準本身很強**——這 194 檔 5 年是上漲的，「任意一天買」60 日中位就 +4.97%。
   ⭐ **在一個持續上漲的池子裡，擇時訊號要贏「一直在場內」本來就很難**，
   這跟 5.5 年 SuperTrend 回測得到的是同一個形狀。

**它證明的是**：作為**在我們守備清單上單獨使用的進場觸發器**，這個組合不優於一直持有。

### 五、🔴 報告可信度：中信**不能**加進 TRUSTED（撞名陷阱）

老墨說「中信報告可信」。直覺是加進 `target_changes.TRUSTED`，但實查那張**每日目標價
異動表**出現過的 20 家券商，**中信投顧（CTBC）從來沒出現過**——那張表上是外資
（Morningstar 15／Goldman 6／JP Morgan 6…）加少數本土（SinoPac 5／KGI 3／元大 1）。

⚠️ 而照字面對過去，最接近的是 **`CITI`——那是花旗，不是中國信託**。
⭐ 這跟 `"gs"` 命中 `"mornin(gs)tar"` 是同一類，只是**名字更像、更難發現**。

→ 另開 `broker_credibility.py`，**兩個名單各管各的**。只標註不過濾，
每條標明「這是老墨的經驗判斷，不是我們回測出來的」。實測 CITI／Citigroup／
citigroup inc 都不會誤中；別名只放不會撞名的（`goldman` 安全、`gs` 絕對不行）。

・中信投顧（11/16 份）→ ★ 可信度較高　⚠️ **我們沒有驗證數據**：中信 11 份報告
　**全部不附目標價調整史**，無從回溯它準不準
・高盛 → ⚠️ 期望值偏高　**這條有數據佐證**：它自附的 17 筆調整史顯示
　目標價 +665% vs 同期股價 +344%
・統一 → 📼 需搭配法說會（目前 0 份報告，先放著）

### 六、順手抓到的小 bug

`fwd()` 沒擋 NaN → 18 萬筆基準樣本混到一筆壞資料，**整個「60 日基準平均」變 nan**，
而中位數若無其事。⭐ **一個 NaN 就能污染整組平均，中位數不會抗議——兩個一起看才看得出來。**
