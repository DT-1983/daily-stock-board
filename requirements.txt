# daily-stock-board 依賴
#
# 2026-09-01 精簡：原本這份是上游 ZhuLinsen/daily_stock_analysis 的 73 行
# 依賴表，含 efinance / akshare / tushare / pytdx / baostock / longbridge
# 等中國券商與行情源、lark-oapi(飛書) / dingtalk-stream / discord.py 等推播
# SDK、以及 fastapi / jinja2 / newspaper3k 等本專案完全沒用到的套件。
# 上游程式碼移除後全部不再需要，Actions 每次卻都在裝。
#
# 一併移掉的還有這行（每次 CI 都從上游作者的 GitHub 直接安裝程式碼）：
#   git+https://github.com/ZhuLinsen/alphasift.git@377049...#egg=alphasift
#
# 下列清單以 65 支 .py 的實際 import 靜態掃描產生，不是憑印象刪的。
# 間接依賴交給 pip 解析，不在這裡列。

# 資料源
yfinance>=0.2.0                      # 31 支在用，主力行情源
tradingview_screener>=3.0.0,<4.0.0          # data_tv.py / industry_rotation.py

# 資料處理
pandas>=2.0.0
numpy>=1.24.0

# 網路
requests>=2.31.0                     # 18 支在用
python-dotenv>=1.0.0                 # .env 讀取

# AI
litellm>=1.80.10,!=1.82.7,!=1.82.8,<2.0.0   # chain_themes.py / llm_board.py

# Discord bot（discord_bot.py，2026-09-03 新增，路線圖第1項階段2）
# ⚠️ 2026-09-01 才把上游帶的 discord.py 當死依賴移除，這次是真的在用，
#   之後任何「清掉沒人 import 的套件」都要先 grep discord_bot.py 再刪。
discord.py>=2.4.0,<3.0.0

# 文字處理
markdown>=3.5.0                      # board_html.py 解析報告用
opencc-python-reimplemented>=0.1.7   # llm_board.py 簡體字驗收、tw_report.py 簡繁轉換
#   ⚠️ 這兩個一定要留在 requirements.txt：2026-08-17 發現 portfolio-nav.yml
#   只裝 requirements.txt、沒有像 tw-board.yml 那樣額外 pip install，
#   結果每日模擬倉調倉靜默失敗多日。
