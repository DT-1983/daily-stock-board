# 环境变量文件（包含敏感信息，禁止提交）
.env
.env.*
*.env
*.env.local
.env.*.local

# 测试文件（可能包含敏感配置）
# 仅忽略仓库根目录下的临时 test_*.py 脚本；tests/ 目录下的单元测试需要纳入版本控制。
/test_*.py
/test.sh

# 本地评审与高级路由配置（可能包含敏感信息或临时分析产物）
/.codex
/review.md
/litellm_config.yaml
/litellm_config.*.yaml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 虚拟环境
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.cursorrules

# 数据和日志
/data/
/.llm_usage_hmac_secret
**/.llm_usage_hmac_secret
logs/
reports/
*.db
*.sqlite
*.sqlite3

# 系统文件
.DS_Store
Thumbs.db

# 测试
.pytest_cache/
.coverage
htmlcov/

local/

run.sh

verify_*.py

# ignore Claude workspace artifacts, but keep shared repo skills versioned
.claude/*
!.claude/skills/
!.claude/skills/**

# ignore static files
static/
/apps/dsa-desktop/dist/
/apps/dsa-desktop/node_modules/
docs/specs/
.fm_cache/
trade_plan.py

# 股價快取（price_store.py）：可重建的衍生資料，不進版控
# 296 檔 10MB、擴到 industry 細分類會到 1,400 檔約 50MB，每天都在變動，
# 進版控會讓 repo 迅速肥大且每次 commit 都是大 diff。
# ⚠️ 代價：GitHub Actions 上沒有快取可用（每次都是冷啟動重抓）。
# 本機排程有快取所以很快；Actions 那邊靠批次下載本身就已經快 8 倍，可接受。
state/price_store/

# 2026-09-01：投資判斷與研究筆記移出版控。
# 這個 repo 是 PUBLIC（GitHub Pages 免費方案的必要條件），而這兩個檔是孔明對
# 每一檔的完整判斷＋推理，其中 held=true 直接標明持有哪些。沒有金額股數，
# 但投資思路是完整的。實查 GitHub Actions 完全沒用到（只有本機 8 支 .py 讀），
# 移出版控零成本。歷史已公開，移除只能防未來。
# ⚠️ 光加這裡沒用，-f 會繞過 .gitignore——board_analyze_daily.cmd 的
#    `git add -f ... state ...` 與 researcher_stock_sync.cmd 的逐檔強制加
#    都已一併改掉。改動這三處要一起看。
state/advisor_verdicts.jsonl
state/research_notes.jsonl
# 2026-09-03 補漏：9/1 那次只移了上面兩個，**漏掉這個登錄簿**——它同樣每檔帶
# `held: true`，等於持股清單一直留在公開 repo 裡（實查遠端 board/main 版本確有
# 12 檔 held=true）。它是 investment_chief 登錄、thesis_check 每天讀的失效條件簿。
# 實查只有本機 4 支 .py 讀它（ask_advisor/investment_chief/thesis_check），
# GitHub Actions 沒用到，移出版控不影響管線。歷史已公開，移除只能防未來。
state/thesis_conditions.json

# 2026-09-03 交易記錄（私人，跟 advisor_verdicts 同一批不進公開 repo）
state/trade_journal.jsonl

# 個人持股清單（公開 repo 不可入庫，2026-09-03）
holdings.json

# 本機預覽用暫存頁
docs/_lk_preview.html

# 券商研究報告解析結果（第三方付費研究內容，公開 repo 不可入庫，2026-09-03）
state/advisor_reports.json
state/advisor_reports_today.json

# 券商目標價異動表解析結果（第三方研究內容，同 advisor_reports，2026-09-03）
state/target_changes.json
state/target_changes_today.json

# 月營收報告的統一投顧評論（2026-09-16）：comment 欄位是改寫過的券商評論，
# 跟 advisor_reports.json 同一個理由不能進公開 repo；revenue/yoy/mom 數字本身
# 是公開資訊，但整份檔案混在一起，不拆開處理。
state/monthly_revenue.json

# 產業筆記登錄簿（industry_notes.py，2026-09-16）：目前收的都是公開社群貼文
# 整理出來的事實，但跟 advisor_reports.json 同一個「輸入來源可能之後換成
# 機密內容」的道理一樣先擋起來，不要之後漏改這裡才驚覺已經進公開 repo。
state/industry_notes.json
bt_sector_gics.json

# 每週總體報告的週快照（2026-09-14）。理由跟上面兩個不同——這份**不是**敏感內容
# （全是 FRED/國發會/證交所的公開數字），純粹是「上週的值長怎樣」的執行期快取，
# 每週覆蓋一次、只用來算週變化。進版控只會讓公開 repo 每週多一筆無意義的 diff。
# ⚠️ 這是照 new_state_check.py 的要求做的**明確決定**，不是漏掉。
state/macro_weekly.json
bt_mofi_*.json
portfolios.*.before-*.json

# 帳戶 → 顯示標籤（含家人姓名與券商分公司，不可進公開 repo）
account_labels.json

# 軍師對話（私人：持股、成本、在煩惱什麼——比燈號本身更敏感）
state/war_room_chats.jsonl
state/war_room_sessions.json

# 個股簡介快取（yfinance 業務說明濃縮，非私人但會一直長）
state/company_intro.json

# 財報日曆快取（yfinance 公布日/下次預估）
state/earnings_calendar.json

# 俗貴價對帳（含持股清單，不進公開 repo）
state/mikeon_crosscheck.json
state/mikeon_crosscheck_before_fix.json
state/mikeon_crosscheck_history/

# 2026-09-08 本機重啟服務時的暫存 log／備份檔，不進 repo
bot_restart.log
bot_restart.log.err
*.bak
portfolios.json.*.bak

# 讀私人失效條件登錄簿的本機工具（跟 trade_plan.py 同級）。
# 註解裡帶有實際持股代號與門檻價 —— 程式碼那層也算外流。
thesis_repricing.py

# 每天變動的運行狀態，不進版控（發送紀錄／量測樣本）
state/daily_warroom_sent.json
state/us_close_probe.jsonl
state/warroom_watchdog_attempts.json

# 每週總體表態紀錄（2026-09-14）：給陳壽回測用的累積樣本。
# 跟 advisor_verdicts 同一批不進公開 repo。⚠️ 它同時因隱私被 gitignore，
# 所以**只靠 tools/backup_ai_projects.py 保存**——那支的副檔名白名單
# 已於同日補上 .jsonl，動那份白名單前要先想到這裡。
state/macro_stance_log.jsonl

# 本機專用的外部資料抓取工具，不進版控（理由見 obis 私人開發日誌 2026-09-14）
video_transcript.py

# 2026-09-16：這支把券商影音報告的內容直接寫死在程式碼裡（產業分析文字、
# 逐字稿摘要），不是只有「怎麼抓」，是連「抓到的機密內容本身」都在原始碼裡——
# 跟 code_is_data_privacy 那條規則是同一件事，比 video_transcript.py 更嚴重，
# 絕對不能進公開 repo。obis 私人區的 HTML 產出物才是給 Leo 看的那份。
bulk_shipping_report.py

# 2026-09-16：同上一條，這支也把統一投顧的個股評論直接寫死在程式碼裡。
revenue_report.py
